---
phase: 03-video-preservation-fallback-control
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/domain/inspect-summary.ts
  - src/domain/cli-request.ts
  - src/cli/command.ts
  - src/cli/render.ts
  - src/cli/main.ts
  - src/app/inspect.ts
  - test/domain/inspect-summary.test.ts
  - test/app/inspect.test.ts
autonomous: true
requirements:
  - VIDEO-02
  - VIDEO-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 03-2026-05-02T00-05-47
generated_at: "2026-05-02T00:12:00.000Z"
must_haves:
  truths:
    - "`InspectPlanSummary` exposes deterministic `preservationNotes` bullets derived from modality + stable reasonCodes."
    - "`inspect` exits with `fallback-required` when plan modality is `fallback-required` unless `--allow-video-fallback` is passed."
    - "Passing `--allow-video-fallback` returns exit 0 success while still emitting the same summary payloads for scripting review."
---

<objective>
Layer **VIDEO-02** explanatory inspect output and **VIDEO-03** CLI policy atop Plan 03-01: summaries must articulate consequences (`would require …` for fallback, `Stream-copy …` line for copy-safe) while automation denies fallback plans unless `--allow-video-fallback` acknowledges them explicitly.

Artifacts: richer `InspectPlanSummary` (`preservationNotes` array), Commander flag plumbing, orchestration enforcement in `runInspectRequest`, `renderInspectPlanText` subsection **`Preservation notes`**, Bun tests for builder + mocked ffprobe workflows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/domain/command-outcome.ts
@src/app/inspect.ts
@src/domain/inspect-summary.ts
@src/domain/output-plan.ts
@src/cli/command.ts
@src/cli/main.ts
@src/cli/render.ts
@src/domain/cli-request.ts
@test/app/inspect.test.ts

<summary_fields>
Extend `InspectPlanSummary` with **`readonly preservationNotes: readonly string[]`** on every modality.

Mandatory builder semantics (implement so tests grep reliably):
1. `fallback-required` plans include at least one note containing substring **`would require`** (video transcoding or non-copy-safe remux wording).
2. `video-copy-safe` plans populate **≥ 1** note containing substring **`Stream-copy`** (capital **S**, hyphenated **Stream-copy**).
3. `audio-only` plans either use `[]` or include substring **`audio-only`** (lowercase phrase allowed inside sentence).

Expose **`buildPreservationNotesFromPlan(plan: OutputPlan): readonly string[]`** from `inspect-summary.ts` so unit tests can target it directly.

VIDEO-03 gate pseudocode (**exact failure message literal required in `src/app/inspect.ts`**):

```typescript
if (plan.modality === "fallback-required" && !request.allowVideoFallback) {
  return {
    kind: "failure",
    reason: {
      kind: "fallback-required",
      message:
        "Planned output requires video preservation fallback approval. Pass --allow-video-fallback to acknowledge.",
    },
  };
}
```

Commander `.option("--allow-video-fallback", "...description...", false)` → request field **`allowVideoFallback`** boolean.

Hard cap **`MAX_PRESERVATION_NOTES = 5`** inside builder to mitigate runaway bullets (truncate deterministically oldest-first documented in comment).

Rendering: textual inspect prints labelled section **`Preservation notes`** beneath reason codes listing each note prefixed with `- `.
JSON success payload includes **`preservationNotes`** array unchanged from summary object.
</summary_fields>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect summary preservation notes builder</name>
  <files>src/domain/inspect-summary.ts, test/domain/inspect-summary.test.ts</files>
  <read_first>
    - src/domain/inspect-summary.ts
    - src/domain/output-plan.ts
    - .planning/phases/03-video-preservation-fallback-control/03-RESEARCH.md
  </read_first>
  <action>
    Add `preservationNotes` plus exported `buildPreservationNotesFromPlan`. Wire `outputPlanToInspectSummary` to include builder output across `unsupported`/supported modalities.

    Create `test/domain/inspect-summary.test.ts` asserting substrings **`would require`**, **`Stream-copy`**, and audio-only/`unsupported` expectation paths using small literal `OutputPlan` objects typed inline (mirror union discriminant branches).
  </action>
  <verify>
    <automated>bun test test/domain/inspect-summary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MAX_PRESERVATION_NOTES" src/domain/inspect-summary.ts`
    - `rg -n "buildPreservationNotesFromPlan|preservationNotes" src/domain/inspect-summary.ts`
    - File exists `test/domain/inspect-summary.test.ts`
    - `bun test test/domain/inspect-summary.test.ts` exits 0.
  </acceptance_criteria>
  <done>Pure domain builder locked by tests independent of subprocess noise.</done>
</task>

<task type="auto">
  <name>Task 2: CLI flag plumbing + VIDEO-03 policy gate</name>
  <files>src/domain/cli-request.ts, src/cli/command.ts, src/cli/main.ts, src/app/inspect.ts</files>
  <read_first>
    - src/domain/cli-request.ts
    - src/cli/command.ts
    - src/app/inspect.ts
    - src/cli/main.ts
  </read_first>
  <action>
    Extend CliRequest inspect variant with **`readonly allowVideoFallback: boolean`**.

    Commander: add `.option("--allow-video-fallback", "Acknowledge fallback-required preservation plans instead of exiting non-zero.", false)`; map **`Boolean(options.allowVideoFallback)`** into request.

    Implement gate after `planMediaOutput` resolves: blocked path MUST omit `inspect` success payload entirely; acknowledgement path behaves like Phase 2 success with enriched summary.

    Update `cli/main.ts` handler wiring accordingly.
  </action>
  <verify>
    <automated>bun test test/app/inspect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\\-\\-allow-video-fallback" src/cli/command.ts`
    - Exact message string `Planned output requires video preservation fallback approval. Pass --allow-video-fallback to acknowledge.` occurs in `src/app/inspect.ts`.
    - `rg -n "allowVideoFallback" src/domain/cli-request.ts src/cli/main.ts`
    - Tests prove: default denies fallback-required plan with `fallback-required` outcome; flag flips success.
    - Achieve by mocking `runInspectRequest` deps with ffprobe stdout returning JSON built from probes that intentionally trigger `fallback-required` per Plan 03-01 feasibility (dual video stream OR `codec_name":"vp9"` etc.).
    - `bun test test/app/inspect.test.ts` exits 0.
  </acceptance_criteria>
  <done>Automation observes stable exit taxonomy for policy denial vs acknowledgement.</done>
</task>

<task type="auto">
  <name>Task 3: Render preservation notes subsection</name>
  <files>src/cli/render.ts</files>
  <read_first>
    - src/cli/render.ts (`renderInspectPlanText`)
    - src/domain/inspect-summary.ts
  </read_first>
  <action>
    Append section:

```
Preservation notes
- …
```
after `Reason codes` block when array non-empty (skip heading entirely when array empty to avoid noisy output).

Stretch goal (optional if trivial): assertion in `inspect.test.ts` inspecting rendered text substring **`Preservation notes`** for fallback-required success-with-flag path OR dedicated render test harness if present.
  </action>
  <verify>
    <automated>bun test test/app/inspect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Preservation notes" src/cli/render.ts`
    - `bun test test/app/inspect.test.ts` exits 0 after renderer change.
  </acceptance_criteria>
  <done>Human-readable inspect stays aligned with JSON machine view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv parsing | Commander boolean flags only—argv arrays stay shell-free per Phase 1 trust model. |
| Inspect outcome propagation | Fallback failures remain typed `fallback-required`; renderer cannot coerce them into success banners. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation Plan |
|-----------|----------|-----------|----------|-----------------|
| T-03-02-01 | Elevation | `--allow-video-fallback` acknowledgement | medium | Explicit long flag naming; denial default persists; MESSAGE literal documents required companion flag. |
| T-03-02-02 | Tampering | Note builder injection via reason codes | low | Builder uses closed template map + `MAX_PRESERVATION_NOTES` cap; avoids concatenating unchecked user filenames. |
| T-03-02-03 | Information disclosure | Overly verbose remediation text | low | Single sentence remediation referencing flag; excludes raw FFmpeg output. |
</threat_model>

<verification>
`bun test test/domain/inspect-summary.test.ts test/app/inspect.test.ts` followed by **`bun run verify`** gate from package scripts.

</verification>

<success_criteria>
VIDEO-02 surface guidance and VIDEO-03 opt-in semantics are exercised by automated tests alongside renderer updates.

</success_criteria>

<output>
Refresh `.planning/phases/03-video-preservation-fallback-control/03-02-SUMMARY.md` post-execution tying tasks to REQ coverage.

</output>
