---
phase: 03-video-preservation-fallback-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/media-probe.ts
  - src/domain/stream-copy-feasibility.ts
  - src/domain/output-plan.ts
  - test/domain/output-plan.test.ts
  - test/fixtures/ffprobe/minimal-video-audio.json
autonomous: true
requirements:
  - VIDEO-01
  - VIDEO-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 03-2026-05-02T00-05-47
generated_at: "2026-05-02T00:12:00.000Z"
must_haves:
  truths:
    - "`video-copy-safe` is emitted only when the narrow v1 rule matches: exactly one video stream, `codec_name` lowercased is `h264`, `format.format_name` is present, and planned defaults remain AAC audio into MP4 (VIDEO-01)."
    - "Any other lone-video codec, multiple video streams, or missing `format.format_name` yields `fallback-required` with deterministic `reasonCodes` from RESEARCH §Reason-code taxonomy (VIDEO-02 plan-time)."
    - "Phase 2 stub reason `phase-2-stub-video-copy-safe` is removed from nominal success paths; audio-only retains `phase-2-stub-audio-only`."
  artifacts:
    - path: src/domain/stream-copy-feasibility.ts
      provides: evaluateStreamCopyFeasibility
      contains: video-fallback
    - path: src/domain/output-plan.ts
      provides: planMediaOutput
      contains: video-copy-h264-mp4-v1
    - path: test/domain/output-plan.test.ts
      provides: fixture-backed regression suite
---

<objective>
Deliver **VIDEO-01** truthful stream-copy modality classification before execution by replacing Phase 2’s optimistic mixed A/V stub, and scaffold **VIDEO-02** structured `reasonCodes` for fallbacks plus copy-safe confirmations at the pure domain layer (`planMediaOutput`).

Output: new pure feasibility evaluator, tightened `planMediaOutput` integration, augmented probe schema (`format.format_name`), updated fixtures, and deterministic unit tests asserting whitelist / multi-video / codec / metadata gaps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-video-preservation-fallback-control/03-CONTEXT.md
@.planning/phases/03-video-preservation-fallback-control/03-RESEARCH.md
@.planning/REQUIREMENTS.md
@src/domain/media-probe.ts
@src/domain/output-plan.ts

<interfaces_hint>
Expose a **pure function** callable from `planMediaOutput` after audio stream selection—for example:

```typescript
export function evaluateStreamCopyFeasibility(args: {
  readonly probe: MediaProbe;
  readonly plannedContainer: PlannedContainer;
  readonly plannedAudioCodec: PlannedAudioCodec;
}): ...
```

Planner implementation detail: consolidate return shape with **discriminated unions** avoiding optional foot-guns (`kind: "video-copy-safe" | "fallback-required"` + `reasonCodes` + optional `fallbackKind` literals).

**Whitelist:** Exactly one video stream, `codec_name.toLowerCase() === "h264"`, `probe.format?.format_name` trimmed string length > 0, `plannedContainer === "mp4"` ( literals ) when video exists—the current Phase 2 default path.

**Regression removal:** Eliminate emitting `phase-2-stub-video-copy-safe`; replace passing case reason with `video-copy-h264-mp4-v1`.

**Important:** Preserve existing audio-only (`phase-2-stub-audio-only`) and unsupported `no-audio-stream` semantics unchanged from Phase 2 tests unless CONTEXT explicitly evolves them—only the video stub path changes.</interfaces_hint>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe schema gains format_name</name>
  <files>src/domain/media-probe.ts, test/domain/output-plan.test.ts, test/fixtures/ffprobe/minimal-video-audio.json</files>
  <read_first>
    - src/domain/media-probe.ts (ffprobeFormatSchema block)
    - test/fixtures/ffprobe/minimal-video-audio.json
    - test/domain/output-plan.test.ts (fixture composition patterns)
    - .planning/phases/03-video-preservation-fallback-control/03-RESEARCH.md (metadata gate)
  </read_first>
  <action>
    Extend Zod schema `ffprobeFormatSchema` so `MediaProbe.format` retains optional **`format_name`** as `z.string().optional()` (keep `passthrough`). Update `minimal-video-audio.json` `"format"` object to include a realistic **`format_name`** string such as **`"mov,mp4,m4a,3gp,3g2,mj2"`** (literal must appear exactly for grep acceptance). Adjust any assertions that stringify entire probe objects expecting exact JSON equality if brittle (prefer semantic comparisons).
  </action>
  <verify>
    <automated>bun test test/domain/output-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'format_name' src/domain/media-probe.ts`
    - `rg -n "mov,mp4,m4a,3gp,3g2,mj2" test/fixtures/ffprobe/minimal-video-audio.json`
    - Fixture still parses via `parseFfprobeJson` in at least one test path (explicit or via `planMediaOutput` importing typed probe).
    - `bun test test/domain/output-plan.test.ts` exits 0 after dependent tasks merge.
  </acceptance_criteria>
  <done>Format-level metadata flows into domain without weakening parse strictness unnecessarily.</done>
</task>

<task type="auto">
  <name>Task 2: Pure stream-copy feasibility module</name>
  <files>src/domain/stream-copy-feasibility.ts (new)</files>
  <read_first>
    - .planning/phases/03-video-preservation-fallback-control/03-RESEARCH.md (whitelist + codes)
    - src/domain/output-plan.ts (PlannedAudioCodec | PlannedContainer usage)
    - src/domain/media-probe.ts (`MediaProbe` shape)
  </read_first>
  <action>
    Create `src/domain/stream-copy-feasibility.ts` exporting `evaluateStreamCopyFeasibility` per CONTEXT hint. Normalize codec names via `trim().toLowerCase()`.

    Explicit outcomes:
    1. **Fallback** + codes `[ "video-fallback-multi-video-streams" ]` when `videoStreams.length !== 1`.
    2. **Fallback** + `[ "video-fallback-missing-format-metadata" ]` when `plannedContainer === "mp4"` and video branch applies but **`format.format_name` missing/blank** after coercion.
    3. **Fallback** + `[ "video-fallback-non-h264-video" ]` when lone codec ≠ `h264`.
    4. **Fallback** defensive branch if `plannedContainer !== "mp4"` while Phase 3 path still engages video modality—codes `[ "video-fallback-non-mp4-output-not-supported-for-video-v1" ]` (explicit literal string tokens for tests).
    5. **Success** emitting reason token **`video-copy-h264-mp4-v1`** when whitelist satisfied.

    **Do not log**—pure function returns data only.</action>
  <verify>
    <automated>bun test test/domain/output-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/domain/stream-copy-feasibility.ts`
    - `rg -n "evaluateStreamCopyFeasibility" src/domain/stream-copy-feasibility.ts`
    - `rg -n "video-fallback-multi-video-streams|video-fallback-missing-format-metadata|video-fallback-non-h264-video|video-copy-h264-mp4-v1" src/domain/stream-copy-feasibility.ts`
    - No imports from adapters or Bun-only APIs besides types (should be adapter-free).
    - `rg -n "stream-copy-feasibility" src/domain/output-plan.ts` wires importer path.
  </acceptance_criteria>
  <done>Centralized deterministic feasibility logic separated from orchestration boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite planMediaOutput video branch</name>
  <files>src/domain/output-plan.ts, test/domain/output-plan.test.ts</files>
  <read_first>
    - src/domain/output-plan.ts
    - src/domain/stream-copy-feasibility.ts
    - test/domain/output-plan.test.ts (current expectations referencing stub)
    - .planning/phases/03-video-preservation-fallback-control/03-CONTEXT.md (D-01 … D-04)
  </read_first>
  <action>
    After selecting audio streams, when **`videoStreams.length > 0`**, call evaluator with probe + literals **`plannedAudioCodec: "aac"`**, **`plannedContainer: "mp4"`** identical to Phase 2 shipping defaults—do not introduce new branching on codec/container combos yet beyond evaluator guard.

    - If evaluator returns fallback → `planMediaOutput` returns supported plan branch with **`modality: "fallback-required"`**, **`reasonCodes` exactly equal to evaluator list** ordering stable (document order = insertion order arrays).
    - If success → modality **`video-copy-safe`** carrying single reason **`video-copy-h264-mp4-v1`**.
    - Remove references to **`phase-2-stub-video-copy-safe`** except comments explaining historical stub if retained once.

    Update tests:
    - happy path minimal fixture (post format_name augmentation) ⇒ `video-copy-safe` + new reason.
    - add explicit objects for **dual video**, **vp9 lone video**, **`format.format_name` absent**.
    Each must assert modality + containment of canonical reason substring via `rg` list in acceptance.
  </action>
  <verify>
    <automated>bun test test/domain/output-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase-2-stub-video-copy-safe" src/domain/output-plan.ts test/domain/output-plan.test.ts` yields **zero** occurrences (preferred) OR only appears in changelog comment prefixed `Historical:`—preferred zero in tests asserting behavior.
    - `rg -n "video-copy-h264-mp4-v1|video-fallback" test/domain/output-plan.test.ts`
    - `bun test test/domain/output-plan.test.ts` exits 0 after updates.
    - Existing audio-only test still expects `phase-2-stub-audio-only`.
  </acceptance_criteria>
  <done>Domain truth aligns with CONTEXT conservative defaults before CLI policy layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ffprobe JSON → Zod-validated probe | Parsing stays at boundary—feasibility runs on typed `MediaProbe` only. |
| Planning vs execution | Planner never invokes FFmpeg binaries; attackers cannot escalate via malformed argv here—only misuse summary fields downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation Plan |
|-----------|----------|-----------|-----------|----------------|
| T-03-01-01 | Spoofing | Fabricated probes in tests/fixtures | low | Fixtures live in-repo; executor later must still re-probe inputs at execution boundary with fresh probes. |
| T-03-01-02 | Tampering | Overly permissive whitelist | medium | Narrow `h264` + mandatory `format_name` string + single stream rule; exhaustive expansion requires new roadmap work. |
| T-03-01-03 | Information disclosure | Reasons leak internal policy | low | Reason codes intentional for automation; wording kept technical—inspect layer adds humane copy Plan 03-02. |
</threat_model>

<verification>
`bun test test/domain/output-plan.test.ts`
</verification>

<success_criteria>
VIDEO-01 and domain-layer VIDEO-02 satisfied: honest modalities, conservative fallbacks, no silent copy-safe regressions versus Phase 2 stub semantics.
</success_criteria>

<output>
Create `.planning/phases/03-video-preservation-fallback-control/03-01-SUMMARY.md` referencing decisions D-01..D-06 after execution completes.
</output>
