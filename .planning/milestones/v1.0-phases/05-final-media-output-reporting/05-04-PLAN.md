---
phase: 05-final-media-output-reporting
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - src/domain/cli-request.ts
  - src/cli/command.ts
  - src/app/run-command.ts
  - src/cli/render.ts
  - test/cli/command.test.ts
autonomous: true
requirements:
  - MEDIA-02
  - VIDEO-04
  - TRUST-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 05-2026-05-02T00-37-51
generated_at: "2026-05-02T02:00:00.000Z"
must_haves:
  truths:
    - "**`clean`** exposes **`--allow-video-fallback`** boolean default **`false`** mirroring **`inspect`** (**`command.ts`** Commander definition)."
    - "Successful non-JSON **`clean`** stdout prints **`renderCleanRunReportText`** output after existing summary OR integrated checklist—must contain **`Verified:`** substring on mocked integration test (**VIDEO-04**, **TRUST-02**)."
    - **`src/cli/render.ts`** guidance/help strings remove stale **Phase 5 will add** promise for **`video-copy-safe`** **`clean`** — replace with factual **extract+remux** wording; **`rg -n \"Phase 5 will add\" src/cli/render.ts`** returns **no hits**."
  artifacts:
    - path: src/cli/command.ts
      provides: clean flags parity with inspect fallback policy
---

<objective>
Wire **`--allow-video-fallback`** into **`CliRequest`/`clean`** routing, render **final report text** on success, refresh **help/guidance** copy (**VIDEO-04**, **TRUST-02** surface), and extend **`command.test.ts`** parsing assertions.

Purpose: Makes Phase 5 behavior discoverable and user-visible.

Output: CLI + renderer + tests listed in **`files_modified`**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/cli/command.ts
@src/domain/cli-request.ts
@src/app/run-command.ts
@src/cli/render.ts
@test/cli/command.test.ts
@src/app/clean.ts (`CleanRunInput`)

<interfaces_hint>
**`CliRequest` `clean` variant** adds **`allowVideoFallback: boolean`**.

**Commander:**

```text
.option("--allow-video-fallback", "Allow executing plans that require video/container fallback", false)
```

Use Commander **boolean flag** pattern consistent with **`inspect`** (**freeze long flag spelling **`allow-video-fallback`**).

**`runCliRequest`**: pass field into **`runCleanRequest`**.

**Renderer:**

- When **`clean`** success and **`!json`**, print **`reportText`** from outcome if present **before** or **after** step list—pick deterministic ordering documented in **`05-04-SUMMARY.md`**.
- JSON mode: include **`report`** object mirroring **`CleanRunReport`** fields OR embed **`reportText`** key **`clean.reportText`** — choose one; **`command.test.ts`** freezes minimal JSON keys.

**Tests:**

- Parse argv **`clean --allow-video-fallback`** yields **`allowVideoFallback: true`**.
- Omit flag ⇒ **`false`**.

</interfaces_hint>
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Fallback flag | accidental destructive encode | Help text warns inspect first |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-04-01 | I | misleading help | mitigate | Align literals with **`inspect`** semantics |

</threat_model>

<verification_criteria>
- `bun test test/cli/command.test.ts` exits **0**.
- `rg --null -n "allow-video-fallback" src/cli/command.ts`
- `! rg -n "Phase 5 will add" src/cli/render.ts`
</verification_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extend CliRequest + Commander + run-command wiring</name>
  <files>src/domain/cli-request.ts, src/cli/command.ts, src/app/run-command.ts</files>
  <read_first>
    - src/cli/command.ts
    - src/domain/cli-request.ts
    - src/app/run-command.ts
    - src/cli/render.ts
  </read_first>
  <action>
    Add **`allowVideoFallback`** to **`clean`** **`CliRequest`** branch + Commander option + **`runCliRequest`** mapping into **`CleanRunInput`**.
  </action>
  <verify>
    <automated>bun test test/cli/command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`rg -n "allowVideoFallback" src/domain/cli-request.ts`**
    - **`rg -n "allow-video-fallback" src/cli/command.ts`**
    - **`bun test test/cli/command.test.ts`** exits **0**
  </acceptance_criteria>
  <done>CLI parses new flag.</done>
</task>

<task type="auto">
  <name>Task 2: Render report + refresh guidance strings</name>
  <files>src/cli/render.ts</files>
  <read_first>
    - src/cli/render.ts
    - src/app/clean.ts
    - test/cli/command.test.ts
  </read_first>
  <action>
    Implement renderer branch surfacing **`Verified:`** checklist text.

    Remove obsolete **`Phase 5 will add`** substring from **`render.ts`** guidance/help (**`rg` gate**).

    Extend **`command.test.ts`** (or integration harness) asserting **`Verified:`** appears when mocking **`runCleanRequest`** success payload containing **`reportText`** — if **`runCli`** integration too heavy, unit **`render`** test acceptable addition under **`test/cli/`** with explicit note in **`05-04-SUMMARY.md`**.
  </action>
  <verify>
    <automated>bun test test/cli/command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`! rg -n "Phase 5 will add" src/cli/render.ts`** (exit **1** expected from **`rg`** = success)
    - **`rg -n "Verified:" src/cli/render.ts`**
    - **`bun test test/cli/command.test.ts`** exits **0**
  </acceptance_criteria>
  <done>UX reflects Phase 5 capabilities honestly.</done>
</task>

</tasks>

<success_criteria>
- Users can approve fallback on **`clean`** same as **`inspect`**.
- Final report visible without **`--json`**.

</success_criteria>

<output>
After completion, create `.planning/phases/05-final-media-output-reporting/05-04-SUMMARY.md`
</output>
