---
phase: 05-final-media-output-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/clean-run-report.ts
  - src/domain/clean-output-verify.ts
  - test/domain/clean-output-verify.test.ts
autonomous: true
requirements:
  - VIDEO-04
  - TRUST-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 05-2026-05-02T00-37-51
generated_at: "2026-05-02T02:00:00.000Z"
must_haves:
  truths:
    - "Exported constants **`DURATION_VERIFY_RELATIVE_FRACTION = 0.005`** and **`DURATION_VERIFY_MIN_ABS_SECONDS = 0.5`** define tolerance **`max(duration * REL, MIN_ABS)`** matching **05-CONTEXT D-08**."
    - "**`verifyCleanOutput`** returns discriminated **`ok`** vs **`failure`** with frozen machine-readable **`reason`** tags (e.g. **`missing-file`**, **`empty-output`**, **`probe-parse`**, **`duration-mismatch`**, **`video-copy-mismatch`**)—never throws for invalid probes passed as typed inputs."
    - "**`CleanRunReport`** (name exact per implementation) captures **`videoPolicy`** (**`copied` | `re-encoded` | `n/a-audio-only`**), **`audioCodecSummary`**, **`droppedStreamsLabels`** string array, and **`verificationOk`** boolean—fields populated by orchestration in later waves."
  artifacts:
    - path: src/domain/clean-output-verify.ts
      provides: pure post-run verification decision
    - path: src/domain/clean-run-report.ts
      provides: typed final report model + text renderer hook point
---

<objective>
Add **pure-domain** types for **VIDEO-04** final reporting and **TRUST-03** post-run verification (duration epsilon + optional video-copy codec match), with **unit tests** using **hand-crafted `MediaProbe`-shaped inputs**—no FFmpeg subprocess in this plan.

Purpose: Locks verification math and report shape before argv builders and orchestration land.

Output: `src/domain/clean-run-report.ts`, `src/domain/clean-output-verify.ts`, `test/domain/clean-output-verify.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-final-media-output-reporting/05-CONTEXT.md (`D-08`, `D-09`, `D-04`)
@src/domain/media-probe.ts
@src/domain/output-plan.ts (`OutputModality`)

<interfaces_hint>
**Tolerance helper** (export name freeze **`durationVerificationToleranceSeconds`**):

```typescript
export const DURATION_VERIFY_RELATIVE_FRACTION = 0.005;
export const DURATION_VERIFY_MIN_ABS_SECONDS = 0.5;

export function durationVerificationToleranceSeconds(inputDurationSec: number): number {
  return Math.max(
    inputDurationSec * DURATION_VERIFY_RELATIVE_FRACTION,
    DURATION_VERIFY_MIN_ABS_SECONDS,
  );
}
```

**Verification API** (signature illustrative—keep names grep-stable):

```typescript
export type CleanVerifyParams = {
  readonly outputPath: string;
  readonly outputExists: (p: string) => boolean;
  readonly outputFileSize: (p: string) => number;
  readonly inputProbe: MediaProbe;
  readonly outputProbe: MediaProbe;
  readonly plannedModality: "audio-only" | "video-copy-safe" | "fallback-required";
  readonly claimedVideoCopied: boolean;
};

export type CleanVerifyResult =
  | { readonly kind: "ok" }
  | { readonly kind: "failure"; readonly reason: CleanVerifyFailureReason; readonly detail: string };
```

When **`plannedModality === "video-copy-safe"`** AND **`claimedVideoCopied === true`**: locate **first stream** where **`codec_type === "video"`** in **both** probes; require **`codec_name`** equal string match (and document if missing stream ⇒ **`failure`** **`video-copy-mismatch`**).

**Duration:** parse **`format.duration`** from both probes using same parsing helper used elsewhere if exists; else **`Number.parseFloat`** with finite guard.

</interfaces_hint>
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Paths | irrelevant here — pure logic receives probes | No filesystem reads beyond injected **`outputExists`**/**`outputFileSize`** |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-01-01 | T | bogus probe JSON | mitigate | Types assumed already validated by **`runFfprobeProbe`** adapter |

</threat_model>

<verification_criteria>
- `bun test test/domain/clean-output-verify.test.ts` exits **0**.
- `rg -n "durationVerificationToleranceSeconds" src/domain/clean-output-verify.ts`
- `rg -n "DURATION_VERIFY_RELATIVE_FRACTION" src/domain/clean-output-verify.ts`
</verification_criteria>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for verify + tolerance</name>
  <files>test/domain/clean-output-verify.test.ts</files>
  <read_first>
    - src/domain/media-probe.ts
    - src/domain/output-plan.ts
  </read_first>
  <behavior>
    - **`durationVerificationToleranceSeconds(100)`** expectation **`0.5`** (min abs wins).
    - **`durationVerificationToleranceSeconds(200)`** expectation **`1.0`** (200 × 0.005).
    - Matching durations within tolerance ⇒ **`ok`**.
    - Delta beyond tolerance ⇒ **`failure`** **`duration-mismatch`** substring in **`detail`**.
    - Video copy: matching **`codec_name`** **`h264`** both sides ⇒ **`ok`**; output **`hevc`** vs input **`h264`** ⇒ **`video-copy-mismatch`**.
    - Zero-byte file ⇒ **`failure`** reason tag includes **`empty`** or **`empty-output`** (freeze one substring for **`rg`** below).
  </behavior>
  <action>
    Create **`test/domain/clean-output-verify.test.ts`** importing verification helpers from **`src/domain/clean-output-verify.ts`** (implementation follows in Task 2).

    Build minimal **`MediaProbe`** objects inline (only **`format.duration`** + **`streams[]`** fields required by verify function — satisfy **`MediaProbe`** type via **`as const` partial** only if needed; prefer full minimal valid structs mirroring **`inspect`** fixtures).

    Inject **`outputExists`** returning **true**, **`outputFileSize`** returning **100** for non-empty tests.
  </action>
  <verify>
    <automated>bun test test/domain/clean-output-verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`rg -n "durationVerificationToleranceSeconds" test/domain/clean-output-verify.test.ts`**
    - **`rg -n "video-copy-mismatch" test/domain/clean-output-verify.test.ts`**
    - **`bun test test/domain/clean-output-verify.test.ts`** exits **0**
  </acceptance_criteria>
  <done>Tests fail until implementation exists; pass after Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Implement clean-run-report + clean-output-verify modules</name>
  <files>src/domain/clean-run-report.ts, src/domain/clean-output-verify.ts</files>
  <read_first>
    - test/domain/clean-output-verify.test.ts
    - src/domain/media-probe.ts
    - .planning/phases/05-final-media-output-reporting/05-CONTEXT.md
  </read_first>
  <action>
    Implement **`durationVerificationToleranceSeconds`**, **`CleanVerifyParams`**, **`CleanVerifyResult`**, and **`verifyCleanOutput`** in **`src/domain/clean-output-verify.ts`** per interfaces_hint.

    Implement **`CleanRunReport`** interface + **`renderCleanRunReportText(report: CleanRunReport): string`** in **`src/domain/clean-run-report.ts`** producing lines containing literal substrings **`Video:`**, **`Audio:`**, **`Verified:`** at least once each when those sections apply (grep-stable smoke).

    Export failure reason union type as **`CleanVerifyFailureReason`** string union including **`"missing-file"`**, **`"empty-output"`**, **`"duration-mismatch"`**, **`"video-copy-mismatch"`**, **`"missing-video-stream"`** as needed.
  </action>
  <verify>
    <automated>bun test test/domain/clean-output-verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`rg -n "renderCleanRunReportText" src/domain/clean-run-report.ts`**
    - **`rg -n "verifyCleanOutput" src/domain/clean-output-verify.ts`**
    - **`bun test test/domain/clean-output-verify.test.ts`** exits **0**
  </acceptance_criteria>
  <done>Pure domain modules green.</done>
</task>

</tasks>

<success_criteria>
- Verification logic is fully unit-testable without FFmpeg.
- Report renderer exists for orchestration to populate in wave 3–4.

</success_criteria>

<output>
After completion, create `.planning/phases/05-final-media-output-reporting/05-01-SUMMARY.md`
</output>
