---
phase: 04-core-audio-pipeline-sox-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/domain/audio-pipeline-argv.ts
  - test/domain/audio-pipeline-argv.test.ts
autonomous: true
requirements:
  - PIPE-04
  - PIPE-05
  - TOOL-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 04-2026-05-02T00-13-28
generated_at: "2026-05-02T01:00:00.000Z"
must_haves:
  truths:
    - "Each logical step maps to exactly one **`createProcessCommand`** succeed path producing argv arrays (no shell strings) for FFmpeg or discovered SoX binary (PIPE-04, TRUST-01, D-13)."
    - "Extract step emits WAV PCM **`pcm_s16le`** with explicit **`-ar`** / **`-ac`** copied from probe-derived integers (PIPE-05)."
    - "SoX argv uses **`sox`** or **`sox_ng`** absolute executable passed from caller—builders remain pure aside from **`createProcessCommand`** (TOOL-02, D-10)."
  artifacts:
    - path: src/domain/audio-pipeline-argv.ts
      provides: buildExtractPcmWavCommand / buildAfftdnWavCommand / buildEncodeDeliverableCommand / buildSoxGentleDynamicsCommand
    - path: test/domain/audio-pipeline-argv.test.ts
      provides: argv snapshots for trust assertions
---

<objective>
Add **pure argv builders** from **`LogicalPipelineStep`** instances to **`ProcessCommand`** values (via **`createProcessCommand`**), preserving PCM WAV interchange and argv-only semantics.

Purpose: Enables deterministic subprocess planning tested without spawning media tools.

Output: `src/domain/audio-pipeline-argv.ts` + argv snapshot tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/domain/process-command.ts
@src/domain/audio-pipeline-plan.ts
@src/domain/media-probe.ts
@.planning/phases/04-core-audio-pipeline-sox-cleanup/04-CONTEXT.md

<interfaces_hint>
Import **`createProcessCommand`, `type ProcessCommandResult`** from **`process-command.ts`**.

Suggested exported builder **`buildLogicalStepCommand(params)`** switching on **`step.tool`** / **`step.step.kind`** returning **`ProcessCommandResult`**.

**Extract PCM (`ffmpeg`):** Args MUST include **`-nostdin`**, **`-hide_banner`**, **`-loglevel`**, **`error`** (or **`warning`** — pick one literal and stay consistent), **`-y`**, **`-i`**, `<inputPath>`, **`-map`**, `` `0:${streamIndex}` ``, **`-vn`**, **`-acodec`**, **`pcm_s16le`**, **`-ar`**, `${sampleRate}`, **`-ac`**, `${channelCount}`, **`-f`**, **`wav`**, `<outputWavPath>`.

**AFFTDN (`ffmpeg`):** Input/output WAV paths; include **`-af`**, **`afftdn=`** filter string computing **`nf`** from **`noiseStrength`** using deterministic formula documented inline (example: linear map **`nf = -15 - noiseStrength * 50`** bounded to **`[-80,-10]`** floats — freeze literals in tests).

**Encode deliverable (`ffmpeg`):** Must honor **`PlannedAudioCodec` / `PlannedContainer`**:

- **`aac` + `mp4`:** **`-f mp4`** (or `-movflags +faststart` optional — if added, freeze in tests), **`-c:a aac`**, explicit **`-b:a 192k`** (literal) unless research contradicts—freeze here for VIDEO-05 alignment stub.

**SoX gentle dynamics:** Effects chain literal frozen in tests, minimum tokens **`compand`** + **`gain`** sequence — example shape:

`highpass`, **`80`**, then **`compand`** with numeric thresholds — choose one chain and snapshot full argv tail after **`sox in.wav out.wav`**.

Executable path parameter **`soxExecutable`** passed from orchestrator (**`sox_ng` preferred path else `sox`**).

Return **`invalid`** **`ProcessCommandResult`** if **`createProcessCommand`** fails only—never throw.
</interfaces_hint>
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Disposition |
|----------|------|-------------|
| File paths embedded in argv | symlink surprises / traversal | mitigate + document |
| Filter numeric mapping | unstable audio output | accept product risk |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-02-01 | T | argv builders | mitigate | Never concatenate shell meta-characters; only structured args array; **`renderDisplayCommand`** used for summaries only |
| T-04-02-02 | D | hostile huge WAV | accept/limit | Phase 4 documents no artificial byte-cap; orchestrator may pass **`timeoutMs`** later — not required here |

Optional FFmpeg stderr handling deferred to orchestration layer (**no secret leakage** — builders emit no secrets).
</threat_model>

<verification_criteria>
- `bun test test/domain/audio-pipeline-argv.test.ts` exits 0.
- `rg -n 'pcm_s16le' src/domain/audio-pipeline-argv.ts`
- `rg -n 'afftdn' src/domain/audio-pipeline-argv.ts test/domain/audio-pipeline-argv.test.ts`
</verification_criteria>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests fixing FFmpeg + SoX argv snapshots</name>
  <files>test/domain/audio-pipeline-argv.test.ts</files>
  <read_first>
    - src/domain/process-command.ts
    - src/domain/audio-pipeline-plan.ts
    - test/domain/process-command.test.ts
    - test/domain/audio-pipeline-plan.test.ts
  </read_first>
  <behavior>
    - Extract builder produces args containing **`-map`**, **`0:2`**, **`-acodec`**, **`pcm_s16le`**, **`-ar`**, **`48000`**, **`-ac`**, **`2`** in order for sample fixture integers.
    - AFFTDN builder at **`noiseStrength === 0`** produces deterministic **`nf=`** substring matching implementation formula snapshot.
    - Encode **`aac`/`mp4`** includes **`-c:a`**, **`aac`**, and **`-b:a`**, **`192k`** literals.
    - SoX builder prefixes **`executable`** **`/tmp/mock-sox`** and includes **`highpass`** + **`compand`** tokens from frozen chain.
  </behavior>
  <action>
    Create **`test/domain/audio-pipeline-argv.test.ts`** importing builders from **`src/domain/audio-pipeline-argv.ts`** (initial RED acceptable until Task 2).

    Assert **`created`** branch extracts **`.command.args`** arrays exactly—avoid snapshot drift by comparing **`includes`** sequences tied to acceptance literals above.

    Cover **`invalid`** path by forcing **`executable: ""`** if exposed via helper—or skip if unreachable—prefer testing **`createProcessCommand`** rejection only if helper exposes wrapper.
  </action>
  <verify>
    <automated>bun test test/domain/audio-pipeline-argv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests reference **`pcm_s16le`** literal assertion at least once.
    - Tests reference **`192k`** literal assertion at least once.
    - **`rg -n "0:2" test/domain/audio-pipeline-argv.test.ts`** matches stream map scenario.
  </acceptance_criteria>
  <done>Argv expectations encoded before/with implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement argv builders</name>
  <files>src/domain/audio-pipeline-argv.ts</files>
  <read_first>
    - src/domain/audio-pipeline-argv.ts
    - test/domain/audio-pipeline-argv.test.ts
    - src/domain/process-command.ts
    - src/domain/audio-pipeline-plan.ts
    - src/domain/output-plan.ts
  </read_first>
  <action>
    Implement builders per CONTEXT:

    - Export **`AudioArgvContext`** struct bundling **`streamIndex`**, **`sampleRate`**, **`channelCount`**, **`plannedAudioCodec`**, **`plannedContainer`**, filesystem paths (**`inputMediaPath`**, **`intermediateInPath`**, **`intermediateOutPath`**, **`finalOutputPath`** as applicable).

    - **`buildLogicalStepCommand({ step, ctx, ffmpegExecutable, maybeSoxExecutable })`** returns **`ProcessCommandResult`**:

      - **`gentle-dynamics`** requires **`maybeSoxExecutable`** non-null string; otherwise return **`invalid`** with **`empty-executable`** OR introduce **`missing-sox`** guard returning **`invalid`** reason **`empty-executable`** only if simplest—prefer orchestrator preflight; builder should still **`trim()`** SoX path.

    Map **`noiseStrength`** identically to Plan 01 tests.

    Document in top-of-file comment: **PCM WAV s16le** interchange obligation (**PIPE-05**).

    No **`node:fs`** imports.
  </action>
  <verify>
    <automated>bun test test/domain/audio-pipeline-argv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test test/domain/audio-pipeline-argv.test.ts` exits **0**.
    - `rg -n "buildLogicalStepCommand" src/domain/audio-pipeline-argv.ts`
    - `rg -n "192k" src/domain/audio-pipeline-argv.ts`
  </acceptance_criteria>
  <done>All builders compile; tests green.</done>
</task>

</tasks>

<success_criteria>
- Deterministic FFmpeg + SoX argv arrays produced for each **`LogicalPipelineStep`** kind used by **`speech-light`** and **`speech-soft-sox`** expansions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-audio-pipeline-sox-cleanup/04-02-SUMMARY.md`
</output>
