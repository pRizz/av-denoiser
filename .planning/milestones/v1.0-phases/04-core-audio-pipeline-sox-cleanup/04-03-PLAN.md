---
phase: 04-core-audio-pipeline-sox-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/app/clean.ts
  - test/app/clean.test.ts
autonomous: true
requirements:
  - MEDIA-01
  - PIPE-02
  - PIPE-04
  - TOOL-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 04-2026-05-02T00-13-28
generated_at: "2026-05-02T01:00:00.000Z"
must_haves:
  truths:
    - "**`runCleanRequest`** repeats inspect-grade preflight (**ffprobe**, **`resolveOutputPath`**, **`planMediaOutput`**) before executing (**MEDIA-01**)."
    - "**Modality gate:** continues only when **`plan.modality === "audio-only"`**; otherwise returns **`failure`** with reason **`invalid-input`** OR **`fallback-required`** matching modality nuance described in tasks (**D-02**)."
    - "**Sequential runner:** executes expanded logical steps one-after-another via **`runProcessCommand`**, stopping on first non-zero exit → **`processing-failure`** including truncated stderr (**PIPE-04**, D-07)."
    - "**SoX readiness:** presets requiring SoX fail **`missing-tools`** listing **`[\"sox\", \"sox_ng\"]`** or repo’s canonical missing-tool naming when neither binary resolves (**TOOL-02**, D-11)."
    - "**Dry-run:** returns **`success`** without spawning FFmpeg when **`dryRun: true`**, embedding **`renderDisplayCommand`** strings per step (**PIPE-02**, D-05)."
  artifacts:
    - path: src/app/clean.ts
      provides: runCleanRequest orchestration + modality gate + dry-run summaries
    - path: test/app/clean.test.ts
      provides: mocked process runner coverage + negative modality tests
---

<objective>
Implement **`runCleanRequest`** orchestration: probe/plan parity with **`inspect`**, **`clean`** modality gate (**audio-only**), optional dry-run summaries, sequential **`ProcessRunner`** execution with tempfile chaining, and actionable **`missing-tools`** when SoX required but absent.

Purpose: Delivers MEDIA-01 execution core callable from CLI wiring next wave.

Output: `src/app/clean.ts` + `test/app/clean.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/inspect.ts
@src/domain/output-plan.ts
@src/domain/output-path.ts
@src/domain/command-outcome.ts
@src/adapters/ffprobe.ts
@src/adapters/process-runner.ts
@src/domain/audio-pipeline-plan.ts
@src/domain/audio-pipeline-argv.ts
@src/domain/process-command.ts (`renderDisplayCommand`)

<interfaces_hint>
Mirror **`InspectDeps`** pattern:

```typescript
export type CleanDeps = {
  readonly cwd: string;
  readonly maybeWhich: (name: string) => string | null;
  readonly runProcess: ProcessRunner;
  readonly outputExists: (absolutePath: string) => boolean;
  readonly mkdtempSync?: (prefix: string) => string; // inject for tests
  readonly rmSync?: (path: string, options?: { recursive?: boolean }) => void;
};
```

**Modality messaging (D-02 exact literals required in tests):**

- **`unsupported`** → **`invalid-input`** message contains **`audio-only`** and **`inspect`** substrings.
- **`video-copy-safe`** → **`invalid-input`** message contains **`Phase 5`** substring (exact capitalization).
- **`fallback-required`** → **`fallback-required`** reason kind mirroring **`inspect`** semantics WITHOUT needing **`--allow-video-fallback`** (clean never executes video preservation).

**Temp artifacts:** Under injected **`mkdtempSync`** directory **`${tmpdir}/av-denoiser-clean-*`** naming convention **`step-${idx}.wav`** intermediates + final write to **`plan.resolvedOutputPath`** via encode step output path handling.

**Success stdout payloads:** Typed **`CleanCliSuccess`** similar to **`InspectCliSuccess`** holding **`dryRun`**, **`json`**, **`summary`** objects listing **`warnings`**, **`steps: readonly { tool: string; displayCommand: string }[]`**.

**stderr hygiene:** On **`processing-failure`**, message MUST prefix **`ffmpeg`**/**`sox`** stage tag and include **≤ 500** chars of stderr (constant **`MAX_CLEAN_STDERR_SNIPPET = 500`**) — prevents accidental massive logs (**security note**).

**Required ffmpeg/ffprobe discovery:** identical failure messaging style to **`inspect`** when missing.

**Collision-safe paths:** reuse **`resolveOutputPath`** inputs exactly like **`inspect`** (**D-03**).
</interfaces_hint>
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| User-supplied paths | symlink / traversal surprises | Document local-trust assumption identical to **`inspect`**; optional future **`realpath`** not required Phase 4 |
| Process invocation | command injection | argv-only **`runProcessCommand`** exclusively |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-03-01 | E | FFmpeg/SoX stderr | mitigate | Truncate stderr snippets; never surface env dumps |
| T-04-03-02 | D | huge media runtime | accept | Document CLI trust—no artificial duration/bitrate caps Phase 4 |
| T-04-03-03 | T | partial tempfiles | mitigate | **`rmSync`** cleanup on success path always; on failure optionally retain for debug — bias leave temps on failure per D-07 (**implement leave-on-failure**) |

Optional tools stderr must not include secrets — builders carry none.
</threat_model>

<verification_criteria>
- `bun test test/app/clean.test.ts` exits 0.
- `rg -n 'runCleanRequest' src/app/clean.ts`
- `rg -n 'audio-only' test/app/clean.test.ts`
- `rg -n 'missing-tools' test/app/clean.test.ts`
</verification_criteria>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests for gate, dry-run, sequential spawn order</name>
  <files>test/app/clean.test.ts</files>
  <read_first>
    - src/app/inspect.ts
    - test/app/inspect.test.ts
    - src/domain/command-outcome.ts
    - src/domain/output-plan.ts
    - src/domain/audio-pipeline-plan.ts
  </read_first>
  <behavior>
    - Mock **`runProcess`** fails test if invoked when **`dryRun: true`** on **`speech-light`** happy path (audio-only fixture plan built via **`planMediaOutput`** + handcrafted **`MediaProbe`** or reuse helpers from **`inspect.test`** patterns).
    - Non-audio modality (`video-copy-safe`) expects **`failure`** **`invalid-input`** containing **`Phase 5`**.
    - **`speech-soft-sox`** with **`maybeWhich`** returning **`null`** for both **`sox_ng`** and **`sox`** yields **`missing-tools`** whose **`tools`** array includes **`"sox"`** (and **`"sox_ng"`** if orchestrator enumerates both — freeze exact array ordering lexicographically ascending).
    - Sequential execution: simulate **`runProcess`** counting invocations; **`speech-light`** dry-run=false expects **`runProcess`** called **`3`** times for FFmpeg-only pipeline.
  </behavior>
  <action>
    Add **`test/app/clean.test.ts`** importing **`runCleanRequest`** once **`src/app/clean.ts`** exists.

    Use fake **`maybeWhich`** map:

    ```typescript
    const fakeWhich = (name: string) =>
      ({ ffmpeg: "/bin/ffmpeg", ffprobe: "/bin/ffprobe", sox_ng: null, sox: null } as const)[name] ?? null;
    ```

    Adjust when asserting SoX-present scenarios supplying **`"/mock/sox"`**.

    Provide **`parseFfprobeJson`** fixture consistent with **`audio-only`** modality via **`planMediaOutput`** — reuse minimal fixtures JSON loaders from **`inspect`** tests where possible.

    Stub **`mkdtempSync`** returning deterministic **`/tmp/av-test-clean`** + noop **`rmSync`** capturing paths when helpful.
  </action>
  <verify>
    <automated>bun test test/app/clean.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests mention **`dry-run`** substring inside test name or assertion comment (`rg -n 'dry' test/app/clean.test.ts`).
    - **`rg -n 'Phase 5' test/app/clean.test.ts`**
    - **`rg -n 'speech-soft-sox' test/app/clean.test.ts`** OR **`rg -n 'sox' test/app/clean.test.ts`**
  </acceptance_criteria>
  <done>Automated acceptance encoded prior/during implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement clean orchestration module</name>
  <files>src/app/clean.ts</files>
  <read_first>
    - src/app/clean.ts
    - test/app/clean.test.ts
    - src/app/inspect.ts
    - src/domain/audio-pipeline-argv.ts
    - src/domain/audio-pipeline-plan.ts
    - src/adapters/ffprobe.ts
  </read_first>
  <action>
    Implement **`runCleanRequest(request: CleanRunInput, deps)`** where **`CleanRunInput`** is exported alongside **`runCleanRequest`** from **`src/app/clean.ts`** (Plan 04 maps **`CliRequest`** → **`CleanRunInput`** once the CLI variant exists).

    Required **`CleanRunInput`** fields (**freeze names**):

    - **`inputPath: string`**
    - **`maybeOutputPath?: string`**
    - **`force: boolean`**
    - **`dryRun: boolean`**
    - **`json: boolean`**
    - **`presetId: PresetId`** (typed import from **`audio-pipeline-plan`**)
    - **`knobs: CleanPresetKnobs`**

    Steps:

    1. **`maybeWhich`** probe **`ffprobe`**/`ffmpeg`** identical ordering failure listing absent ones (`missing-tools`).
    2. **`runFfprobeProbe`** identical call signature as **`inspect`**.
    3. **`resolveOutputPath`** parity (**include `force`** flag).
    4. **`planMediaOutput`** — gate modality per tasks above.
    5. **`expandPreset`** using **`plannedAudioCodec`/`plannedContainer`** off **`OutputPlan`** branch (**narrow types** via discriminated narrowing — TS compile must succeed).

    **SoX discovery:** `const soxPath = maybeWhich("sox_ng") ?? maybeWhich("sox");` When **`presetRequiresSox`** && **`soxPath === null`** ⇒ **`missing-tools`** **`tools: ["sox", "sox_ng"]`** sorted ascending.

    **Dry-run:** Build commands via **`buildLogicalStepCommand`** for each logical step using synthetic intermediate basename placeholders OR actual planned temp filenames — **`runProcess`** MUST NOT execute.

    **Execute:** Allocate temp dir, loop steps updating **`previousPath → nextPath`**, pass **`ffmpeg`**/`sox`** absolute paths.

    Encode step writes **`plan.resolvedOutputPath`** directly.

    Map **`ProcessResult`** failures consistent with **`inspect`** mapping patterns (**spawn-failed**, **`signaled`**, **`non-zero-exit`** summarizing trimmed stderr).

    Export **`CleanCliSuccess`** + **`CleanCliOutcome`** typed aliases parallel **`Inspect`** definitions extending **`CommandOutcome`**.

    Update **`describeFfprobeFailure`** by reusing **`inspect`** helper via shared internal export OR duplicate minimally — avoid circular imports.

    Emit **`pipelineWarnings`** into summary alongside **`inspect`-style modality echo**.
  </action>
  <verify>
    <automated>bun test test/app/clean.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test test/app/clean.test.ts` exits **0**.
    - `rg -n "MAX_CLEAN_STDERR_SNIPPET" src/app/clean.ts`
    - `rg -n "presetRequiresSox" src/app/clean.ts`
  </acceptance_criteria>
  <done>Sequential audio cleaning orchestration completes under mocks + modality gates enforced.</done>
</task>

</tasks>

<success_criteria>
- **`runCleanRequest`** callable with injected deps passes tests without touching real FFmpeg when mocked.
- **`speech-light`** pipeline executes FFmpeg logical triple under mock counting semantics.

</success_criteria>

<output>
After completion, create `.planning/phases/04-core-audio-pipeline-sox-cleanup/04-03-SUMMARY.md`
</output>
