---
phase: 06-guided-repeatable-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/domain/cli-request.ts
  - src/cli/command.ts
  - src/app/run-command.ts
  - src/app/guided-clean.ts
  - src/cli/render.ts
  - test/app/guided-clean.test.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - CLI-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 06-2026-05-02T00-47-46
generated_at: "2026-05-02T00:47:46.439Z"
must_haves:
  truths:
    - "**`av-denoiser guided`** registers (**exact subcommand id `guided`**) with description mentioning **guided clean workflow**."
    - "**TTY guard**: non-interactive invocation returns **`failure`** with message substring **`TTY`** or **`interactive`** (grep-stable test)."
    - "Wizard collects **input path**, **output** (optional), **preset**, **noise strength**, **`allow-video-fallback`**, **`force`** → builds **`GuidedCleanSelections`**."
    - "**Dry-run preview** calls **`runCleanRequest`** with **`dryRun: true`** and **`json: false`** using selections — success path renders via **`renderCommandOutcome`** / existing **`CleanCliSuccess`** text path (**same** summary user sees for **`clean --dry-run`**)."
    - "Shows **`Equivalent command:`** line followed by **`argvTokensForEquivalentClean(selections).join(\" \")`** (or safer pretty join)."
    - "**Confirm execute**: cancel is safe; confirm runs **`runCleanRequest`** **`dryRun: false`** (**real clean**). Failures surface via existing **`CommandOutcome`**."
  artifacts:
    - path: src/app/guided-clean.ts
      provides: `@clack` prompts + orchestration
---

<objective>
Ship **interactive guided `clean`** (**UX-01**, **UX-02**): prompts, **`clean --dry-run` equivalent preview** (**UX-03**), **copy/paste argv line** (**UX-04**, **CLI-04**), and **confirmed real execution** using existing **`runCleanRequest`**.

**06-03** adds **progress/summary polish** only — do not leave execute unimplemented here.


</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/domain/guided-clean-selection.ts
@src/domain/guided-clean-equivalent.ts
@src/app/clean.ts (`runCleanRequest`, `CleanDeps`)
@src/app/run-command.ts
@src/cli/render.ts (`renderCleanPlanText`, `renderCommandOutcome`)
@.planning/phases/06-guided-repeatable-workflows/06-RESEARCH.md
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| User paths | traversal / odd names | Use path as opaque string for argv; **`existsSync`** checks only via existing clean path rules |
| Prompt injection | N/A — local CLI | No network |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-02-01 | D | stdin spoof in CI | mitigate | **TTY guard** + injectable **`isInteractive`** dep in tests |

</threat_model>

<verification_criteria>
- `bun test test/app/guided-clean.test.ts` exits **0**.
- `rg -n "command\\(\"guided\"\\)" src/cli/command.ts` OR `rg -n '\"guided\"' src/cli/command.ts` finds registration.
- `rg -n "kind: \"guided-clean\"" src/domain/cli-request.ts`
- `bun run typecheck` exits **0**.
</verification_criteria>

<tasks>

<task type="auto">
  <name>CliRequest + dispatcher</name>
  <files>src/domain/cli-request.ts src/app/run-command.ts</files>
  <read_first>
    - src/domain/cli-request.ts
    - src/app/run-command.ts
  </read_first>
  <action>
    Add union member **`{ readonly kind: \"guided-clean\" }`**.

    Extend **`CliRequestDeps`** with **`readonly guided?: Partial<GuidedCleanDeps>`**.

    In **`runCliRequest`**, **`case \"guided-clean\"`**: **`return runGuidedCleanRequest(deps.guided)`** — **`runGuidedCleanRequest(deps?: Partial<GuidedCleanDeps>)`** lives in **`guided-clean.ts`**.
  </action>
  <acceptance_criteria>
    - **`rg "guided-clean" src/domain/cli-request.ts`**
    - **`rg "guided-clean" src/app/run-command.ts`**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>guided command registration</name>
  <files>src/cli/command.ts</files>
  <read_first>
    - src/cli/command.ts
  </read_first>
  <action>
    Register **`.command(\"guided\")`** with short description (**<= 120 chars**) and **`.action(() => handleRequest({ kind: \"guided-clean\" }))`**.
  </action>
  <acceptance_criteria>
    - **`rg "\\.command\\(\"guided\"\\)" src/cli/command.ts`**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>runGuidedCleanRequest implementation</name>
  <files>src/app/guided-clean.ts</files>
  <read_first>
    - src/app/guided-clean.ts (create)
    - src/domain/guided-clean-selection.ts
    - src/domain/guided-clean-equivalent.ts
    - src/app/clean.ts
    - package.json (@clack/prompts)
  </read_first>
  <action>
    Implement **`runGuidedCleanRequest`**:

    1. **`deps.isTTY ?? ((await import(\"node:tty\")).stdin?.isTTY === true)`** — if false return **`failure`** **`planning-failure`** message containing **`TTY`**.
    2. Use **`@clack/prompts`**: **`text`** for paths, **`select`** for preset ids (**`speech-light`**, **`speech-soft-sox`**), **`confirm`** for booleans (**force**, **allow-video-fallback**), **`text`** or **`slide`** for noise strength (**validate 0–1** — reuse **`parseNoiseStrength`** logic from **`command.ts`** by extracting shared **`domain/guided-clean-parse.ts`** OR duplicate minimal validation with tests).
    3. Build **`GuidedCleanSelections`** (**`dryRun: true`** for preview phase).
    4. Call **`runCleanRequest`** mapping selections → **`CleanRunInput`** (**`json: false`**).
    5. After preview **`CleanCliSuccess`**, append **`renderCleanPlanText`** output into accumulator string **`guidedHumanSummary`**. Append blank line + **`Equivalent command:`** + **`argvTokensForEquivalentClean(selectionsForReplay)`** joined with spaces (**`selectionsForReplay`** uses **`dryRun: false`** even though preview used **`dryRun: true`**).
    6. **`confirm`** “Run clean now?” — if no return **`success`** with human message “cancelled” (still **exit 0**) OR **`success`** with zero side effects — **pick one** and document in **`render`**; if yes run **`runCleanRequest`** **`dryRun: false`**.

    Handle **`isCancel`** from clack → treat as user cancel.

    Export **`GuidedCleanDeps`** with injectable **`prompts`** stubs for tests.
  </action>
  <acceptance_criteria>
    - **`rg "runGuidedCleanRequest" src/app/guided-clean.ts`**
    - **`rg "@clack/prompts" src/app/guided-clean.ts`**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>CliCommandOutcome + render</name>
  <files>src/app/run-command.ts src/cli/render.ts</files>
  <read_first>
    - src/app/run-command.ts (`CliCommandOutcome`)
    - src/cli/render.ts (`renderCommandOutcome`, `renderCliRequest`)
    - src/cli/render.ts (`renderCleanPlanText`)
  </read_first>
  <action>
    1. Extend **`CliCommandOutcome`** with **`readonly guidedHumanSummary?: string`** — **`runGuidedCleanRequest`** sets this on **`success`** (**must include dry-run **`renderCleanPlanText`** block + **`Equivalent command:`** line**); after confirmed execute append final **`CleanCliSuccess`** human output (**`renderCleanPlanText`** + **`maybeReportText`** when present).

    2. In **`renderCommandOutcome`**, before generic fallbacks: **`if (request.kind === \"guided-clean\")`** — **`failure`** uses **`appendFailureDetails`** with prefix **`av-denoiser guided failed.`**; **`success`** returns **`guidedHumanSummary ?? \"av-denoiser guided finished.\"`**.

    3. Extend **`renderCliRequest`** **`switch`** with **`case \"guided-clean\"`** stub (**>= 3 lines**) pointing users at **`av-denoiser guided`**.

    **Forbidden:** **`console.log`** in **`guided-clean.ts`** for user-visible transcript (**stderr/stdout routing stays in **`render.ts`** / **`runCli`**).
  </action>
  <acceptance_criteria>
    - **`rg \"guidedHumanSummary\" src/app/run-command.ts`**
    - **`rg \"guided-clean\" src/cli/render.ts`** matches **>= 2** lines.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>guided-clean tests with injected prompts</name>
  <files>test/app/guided-clean.test.ts</files>
  <read_first>
    - src/app/guided-clean.ts
    - src/app/clean.ts
    - test/app/clean.test.ts (patterns)
  </read_first>
  <action>
    Tests:

    1. **Non-TTY** → outcome **`failure`** message contains **`TTY`**.
    2. **Happy path**: fake prompts return canned **`GuidedCleanSelections`** — assert **`runCleanRequest`** called twice (**dry-run** then **execute**) OR once if implementation merges — **prefer twice** — with **`dryRun`** **`true`** then **`false`**.
    3. Assert equivalent argv helper invoked / snapshot **`Equivalent command:`** rendering via **`renderCommandOutcome`** if exercised.

    Mock **`runCleanRequest`** via **`GuidedCleanDeps`**.
  </action>
  <acceptance_criteria>
    - **`bun test test/app/guided-clean.test.ts`** exits **0**
  </acceptance_criteria>
</task>

</tasks>
