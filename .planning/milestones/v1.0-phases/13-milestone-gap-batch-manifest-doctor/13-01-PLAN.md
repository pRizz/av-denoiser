---
phase: 13-milestone-gap-batch-manifest-doctor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/run-command.ts
  - src/app/batch.ts
  - test/app/run-command.test.ts
autonomous: true
requirements:
  - BATCH-05
  - BATCH-01
  - BATCH-02
  - BATCH-03
  - BATCH-04
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 13-2026-05-02T12-13-43
generated_at: "2026-05-02T18:00:00.000Z"
must_haves:
  truths:
    - "**`runCliRequest`** **`case \"batch\"`** passes **`discoverTools: deps.discoverTools ?? createDoctorReport`** into **`runBatchRequest`** so the production CLI matches **`doctor`** default discovery (**13-CONTEXT D-01**)."
    - "**Direct **`runBatchRequest`** callers** that omit **`discoverTools`** still write manifests with **`maybeDoctorFacts: null`** (**13-CONTEXT D-01**); **`test/app/batch.test.ts`** stays green without edits."
    - "If **`discoverTools`** **throws**, **`runBatchRequest`** returns a **`failure`** **`CommandOutcome`** before **`Bun.write`** (no manifest file produced on discovery failure — **13-CONTEXT D-03**)."
    - "**Hermetic tests** prove **`maybeDoctorFacts`** receives the stub **`DoctorReport`** and that discovery rejection surfaces as **`failure`** (**13-CONTEXT D-04**)."
  artifacts:
    - path: src/app/run-command.ts
      provides: default discovery wiring for batch CLI path
    - path: src/app/batch.ts
      provides: discovery try/catch → structured failure without manifest write
    - path: test/app/run-command.test.ts
      provides: BATCH-05 regression + discovery failure regression
---

<objective>
Close **BATCH-05** for the default **`batch`** CLI path by wiring **`discoverTools`** like **`doctor`**, persisting **`DoctorReport`** into **`manifest.maybeDoctorFacts`**, failing cleanly when discovery throws (**13-CONTEXT D-03**), and adding **`runCliRequest`**-level tests without weakening **`runBatchRequest`** hermetic defaults (**13-CONTEXT D-01**, **D-04**).

Output: **`run-command.ts`** batch branch update, **`batch.ts`** discovery error handling, new **`test/app/run-command.test.ts`**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-milestone-gap-batch-manifest-doctor/13-CONTEXT.md
@src/app/run-command.ts
@src/app/batch.ts
@src/app/doctor.ts
@test/app/batch.test.ts
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| **`DoctorReport`** in manifest | Embeds **local paths** / environment facts | Same as Phase **07** manifest — **local audit artifact** only |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-13-01-01 | D | Discovery probes subprocess | mitigate | Reuse **`createDoctorReport`** — already reviewed tool-discovery argv discipline (**TRUST-01**) |

</threat_model>

<verification_criteria>
- **`rg 'case \"batch\"' src/app/run-command.ts`** shows **`discoverTools`** / **`createDoctorReport`** wiring
- **`rg \"maybeDoctorFacts\" src/app/batch.ts`**
- **`bun test test/app/run-command.test.ts`** exits **0**
- **`bun test test/app/batch.test.ts`** exits **0**
- **`bun run verify`** exits **0**
</verification_criteria>

<tasks>

<task type="auto">
  <name>Wire batch default discoverTools</name>
  <files>src/app/run-command.ts</files>
  <read_first>
    - src/app/run-command.ts (full file — **`doctor`** branch pattern)
    - src/app/batch.ts (**`BatchOrchestratorDeps`**)
    - src/app/doctor.ts (**`createDoctorReport`** export)
  </read_first>
  <action>
    Replace **`case \"batch\": return runBatchRequest(request, deps);`** with a call that passes an orchestrator deps object containing:

    - **`discoverTools: deps.discoverTools ?? createDoctorReport`**
    - **`clean: deps.clean`**
    - **`batch: deps.batch`**

    Ensure **`createDoctorReport`** is **already imported** from **`./doctor`** (it is used by **`doctor`** case — reuse).

    Do **not** change **`doctor`**, **`clean`**, or **`guided-clean`** branches.
  </action>
  <acceptance_criteria>
    - **`rg \"discoverTools: deps.discoverTools ?? createDoctorReport\" src/app/run-command.ts`** exits **0**
    - **`rg 'case \"batch\":' src/app/run-command.ts`** exits **0**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Discovery failure handling in runBatchRequest</name>
  <files>src/app/batch.ts</files>
  <read_first>
    - src/app/batch.ts (discovery block ~176–180; **`outcomeFromMaybeThrow`**)
    - src/domain/command-outcome.ts (**`CommandOutcome`** failure shapes used elsewhere)
  </read_first>
  <action>
    Wrap **`await deps.discoverTools()`** (inside the **`deps.discoverTools !== undefined`** branch only) with **`try/catch`**:

    - On throw: **`return outcomeFromMaybeThrow(error)`** (same helper used after **`expandBatchInputs`**).

    Ensure **`doctorFacts`** assignment happens only on success.

    **Ordering constraint:** Discovery stays **before** manifest **`Bun.write`** (already true); failure path must **not** call **`Bun.write`** (early **`return`** before **`const document`** assembly is acceptable **only if** no partial manifest is written — verify control flow: **`return`** occurs before **`Bun.write`**).
  </action>
  <acceptance_criteria>
    - **`rg \"deps.discoverTools\" src/app/batch.ts`** shows **`try`** wrapping **`await`** OR equivalent guarded pattern
    - **`rg \"outcomeFromMaybeThrow\" src/app/batch.ts`** exits **0**
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>runCliRequest batch doctor snapshot tests</name>
  <files>test/app/run-command.test.ts</files>
  <read_first>
    - test/app/batch.test.ts (temp dir + **`CliRequest`** **`batch`** shape)
    - src/app/run-command.ts (**`runCliRequest`** signature)
    - src/domain/cli-request.ts (**`CliRequest`** batch fields if needed)
    - src/domain/doctor-report.ts (**`DoctorReport`** minimal stub shape)
  </read_first>
  <action>
    Create **`test/app/run-command.test.ts`** with **`bun:test`**.

    **Test A — stub snapshot:** Temp dir; one **`*.wav`** input; **`CliRequest`** **`kind: \"batch\"`** mirroring **`batch.test.ts`** defaults (**`dryRun: true`**, **`force: true`**, **`inputPaths`**, **`maybeManifestPath`**, **`presetId`**, **`knobs`**, **`concurrency: 1`**, **`failFast: false`**, empty **`globs`**, **`acceptGlobRisk: false`**, **`maybeOutputDir`** omitted unless needed).

    Call **`runCliRequest(request, { discoverTools: async () => stubReport, batch: { runClean }, clean: { cwd: dir, outputExists: () => false } })`** where **`stubReport`** is **`{ tools: [] }`** **`const`** (**valid **`DoctorReport`**).

    Mock **`runClean`** returns **`success`** with minimal **`CleanCliOutcome`** like **`batch.test.ts`**.

    Assert **`outcome.kind === \"success\"`**, **`outcome.batch?.document.maybeDoctorFacts`** **`===`** **`stubReport`** (reference equality optional — **`toEqual`** acceptable).

    **Test B — discovery throw:** Same harness; **`discoverTools: async () => { throw new Error(\"discovery boom\"); }`**.

    Assert **`outcome.kind === \"failure\"`**, **`reason.kind === \"invalid-input\"`**, and **`reason.message`** contains **`discovery boom`** (per **`outcomeFromMaybeThrow`**).

    Cleanup **`rmSync`** temp dirs.

  </action>
  <acceptance_criteria>
    - **`test/app/run-command.test.ts`** exists with **`≥ 2`** **`test(`** blocks (**A** + **B**)
    - **`bun test test/app/run-command.test.ts`** exits **0**
  </acceptance_criteria>
</task>

</tasks>

---

## PLANNING COMPLETE

Plans in phase directory: **1** (**13-01-PLAN.md**).
