---
phase: 01-bun-cli-foundation-trust-model
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/process-runner.ts
  - src/adapters/tool-discovery.ts
  - test/adapters/tool-discovery.test.ts
autonomous: true
requirements:
  - TRUST-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 01-2026-05-01T23-00-58
generated_at: "2026-05-01T23:45:00.000Z"
must_haves:
  truths:
    - "`runProcessCommand` always calls `Bun.spawn` with `[executable, ...args]` (array spawn only)."
    - "Tool discovery never concatenates user/tool paths into shell strings; version probes use injected `ProcessRunner`."
  artifacts:
    - path: src/adapters/process-runner.ts
      provides: "Argv-only Bun.spawn wrapper"
      contains: "Bun.spawn(["
    - path: src/adapters/tool-discovery.ts
      provides: "Injectable which + probe orchestration"
      contains: "discoverTools"
---

<objective>
Verify and strengthen the imperative-shell adapters that execute external processes and discover tools so `TRUST-01` remains true: argv-only execution, injectable doubles, bounded IO capture.

Purpose: Every future FFmpeg/FFprobe invocation inherits this gate (D-04).
Output: Adapter-level tests proving spawn shape; only minimal code changes if gaps surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/adapters/process-runner.ts
@src/adapters/tool-discovery.ts
@test/adapters/tool-discovery.test.ts
@src/domain/process-command.ts
@.planning/phases/01-bun-cli-foundation-trust-model/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Process runner integration smoke (argv-only)</name>
  <files>src/adapters/process-runner.ts, test/adapters/process-runner.test.ts</files>
  <read_first>
    - src/adapters/process-runner.ts
    - src/domain/process-command.ts
    - .planning/phases/01-bun-cli-foundation-trust-model/01-RESEARCH.md
  </read_first>
  <behavior>
    - Primary case: `runProcessCommand({ executable: <current Bun binary>, args: ['--version'], stdin: 'ignore' })` yields `{ kind: 'exited', exitCode: 0 }` and stdout contains `bun` (case-insensitive).
    - Secondary case: duplicate `--version` smoke or a trivial `-h` flag **only** if supported without shell; never invoke `/bin/sh` or pass one concatenated command string.
  </behavior>
  <action>
    Add `test/adapters/process-runner.test.ts` that imports `runProcessCommand` and targets the **same Bun binary running the test suite** via `process.execPath` (or equivalent portable Bun test pattern). Assert successful exit and readable stdout using argv `['--version']`. Keep stdin ignored to match production defaults. Optional: add `timeoutMs` assertion using a tiny timeout only if flakiness requires—default omit. Do not spawn FFmpeg/ffprobe here.
  </action>
  <verify>
    <automated>bun test test/adapters/process-runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `test/adapters/process-runner.test.ts` exists.
    - `rg 'runProcessCommand' test/adapters/process-runner.test.ts src/adapters/process-runner.ts` succeeds.
    - `rg 'Bun\\.spawn\\(\\[' src/adapters/process-runner.ts` confirms array spawn.
    - `rg '/bin/sh|exec\\(|spawn\\(\"' src/adapters/process-runner.ts test/adapters/process-runner.test.ts` exits 1 (no shell string APIs in adapter/test).
  </acceptance_criteria>
  <done>Spawn uses argv arrays in production code and is covered by an automated integration smoke test.</done>
</task>

<task type="auto">
  <name>Task 2: Tool discovery injection + command shaping</name>
  <files>src/adapters/tool-discovery.ts, test/adapters/tool-discovery.test.ts</files>
  <read_first>
    - src/adapters/tool-discovery.ts
    - test/adapters/tool-discovery.test.ts
    - src/adapters/process-runner.ts
  </read_first>
  <action>
    Audit `discoverTools` to ensure version probes build `ProcessCommand` via `createProcessCommand` results (no string shells). Extend tests to cover at least one probe where args include a user-controllable-looking path segment **as a single argv element** (e.g., temporary filename with spaces) if such probe exists; if no path args yet, add regression/assertion that `runProcess` receives commands with `executable` equal to resolved tool path and args remain discrete (`['-version']`, etc.). Validate injectable `maybeWhich`/`runProcess` remain defaults for production (`Bun.which`, `runProcessCommand`).
  </action>
  <verify>
    <automated>bun test test/adapters/tool-discovery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'discoverTools' src/adapters/tool-discovery.ts test/adapters/tool-discovery.test.ts` succeeds.
    - `rg 'createProcessCommand' src/adapters/tool-discovery.ts` succeeds.
    - Tests demonstrate fake `runProcess` passed into deps (no mandatory real tools).
  </acceptance_criteria>
  <done>Tool discovery stays on argv-only ProcessCommand specs end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PATH/tool resolution → spawn | Executable paths flow into `Bun.spawn` argv[0]; additional args are explicit arrays. |
| External tool stdout/stderr → CLI | Bounded reads via stream-to-text; failures surface as structured process results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Elevation of privilege | Command injection | mitigate | High severity: forbid shell/string APIs; enforce array spawn only in `runProcessCommand`; tests guard regression. |
| T-01-03-02 | Tampering | Tool PATH spoofing | accept | Local CLI trust model; doctor surfaces resolved paths for human inspection (ASVS L1). |
| T-01-03-03 | Denial of Service | Runaway subprocess | mitigate | Honor `timeoutMs` when present on `ProcessCommand`; keep defaults sane for version probes only. |
</threat_model>

<verification>
Run `bun test test/adapters/tool-discovery.test.ts test/adapters/process-runner.test.ts`.
</verification>

<success_criteria>
TRUST-01 holds at the adapter layer: external tools are only invoked through argv-array process commands with automated regression coverage.
</success_criteria>
