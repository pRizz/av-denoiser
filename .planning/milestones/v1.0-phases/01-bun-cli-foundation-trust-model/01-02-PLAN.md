---
phase: 01-bun-cli-foundation-trust-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/exit-codes.ts
  - src/domain/command-outcome.ts
  - src/domain/process-command.ts
  - src/domain/doctor-report.ts
  - src/domain/cli-request.ts
  - test/domain/exit-codes.test.ts
  - test/domain/process-command.test.ts
  - test/domain/doctor-report.test.ts
autonomous: true
requirements:
  - CLI-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 01-2026-05-01T23-00-58
generated_at: "2026-05-01T23:45:00.000Z"
must_haves:
  truths:
    - "Exit code integers stay stable and map deterministically from `CommandOutcome` variants."
    - "`ProcessCommand` construction rejects unsafe inputs at the boundary before any spawn."
    - "`DoctorReport` remains a tagged union suitable for structured rendering (no silent optional booleans)."
  artifacts:
    - path: src/domain/exit-codes.ts
      provides: "Stable ExitCode constants"
      contains: "missingTools"
    - path: src/domain/command-outcome.ts
      provides: "Outcome → exit mapper"
      contains: "mapOutcomeToExitCode"
    - path: src/domain/process-command.ts
      provides: "Argv-only command factory"
      contains: "createProcessCommand"
---

<objective>
Lock and regression-test the functional-core trust primitives: exit taxonomy, outcome mapping, argv-only command specs, doctor structured facts, and typed CLI requests—supporting `CLI-03` without re-implementing media logic.

Purpose: Downstream phases depend on numeric exit stability and tagged unions (D-05, D-07, D-09, D-12).
Output: Green tests proving taxonomy + pure transitions; only minimal domain tweaks if tests expose drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-bun-cli-foundation-trust-model/01-CONTEXT.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-RESEARCH.md
@src/domain/exit-codes.ts
@src/domain/command-outcome.ts
@src/domain/process-command.ts
@src/domain/doctor-report.ts
@src/domain/cli-request.ts
@test/domain/exit-codes.test.ts
@test/domain/process-command.test.ts
@test/domain/doctor-report.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exit codes + outcome mapping regression suite</name>
  <files>src/domain/exit-codes.ts, src/domain/command-outcome.ts, test/domain/exit-codes.test.ts</files>
  <read_first>
    - src/domain/exit-codes.ts
    - src/domain/command-outcome.ts
    - test/domain/exit-codes.test.ts
    - .planning/phases/01-bun-cli-foundation-trust-model/01-RESEARCH.md
  </read_first>
  <behavior>
    - Tests pin numeric values `{ success:0, internalError:1, invalidInput:2, missingTools:3, planningFailure:4, processingFailure:5, fallbackRequired:6 }`.
    - Tests cover `mapOutcomeToExitCode` for each failure kind plus internal-error path.
  </behavior>
  <action>
    Extend or tighten `test/domain/exit-codes.test.ts` so every `ExitCode` constant and each `CommandFailureReason` maps to the documented integer per D-05. If implementation drift exists versus RESEARCH table, prefer fixing code to match locked taxonomy (and CONTEXT) rather than changing numbers silently. Do not add Commander imports into domain modules (D-07).
  </action>
  <verify>
    <automated>bun test test/domain/exit-codes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'missingTools: 3' src/domain/exit-codes.ts` succeeds.
    - `rg 'mapOutcomeToExitCode' test/domain/exit-codes.test.ts src/domain/command-outcome.ts` succeeds.
    - `bun test test/domain/exit-codes.test.ts` exits 0.
  </acceptance_criteria>
  <done>CLI-03 numeric taxonomy is test-locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProcessCommand + doctor-report pure behaviors</name>
  <files>src/domain/process-command.ts, src/domain/doctor-report.ts, src/domain/cli-request.ts, test/domain/process-command.test.ts, test/domain/doctor-report.test.ts</files>
  <read_first>
    - src/domain/process-command.ts
    - src/domain/doctor-report.ts
    - src/domain/cli-request.ts
    - test/domain/process-command.test.ts
    - test/domain/doctor-report.test.ts
  </read_first>
  <behavior>
    - `createProcessCommand` tests continue to prove paths-with-spaces remain single argv elements and `renderDisplayCommand` never mutates args.
    - Doctor aggregation tests prove required-missing vs optional-missing severity distinctions align with `DoctorReport` helpers (per existing tests—expand only if coverage gaps exist).
  </behavior>
  <action>
    Review `test/domain/process-command.test.ts` for argv-only guarantees (supports CLI-03/TRUST-01 boundary typing). Review `test/domain/doctor-report.test.ts` for `not-checked-yet` vs `missing` distinctions per D-06. Add minimal missing cases only—avoid expanding doctor scope beyond structured facts. Ensure `CliRequest` union covers `show-help`, `show-default`, `doctor`, and downstream commands without `any`.
  </action>
  <verify>
    <automated>bun test test/domain/process-command.test.ts test/domain/doctor-report.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'createProcessCommand' test/domain/process-command.test.ts src/domain/process-command.ts` succeeds.
    - `rg 'not-checked-yet|missing' test/domain/doctor-report.test.ts` succeeds.
    - `rg 'kind: \"doctor\"|kind: \"inspect\"' src/domain/cli-request.ts` confirms typed requests remain discriminated.
  </acceptance_criteria>
  <done>Pure domain contracts remain enforced by tests; high-risk edge paths are explicitly asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted future CLI flags → domain | Inputs must arrive as parsed `CliRequest` / validated command specs—not raw concatenated strings inside domain modules. |
| Display helpers → execution | `renderDisplayCommand` is diagnostic-only; execution adapters must use argv arrays from `ProcessCommand`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | `createProcessCommand` | mitigate | Reject empty/whitespace executables; preserve args array immutability in tests (prevents accidental shell string reliance). |
| T-01-02-02 | Elevation of privilege | Weak validation | mitigate | Keep validation purely typed (`created` vs `invalid` results); never fallback to string shell execution in domain layer. |
| T-01-02-03 | Repudiation | Doctor unions | mitigate | Maintain explicit `not-checked-yet` facts rather than collapsing to boolean availability (D-06). |
</threat_model>

<verification>
Run `bun test test/domain/exit-codes.test.ts test/domain/process-command.test.ts test/domain/doctor-report.test.ts`.
</verification>

<success_criteria>
CLI-03 taxonomy and pure trust primitives are regression-tested; domain modules remain free of Bun.spawn/Commander imports.
</success_criteria>
