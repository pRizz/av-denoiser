---
phase: 08-optional-heavy-editor-integrations
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03-PLAN.md
files_modified:
  - src/adapters/tool-discovery.ts
  - src/app/doctor.ts
  - src/domain/doctor-report.ts
  - src/domain/audio-pipeline-plan.ts
  - src/domain/audio-pipeline-argv.ts
  - src/app/clean.ts
  - src/cli/command.ts
  - docs/doctor.md
  - test/adapters/tool-discovery.test.ts
  - test/domain/audio-pipeline-argv.test.ts
autonomous: true
requirements:
  - TOOL-07
  - TOOL-08
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-05-02T03-39-48
generated_at: "2026-05-02T03:45:00.000Z"
must_haves:
  truths:
    - "**Doctor** runs **Demucs** discovery per **D-16**: when `demucs` or `python3 -m demucs` is resolvable, record **version/help** (or **`check-failed`**) as **non-blocking** optional tool facts."
    - "**Doctor** reports **`ladspa`** in **`ffmpeg -filters`** when probe succeeds (**TOOL-07** readiness signal)."
    - "User can **execute** a **Kdenlive-derived** cleanup path via an **opt-in FFmpeg `ladspa` filter** step (bounded plugin label + controls string) when **`ladspa`** capability is **available** — new preset or **`--ladspa-plugin`** / **`--ladspa-label`** flags with **plan-time validation** (**TOOL-07** runnable path, not doctor-only)."
    - "**`melt`** shows **version** or **missing** / **check-failed** without blocking required tools; **`docs/doctor.md`** states **FFmpeg-first** fallback when **`melt`** absent (**TOOL-08**)."
  artifacts:
    - path: src/app/clean.ts
      provides: optional FFmpeg ladspa step execution
    - path: src/app/doctor.ts
      provides: Demucs + ladspa + melt probes
    - path: docs/doctor.md
      provides: TOOL-08 operator messaging
---

<objective>
Close **TOOL-07** with a **runnable** optional **FFmpeg `ladspa`** pipeline segment (Kdenlive-derived / headless-style audio effect path), extend **doctor** for **Demucs version/help (D-16)**, **`ladspa`** filter discovery, and **`melt`**, and document **TOOL-08** fallbacks. Lands **after** **08-03** so **`clean.ts`** merges serialize.

Output: domain/argv extensions for **`ladspa`**, doctor probes, docs, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-optional-heavy-editor-integrations/08-CONTEXT.md (D-11, D-12, D-16)
@src/app/doctor.ts
@src/adapters/tool-discovery.ts
@src/domain/audio-pipeline-argv.ts
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| LADSPA plugin labels | User-controlled argv fragment | **Bounded validation**: alphanumeric + known-safe punctuation only for `--ladspa-label` / control string; reject on parse failure |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-04-01 | I | filter list spoofing | best-effort | Doctor output labeled capability hint |
| T-08-04-02 | T | ladspa control injection | mitigate | Zod or regex validation on CLI inputs |

</threat_model>

<verification_criteria>
- `bun test test/adapters/tool-discovery.test.ts` exits **0**
- `bun test test/domain/audio-pipeline-argv.test.ts` exits **0**
- `rg -n "ladspa|LADSPA_PATH|melt|demucs" docs/doctor.md`
- `bun run typecheck` exits **0**
</verification_criteria>

<tasks>

<task type="auto">
  <name>Demucs + melt doctor facts (D-16)</name>
  <files>src/app/doctor.ts src/adapters/tool-discovery.ts src/domain/doctor-report.ts test/adapters/tool-discovery.test.ts</files>
  <read_first>
    - .planning/phases/08-optional-heavy-editor-integrations/08-CONTEXT.md
    - src/app/doctor.ts
    - src/adapters/tool-discovery.ts
  </read_first>
  <action>
    - **Demucs:** When **`demucs`** binary exists, run **`demucs --help`** or **`-h`** (pick one supported) via injected **`runProcess`**; store first ~500 chars or version line in **`ToolAvailability`** (same pattern as ffmpeg version). When absent, try **`python3 -m demucs --help`**. Failures → **`check-failed`** optional, never **`blocked`**.
    - **`melt`:** **`melt -version`** first line when on PATH.
    - Extend tests with **fixtures** for stdout (skip live Demucs in CI).
  </action>
  <acceptance_criteria>
    - **`rg "demucs" src/app/doctor.ts src/adapters/tool-discovery.ts`**
    - **`bun test test/adapters/tool-discovery.test.ts`** exits **0**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>FFmpeg ladspa filter probe</name>
  <files>src/app/doctor.ts src/domain/doctor-report.ts test/adapters/tool-discovery.test.ts</files>
  <read_first>
    - src/app/doctor.ts
    - src/domain/doctor-report.ts
  </read_first>
  <action>
    - When **ffmpeg** is available, run **`-filters`** (or existing list command); flag **`ladspa`** as capability row **available/missing**.
    - Tests: inject stdout with **`ladspa`** token → available.
  </action>
  <acceptance_criteria>
    - **`rg "ladspa" src/app/doctor.ts`**
    - **`bun test test/adapters/tool-discovery.test.ts`** exits **0**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Runnable FFmpeg ladspa logical step</name>
  <files>src/domain/audio-pipeline-plan.ts src/domain/audio-pipeline-argv.ts src/app/clean.ts src/cli/command.ts test/domain/audio-pipeline-argv.test.ts</files>
  <read_first>
    - src/domain/audio-pipeline-plan.ts
    - src/domain/audio-pipeline-argv.ts
    - src/app/clean.ts
    - FFmpeg ladspa filter docs (project STACK / upstream doc link in code comment)
  </read_first>
  <action>
    - Extend **`FfmpegLogicalStep`** with a **`ladspa-apply`** (or similarly named) variant carrying **validated** **`pluginLabel`** + **`controls`** string (may be empty).
    - Add **opt-in preset** or **CLI flags** (`--ladspa-plugin`, `--ladspa-label`, `--ladspa-controls`) that **require** **`ladspa`** doctor capability at **plan time**; otherwise **`planning-failure`** with **TOOL-08**-style message pointing to **`doctor`** and **`LADSPA_PATH`**.
    - **`buildLogicalStepCommand`**: emit **`-af ladspa=file=...:label=...:c=...`** (exact argv per FFmpeg docs — use **`plugin .so` path** from env **`LADSPA_PATH`** resolution **only** via explicit user **`--ladspa-plugin-path`** or documented single plugin file to avoid silent loads).
    - **`runSequentialPipeline`**: no special case if step stays **`tool: ffmpeg`** — prefer **ffmpeg-only** arm.
    - Tests: argv snapshot for ladspa step matches golden array.
  </action>
  <acceptance_criteria>
    - **`rg "ladspa" src/domain/audio-pipeline-plan.ts src/domain/audio-pipeline-argv.ts`**
    - **`bun test test/domain/audio-pipeline-argv.test.ts`** exits **0**
    - **`bun run typecheck`** exits **0**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>docs TOOL-08</name>
  <files>docs/doctor.md</files>
  <read_first>
    - docs/doctor.md
  </read_first>
  <action>
    - Document **Kdenlive/MLT**: **`melt`** optional; **FFmpeg `ladspa`** is the supported headless-style path; **`LADSPA_PATH`**; when features missing, **core FFmpeg/SoX/Demucs** presets still run per **TOOL-08**.
  </action>
  <acceptance_criteria>
    - **`docs/doctor.md` contains `LADSPA_PATH`**
    - **`docs/doctor.md` contains `ladspa`**
    - **`docs/doctor.md` contains `melt`**
  </acceptance_criteria>
</task>

</tasks>
