---
phase: 08-optional-heavy-editor-integrations
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
files_modified:
  - src/app/clean.ts
  - src/cli/command.ts
  - src/domain/cli-request.ts
  - src/app/main.ts
  - test/app/clean.test.ts
autonomous: true
requirements:
  - TOOL-03
  - TOOL-04
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 08-2026-05-02T03-39-48
generated_at: "2026-05-02T03:41:08.360Z"
must_haves:
  truths:
    - "**`runSequentialPipeline`** completes a **`demucs`** step: builds command via **08-01** builder, spawns **`runProcess`**, advances **`inputPathForStep`** to **resolved `vocals.wav`** (or documented stem) on exit **0**."
    - "**`clean --preset`** help lists the **Demucs** preset id from **08-01**."
    - "**`CleanPlanSummary`** / dry-run JSON surfaces **Demucs TOOL-04 warnings** already emitted by **`expandPreset**`."
    - "When **`runBatchRequest`** (or manifest builder) copies **`plannedSummary`**, **pipeline warnings** for Demucs remain visible in manifest items (**08-CONTEXT D-06**) — if batch code snapshots **`CleanPlanSummary`**, extend only if field missing."
  artifacts:
    - path: src/app/clean.ts
      provides: Demucs execution branch + path handoff
    - path: src/cli/command.ts
      provides: preset help + parse for new PresetId
---

<objective>
Wire **Demucs** into **`runCleanRequest`**: execute Demucs steps, resolve vocal output path for downstream FFmpeg encode, expose preset on **`clean`** CLI (**TOOL-03** execution + **TOOL-04** surfacing in summaries). Touch **batch** only if **`plannedSummary`** omits warnings today — minimal delta (**D-06**).

Output: **`clean.ts`**, **`command.ts`**, **`cli-request`** if preset typing ripples, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-optional-heavy-editor-integrations/08-CONTEXT.md
@src/app/clean.ts (`runSequentialPipeline`, `buildStepSummariesFromLogicalSteps`)
@src/app/batch.ts (if exists — manifest snapshot)
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Temp output dirs | Predictable paths in /tmp | Use existing `mkdtemp` prefix; Demucs `-o` under temp root |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-02-01 | I | wrong file picked after Demucs | mitigate | Pure resolver + single success path assertion in test |

</threat_model>

<verification_criteria>
- `bun test test/app/clean.test.ts` exits **0** (extend with mocked `runProcess` sequence including **demucs** step if pattern exists)
- `rg -n "demucs" src/app/clean.ts`
- `bun run typecheck` exits **0**
</verification_criteria>

<tasks>

<task type="auto">
  <name>runSequentialPipeline Demucs branch</name>
  <files>src/app/clean.ts</files>
  <read_first>
    - src/app/clean.ts
    - src/domain/audio-pipeline-argv.ts
    - src/adapters/process-runner.ts
  </read_first>
  <action>
    - In **`runSequentialPipeline`**, handle **`logical.tool === \"demucs\"`**:
      - Resolve **`demucs`** executable via **`maybeWhich(\"demucs\")`** with fallback **`python3`** + **`[\"-m\", \"demucs\"]`** when **`maybeWhich(\"demucs\")`** is null (per **D-01**).
      - If neither path is usable, return **`missing-tools`** or **`planning-failure`** with message listing **demucs** (match existing taxonomy).
      - Build command using **08-01** builder; **`intermediateOutPath`** for Demucs step should be **output directory** or **final vocal wav** per builder contract — **pick one consistent scheme** documented in code.
      - On success, set **`inputPathForStep`** to **`resolveDemucsVocalsWavPath(...)`** (import from domain argv module).
    - Mirror changes in **`buildStepSummariesFromLogicalSteps`** for **`displayCommand`** when tool is **demucs**.
    - Ensure **`mapProcessFailure`** is used with label **`demucs`** (extend signature if needed).
  </action>
  <acceptance_criteria>
    - **`rg "demucs" src/app/clean.ts`** matches at least **3** lines (branch + summary + failure mapping path)
    - **`bun run typecheck`** exits **0**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>CLI preset + batch summary parity</name>
  <files>src/cli/command.ts src/domain/cli-request.ts src/app/main.ts test/app/clean.test.ts</files>
  <read_first>
    - src/cli/command.ts (`--preset` choices)
    - src/domain/cli-request.ts
    - test/app/clean.test.ts
    - src/app/batch.ts
  </read_first>
  <action>
    - Update **`clean`** **`--preset`** description to list **new Demucs preset id** exactly matching **`parsePresetId`**.
    - Trace **`main.ts`** routing — no behavior change beyond types if **`PresetId`** union expanded.
    - If **`runBatchRequest`** builds **`plannedSummary`** from **`CleanPlanSummary`**, verify **pipelineWarnings** are included in manifest **`plannedSummary`** serialization; if not, add field pass-through (**minimal diff**, **D-06**).
    - Add/extend test: **dry-run** with Demucs preset shows **TOOL-04** warning titles in JSON or text snapshot.
  </action>
  <acceptance_criteria>
    - **`rg "speech-vocals-demucs|demucs" src/cli/command.ts`** matches preset help token (**exact id may differ** — test must match implemented id)
    - **`bun test test/app/clean.test.ts`** exits **0**
  </acceptance_criteria>
</task>

</tasks>
