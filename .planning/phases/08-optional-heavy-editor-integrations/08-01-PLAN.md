---
phase: 08-optional-heavy-editor-integrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/audio-pipeline-plan.ts
  - src/domain/audio-pipeline-argv.ts
  - test/domain/audio-pipeline-argv.test.ts
autonomous: true
requirements:
  - TOOL-03
  - TOOL-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-05-02T03-39-48
generated_at: "2026-05-02T03:41:08.360Z"
must_haves:
  truths:
    - "A **new preset id** (e.g. **`speech-vocals-demucs`** — exact name in implementation) exists such that **`expandPreset`** returns at least one **`LogicalPipelineStep`** with **`tool: \"demucs\"`** and **TOOL-04** warnings include **model-download**, **heavy-runtime**, and **resource** messaging per **08-CONTEXT D-05**."
    - "**`buildLogicalStepCommand`** (or a dedicated export consumed by the same call site) returns a **`ProcessCommand`** for **`demucs`** / **`python3 -m demucs`** argv with **`-o`**, input WAV path, **`--two-stems=vocals`** (or Demucs-supported equivalent), and **bounded model id** — **no shell string concatenation** beyond argv array construction."
    - "**`bun test`** for **`test/domain/audio-pipeline-argv.test.ts`** exits **0** with tests that assert argv contains **`--two-stems`** (or equivalent) and **output dir** flag expected by pinned CLI assumptions."
  artifacts:
    - path: src/domain/audio-pipeline-plan.ts
      provides: Demucs logical step + preset expansion + TOOL-04 warnings
    - path: src/domain/audio-pipeline-argv.ts
      provides: argv-only Demucs command builder
---

<objective>
Land **typed Demucs pipeline steps**, **preset expansion**, and **argv-only command building** for an **explicit opt-in** Demucs preset (**TOOL-03**, **TOOL-04** foundations). No `clean.ts` execution changes in this plan — **08-02** wires the runner.

Output: updated **`audio-pipeline-plan.ts`**, **`audio-pipeline-argv.ts`**, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-optional-heavy-editor-integrations/08-CONTEXT.md (D-01–D-06, D-13–D-14)
@.planning/phases/08-optional-heavy-editor-integrations/08-RESEARCH.md
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| User-provided paths | Command injection if passed through shell | **argv[] only** — never `sh -c`; reject newline in paths in builder tests if project already does for FFmpeg |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-01-01 | T | malicious input path embedded in argv | mitigate | Same path validation patterns as existing FFmpeg builders; unit tests |

</threat_model>

<verification_criteria>
- `bun test test/domain/audio-pipeline-argv.test.ts` exits **0**
- `rg -n "demucs|Demucs" src/domain/audio-pipeline-plan.ts src/domain/audio-pipeline-argv.ts`
- `bun run typecheck` exits **0**
</verification_criteria>

<tasks>

<task type="auto">
  <name>Demucs logical step + preset</name>
  <files>src/domain/audio-pipeline-plan.ts</files>
  <read_first>
    - .planning/phases/08-optional-heavy-editor-integrations/08-CONTEXT.md
    - src/domain/audio-pipeline-plan.ts
  </read_first>
  <action>
    Per **D-01–D-04**:
    - Add **`DemucsLogicalStep`** (e.g. `{ kind: \"two-stems-vocals\"; readonly model: string }` — keep **`model`** bounded to an allowlist or `htdemucs` default per discretion).
    - Extend **`LogicalPipelineStep`** with `{ tool: \"demucs\"; readonly step: DemucsLogicalStep }`.
    - Add **`PresetId`** value for the **Demucs** preset and implement **`parsePresetId`** + **`presetRequiresSox`** updates if needed (**Demucs preset may still use FFmpeg + SoX before Demucs per product direction — follow CONTEXT: default shape is vocal isolation step**; minimal v1 can be **extract WAV → demucs → encode** with middle steps as needed).
    - Add **`PipelineWarning`** entries for **TOOL-04**: at minimum ids **`warn-demucs-model-download`**, **`warn-demucs-heavy-runtime`**, **`warn-demucs-resource`** with human titles.
    - **`expandPreset`** for the new preset attaches those warnings in addition to existing FFmpeg warnings where applicable.
  </action>
  <acceptance_criteria>
    - **`rg "tool: \\\"demucs\\\"|\\\"demucs\"" src/domain/audio-pipeline-plan.ts`** matches Demucs branch in union type
    - **`parsePresetId`** accepts the new preset string documented in **`clean --preset` help** (**08-02** updates help — here: export constant or comment for planner traceability)
    - **`expandPreset`** for new preset returns **`steps.some(s => s.tool === \"demucs\")`**
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Demucs argv builder</name>
  <files>src/domain/audio-pipeline-argv.ts test/domain/audio-pipeline-argv.test.ts</files>
  <read_first>
    - src/domain/audio-pipeline-argv.ts
    - src/domain/process-command.ts
    - src/domain/audio-pipeline-plan.ts
  </read_first>
  <action>
    Per **D-01** / **argv-only**:
    - Extend **builder input context** with fields needed for Demucs: **`demucsExecutable`** (`demucs` or `python3`), **`maybeDemucsModuleArgs`** (`[\"-m\", \"demucs\"]` when using Python), **input WAV path**, **absolute output directory**, **`DemucsLogicalStep`**.
    - Return **`createProcessCommand`** with executable + args that invoke **Demucs CLI** with **`-o`** output dir, **`--two-stems=vocals`** (or documented equivalent), **`-n <model>`** if required by CLI.
    - Add **pure helper** **`resolveDemucsVocalsWavPath(outputDirBase: string, trackBasename: string, model: string): string`** documenting expected folder layout (**[ASSUMED] until verified** — lock segments in tests).
    - Add **`test/domain/audio-pipeline-argv.test.ts`** cases: argv includes output flag and stems mode; helper returns path ending with **`vocals.wav`** (adjust to match chosen layout).
  </action>
  <acceptance_criteria>
    - **`bun test test/domain/audio-pipeline-argv.test.ts`** exits **0**
    - **`rg "resolveDemucsVocalsWavPath|demucs" src/domain/audio-pipeline-argv.ts`** exits **0**
  </acceptance_criteria>
</task>

</tasks>
