---
phase: 08-optional-heavy-editor-integrations
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01-PLAN.md
  - 08-02-PLAN.md
files_modified:
  - src/domain/audio-pipeline-plan.ts
  - src/domain/audio-pipeline-argv.ts
  - src/adapters/audacity-pipe.ts
  - src/app/clean.ts
  - src/cli/command.ts
  - src/domain/cli-request.ts
  - docs/doctor.md
  - test/adapters/audacity-pipe.test.ts
autonomous: true
requirements:
  - TOOL-05
  - TOOL-06
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 08-2026-05-02T03-39-48
generated_at: "2026-05-02T03:41:08.360Z"
must_haves:
  truths:
    - "User must pass **explicit opt-in** flags for Audacity automation (**D-07**) — e.g. **`--accept-audacity-pipe-risk`** plus a **macro name** or **script id** flag; without both, planning rejects Audacity steps."
    - "**`AudacityDiagnosticKind`** (or equivalent string union exported from domain) lists at least **`pipe-unavailable`**, **`audacity-not-running`**, **`macro-not-found`**, **`export-failed`**, **`feature-unsupported`** for **TOOL-06**."
    - "**`src/adapters/audacity-pipe.ts`** implements **timeout-bound** pipe round-trip using **argv / file I/O only** (no shell)."
  artifacts:
    - path: src/adapters/audacity-pipe.ts
      provides: mod-script-pipe client
    - path: src/app/clean.ts
      provides: optional Audacity step execution + preflight
---

<objective>
Implement **optional Audacity** automation path: **opt-in CLI**, **preflight diagnostics**, **typed pipeline step** (macro execution), and **actionable errors** (**TOOL-05**, **TOOL-06**). Scope **v1** to **macro batch** commands Audacity documents as scriptable — **do not** claim noise-reduction effect automation if unsupported.

Output: adapter, domain types, **`clean`** integration, tests with **mocked pipe I/O**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-optional-heavy-editor-integrations/08-CONTEXT.md (D-07–D-10)
@.planning/research/FEATURES.md (Audacity scripting limits)
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| mod-script-pipe | Local privilege / arbitrary Audacity commands | **Explicit `--accept-audacity-pipe-risk`** + help text documents risk (**D-07**) |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|----------|------------|
| T-08-03-01 | E | unexpected pipe write | mitigate | Timeout + bounded payload size |

</threat_model>

<verification_criteria>
- `bun test test/adapters/audacity-pipe.test.ts` exits **0**
- `rg -n "accept-audacity|audacity-pipe|Audacity" src/cli/command.ts src/domain/cli-request.ts`
- `bun run typecheck` exits **0**
</verification_criteria>

<tasks>

<task type="auto">
  <name>Audacity domain + adapter skeleton</name>
  <files>src/domain/audio-pipeline-plan.ts src/domain/audio-pipeline-argv.ts src/adapters/audacity-pipe.ts test/adapters/audacity-pipe.test.ts</files>
  <read_first>
    - .planning/phases/08-optional-heavy-editor-integrations/08-CONTEXT.md
    - src/domain/audio-pipeline-plan.ts
  </read_first>
  <action>
    - Add **`AudacityLogicalStep`** (e.g. **`{ kind: \"macro\"; readonly macroName: string }`**) and **`LogicalPipelineStep`** variant **`{ tool: \"audacity\"; ... }`** **only if** `CleanRunInput` / expansion carries **`audacityAccepted`** — otherwise **expandPreset** must **not** insert Audacity steps (**implementation: gate at `expandPreset` or separate `expandPresetWithIntegrations` — choose one pattern and document**).
    - Export **`AudacityDiagnosticKind`** and **`formatAudacityDiagnostic(kind, detail): string`** for doctor + planning.
    - Implement **`audacity-pipe.ts`** with **`probeAudacityPipe(params): Result`** and **`runAudacityMacro(params): AsyncResult`** using **configurable pipe paths** (env or OS defaults documented in **`docs/doctor.md`**).
    - Unit tests **mock** read/write to temp files simulating pipe protocol.
  </action>
  <acceptance_criteria>
    - **`test/adapters/audacity-pipe.test.ts`** exists and **`bun test`** on that file exits **0**
    - **`rg "AudacityDiagnosticKind" src/domain/`** exits **0**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Clean + CLI opt-in wiring</name>
  <files>src/app/clean.ts src/cli/command.ts src/domain/cli-request.ts docs/doctor.md</files>
  <read_first>
    - src/app/clean.ts (`CleanRunInput`)
    - src/cli/command.ts (`clean` options)
    - docs/doctor.md
  </read_first>
  <action>
    - Extend **`CleanRunInput`** with **`maybeAudacityMacro`** + **`acceptAudacityPipeRisk: boolean`** (exact names planner discretion).
    - Commander: **`--audacity-macro <name>`** and **`--accept-audacity-pipe-risk`** (boolean). Validation: macro requires risk flag (**mirror batch glob pattern**).
    - **`runSequentialPipeline`**: branch **`audacity`** tool — call adapter; failures return **`planning-failure`** / **`processing-failure`** with **`formatAudacityDiagnostic`**.
    - **`docs/doctor.md`**: section **Audacity automation** — prerequisites, risk flag, **link to Audacity scripting docs** (full URL).
  </action>
  <acceptance_criteria>
    - **`docs/doctor.md` contains `mod-script-pipe`**
    - **`rg "acceptAudacityPipeRisk|audacity-macro" src/domain/cli-request.ts src/cli/command.ts`**
    - **`bun run typecheck`** exits **0**
  </acceptance_criteria>
</task>

</tasks>
