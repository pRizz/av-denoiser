---
phase: 08-optional-heavy-editor-integrations
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/tool-discovery.ts
  - src/app/doctor.ts
  - src/domain/doctor-report.ts
  - docs/doctor.md
  - test/adapters/tool-discovery.test.ts
autonomous: true
requirements:
  - TOOL-07
  - TOOL-08
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 08-2026-05-02T03-39-48
generated_at: "2026-05-02T03:41:08.360Z"
must_haves:
  truths:
    - "**Doctor** reports whether **FFmpeg** exposes **`ladspa`** filter when probe command succeeds (**TOOL-07** signal)."
    - "**`melt`** optional tool shows **version line** or **`check-failed`** / **missing** with **install hint** — never blocks required tools (**TOOL-08**)."
    - "**`docs/doctor.md`** explains that **Kdenlive/MLT** is **optional**, **FFmpeg-first** cleanup path remains when **`melt`** missing, and points users to **`ladspa`** / **`LADSPA_PATH`** for plugin discovery."
  artifacts:
    - path: src/app/doctor.ts
      provides: extended capability probes
    - path: docs/doctor.md
      provides: TOOL-08 operator messaging
---

<objective>
Extend **doctor** and documentation so **LADSPA / melt** readiness is visible, **optional**, and **actionable** when integrations are unavailable (**TOOL-07**, **TOOL-08**). **No** requirement to implement `.mlt` generation in this plan unless trivial — **FFmpeg `ladspa` discovery** is the primary deliverable per **08-CONTEXT D-11**.

Output: doctor probe hooks, capability rows, **`docs/doctor.md`** updates, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-optional-heavy-editor-integrations/08-CONTEXT.md (D-11, D-12, D-16)
@src/app/doctor.ts
@src/adapters/tool-discovery.ts
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Running `ffmpeg -filters` | Untrusted ffmpeg binary | Same trust as existing doctor — user-installed |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-04-01 | I | spoofed filter list | best-effort | Document as capability hint not security boundary |

</threat_model>

<verification_criteria>
- `bun test test/adapters/tool-discovery.test.ts` exits **0**
- `rg -n "ladspa|melt" docs/doctor.md`
- `bun run typecheck` exits **0**
</verification_criteria>

<tasks>

<task type="auto">
  <name>FFmpeg ladspa capability probe</name>
  <files>src/app/doctor.ts src/domain/doctor-report.ts src/adapters/tool-discovery.ts test/adapters/tool-discovery.test.ts</files>
  <read_first>
    - src/app/doctor.ts
    - src/domain/doctor-report.ts
    - test/adapters/tool-discovery.test.ts
  </read_first>
  <action>
    - When **ffmpeg** is **`available`**, run **`ffmpeg -hide_banner -filters`** (or project’s existing filters probe pattern) via injected **`runProcess`** and parse **`ladspa`** presence; add **`ToolCapabilityStatus`** row **`ladspa-filter`** (**available** / **missing**) with **phase `8`** in **`not-checked-yet`** only if parse skipped.
    - Ensure **optional** failure does not flip doctor to **blocked**.
    - Tests: inject stdout fixture containing **`ladspa`** line → capability **available**; absent line → **missing**.
  </action>
  <acceptance_criteria>
    - **`rg "ladspa" src/app/doctor.ts src/domain/doctor-report.ts`**
    - **`bun test test/adapters/tool-discovery.test.ts`** exits **0**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>melt version + docs TOOL-08</name>
  <files>src/adapters/tool-discovery.ts docs/doctor.md</files>
  <read_first>
    - docs/doctor.md
    - src/adapters/tool-discovery.ts
  </read_first>
  <action>
    - Extend **`melt`** discovery to capture **`melt -version`** first line when binary exists (reuse patterns from ffmpeg version probe).
    - **`docs/doctor.md`**: new **Kdenlive / MLT** subsection — **`melt` optional**, **FFmpeg-first** stance, **`LADSPA_PATH`**, **TOOL-08** wording “pipelines that need melt will log a clear skip; core clean still runs.”
  </action>
  <acceptance_criteria>
    - **`docs/doctor.md` contains `LADSPA_PATH`**
    - **`docs/doctor.md` contains `melt`**
    - **`bun run typecheck`** exits **0**
  </acceptance_criteria>
</task>

</tasks>
