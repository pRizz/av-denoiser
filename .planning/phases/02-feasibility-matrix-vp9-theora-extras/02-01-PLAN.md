---
phase: 02-feasibility-matrix-vp9-theora-extras
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/stream-copy-feasibility.ts
  - test/domain/stream-copy-feasibility.test.ts
requirements_addressed:
  - MULTI-03
  - MULTI-04
  - MULTI-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
autonomous: true
phase_lifecycle_id: 02-2026-05-03T21-30-00Z
generated_at: "2026-05-03T22:05:00.000Z"
must_haves:
  truths:
    - "MP4 H.264/HEVC/AV1 copy-safe behavior and success tokens unchanged (MULTI-12)."
    - "VP9 single-video probes map to plannedContainer webm + video-copy-vp9-webm-v1 when structural gates pass."
    - "Theora maps to plannedContainer matroska + video-copy-theora-matroska-v1 when gates pass."
    - "VP8 maps to fallback-required + video-fallback-vp8-matrix-explicit-v1 with plannedContainer mp4."
---

<objective>
Refactor **`src/domain/stream-copy-feasibility.ts`** so **`MediaProbe`** (single-video + audio already validated by prelude) yields **copy-safe vs fallback-required**, **`plannedContainer`**, and **`reasonCodes`** for **MULTI-03**, **MULTI-04**, **MULTI-05** without weakening **MULTI-12** (**MP4** triple). **`planMediaOutputPrelude`** will consume the new export in **`02-02`** — **this plan stays module-local** (`stream-copy-feasibility` exports a single canonical entry-point used by prelude later).

Defer **`video-copy-vp9-matroska-v1`** (optional row in CONTEXT **D-04**): document backlog comment in **`stream-copy-feasibility`** beside existing taxonomy notes.
</objective>

<threat_model>
**Asset:** locally processed media filenames and probe JSON (unchanged assumption).  
**Threats addressed:** accidental misclassification (optimistic **`video-copy-safe`**) → use explicit tokens and gates; regression on MP4 allowlist tests.  
**Severity:** informational for security ASVS CLI scope; correctness risk is functional, not secrecy.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-feasibility-matrix-vp9-theora-extras/02-CONTEXT.md
@.planning/phases/02-feasibility-matrix-vp9-theora-extras/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/domain/stream-copy-feasibility.ts
@src/domain/output-plan.ts
</context>

<tasks>

<task type="auto">
  <name>T1 — Video matrix types + canonical codec bucket</name>
  <files>src/domain/stream-copy-feasibility.ts</files>
  <read_first>
    - `.planning/phases/02-feasibility-matrix-vp9-theora-extras/02-CONTEXT.md`
    - `src/domain/stream-copy-feasibility.ts`
  </read_first>
  <action>
    1. Extend **`Mp4VideoStreamCopySuccessReasonCode`** OR introduce **`VideoStreamCopySuccessReasonCode`** covering **MULTI-12 MP4 literals** PLUS **`video-copy-vp9-webm-v1`**, **`video-copy-theora-matroska-v1`** (narrow union for copy-safe **`reasonCodes[0]`**).
    2. Export **`canonicalVideoCodecForMatrix(rawCodec: string): string`** (**trim + lowercase**; reuse **`canonicalMp4CopyVideoCodec`** for **h265→hevc** before bucket tests).
    3. Structural gates (**not exactly one video stream**, **`format.format_name`** empty/null, **`codec_name`** empty on lone video stream): **`fallback-required`**, **`plannedContainer: \"mp4\"`**, **existing** **`reasonCodes`** tokens (`video-fallback-multi-video-streams`, `video-fallback-missing-format-metadata`, `video-fallback-missing-video-codec-name`).
    4. **MP4 whitelist path:** when bucket ∈ **`{h264, hevc, av1}`**, preserve branching that yields **`video-copy-safe`** vs **`fallback-required`** with **`[\"video-fallback-non-h264-video\"]`** today — **`plannedContainer: \"mp4\"`** — **unchanged MULTI-12 success literals**.
    5. **`vp9`:** **`video-copy-safe`**, **`plannedContainer: \"webm\"`**, **`reasonCodes: [\"video-copy-vp9-webm-v1\"]`**.
    6. **`theora`:** **`video-copy-safe`**, **`plannedContainer: \"matroska\"`**, **`reasonCodes: [\"video-copy-theora-matroska-v1\"]`**.
    7. **`vp8`:** **`fallback-required`**, **`plannedContainer: \"mp4\"`**, **`reasonCodes: [\"video-fallback-vp8-matrix-explicit-v1\"]`** (**replace** reliance on **`video-fallback-non-h264-video`** for VP8-specific cases).
    8. Any other codec: **`fallback-required`**, **`plannedContainer: \"mp4\"`**, **`[\"video-fallback-non-h264-video\"]`**.
    9. Preferred shape: **`export function planVideoStreamCopyFeasibility(probe: MediaProbe): StreamCopyFeasibilityPlanningResult`** (name negotiable but **exported**, **pure**) returning **`{ kind; plannedContainer; reasonCodes }`** where **`kind`** matches **`\"video-copy-safe\" | \"fallback-required\"`** symmetry with **`StreamCopyFeasibilityResult`** or a shared alias **`StreamCopyPlanningResult`** widening **`plannedContainer`** to **`PlannedContainer`**. **`void plannedAudioCodec`** if unused.
    10. **`evaluateStreamCopyFeasibility`** either (**a**) wraps **`planVideoStreamCopyFeasibility`** for **backward compat** callers with **`plannedContainer` argument validation** (**delete** stale **`plannedContainer !== mp4`** short-circuit in favor-of-matrix), OR (**b**) remove if dead — **prefer (a)** if `rg` finds external callers besides **`planMediaOutputPrelude`**.

    Update taxonomy comment (**existing block**) with **deferral** sentence for **`video-copy-vp9-matroska-v1`** (**Phase **02**/backlog`).
  </action>
  <verify>
    <automated>bun test test/domain/stream-copy-feasibility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n \"planVideoStreamCopyFeasibility|VideoStreamCopySuccessReasonCode|video-copy-vp9-webm-v1\" src/domain/stream-copy-feasibility.ts`
    - `rg -n \"video-copy-h264-mp4-v1\" src/domain/stream-copy-feasibility.ts`
    - `test/domain/stream-copy-feasibility.test.ts` exists covering **VP9→webm**, **theora→matroska**, **VP8 explicit**, **MP4 h264**.
    - `bun test test/domain/stream-copy-feasibility.test.ts` exits **0**.
  </acceptance_criteria>
  <done>Exported pure matrix **`planVideoStreamCopyFeasibility`** (+ compat glue) lives with unit tests excluding prelude wiring (**02-02**).</done>
</task>

</tasks>
