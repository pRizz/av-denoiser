---
phase: 03-ffmpeg-remux-muxers-audio-policy
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/clean.ts
  - test/app/clean.test.ts
requirements_addressed:
  - MULTI-06
  - MULTI-07
generated_by: gsd-plan-phase
lifecycle_mode: interactive
autonomous: true
phase_lifecycle_id: 03-2026-05-03T16-25-31
generated_at: "2026-05-03T17:02:59.527Z"
must_haves:
  truths:
    - "clean dry-run + execute paths pass executablePlan.plannedContainer into buildRemuxVideoWithProcessedAudioCommand for video modalities."
    - "VP9 copy-safe dry-run exposes -f webm + libopus in the remux step displayCommand."
    - "VP8 fallback execute path still emits libx264 remux argv without -f webm on final MP4 output."
---

<objective>
Thread **`plannedContainer`** from **`OutputPlan`** into **`runCleanRequest`** remux builds (**dry-run + execute**) in **`src/app/clean.ts`** and prove **MULTI-06** visibility with a **VP9/video-copy-safe** dry-run test in **`test/app/clean.test.ts`**. Preserve **fallback-required** **`reencode-h264`** behavior (**_MP4 output_**, no WebM mux flag on final file).
</objective>

<threat_model>
**Asset:** same as **03-01**.  
**Threats:** mismatch between **planner** **`plannedContainer`** and argv — prevent by always passing **`executablePlan.plannedContainer`** for **`isVideoCleanModality`** branches only.  
**Severity:** correctness.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-CONTEXT.md
@src/app/clean.ts
@test/app/clean.test.ts
@test/domain/output-plan.test.ts
</context>

<tasks>

<task type="auto">
  <name>T1 — Wire plannedContainer into remux builds</name>
  <files>src/app/clean.ts</files>
  <read_first>
    - `src/app/clean.ts` (both **`buildRemuxVideoWithProcessedAudioCommand`** call sites ~810 and ~956)
    - `src/domain/video-clean-argv.ts`
    - `src/domain/output-plan.ts` (**`OutputPlan`** video branches include **`plannedContainer`**)
  </read_first>
  <action>
    1. At **both** remux builder call sites inside **`runCleanRequest`** (dry-run preview + execute), add **`plannedContainer: executablePlan.plannedContainer`** to the object literal.
    2. TypeScript must narrow correctly: only call when **`isVideoCleanModality(executablePlan)`** (already the case); **`executablePlan.plannedContainer`** is defined on **`video-copy-safe`**, **`fallback-required`**, and **`audio-only`** — for video branches use the video plan type; if **`tsc`** complains, use a local **`const pc = executablePlan.plannedContainer`** after modality guard.
    3. **Invariant (comment 2 lines):** **`executablePlan.modality === \"fallback-required\" && videoStreamMode === \"reencode-h264\"`** ⇒ **`plannedContainer`** is **`\"mp4\"`** from prelude — do not special-case beyond passing through.
  </action>
  <verify>
    <automated>bun test test/app/clean.test.ts</automated>
    <automated>bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n \"buildRemuxVideoWithProcessedAudioCommand\" src/app/clean.ts` shows **2** blocks each containing **`plannedContainer`**.
    - `bun test test/app/clean.test.ts` exits **0**.
    - `bunx tsc --noEmit` exits **0** (or project’s equivalent **`bun run`** typecheck if documented in **`package.json`** — use repo default).
  </acceptance_criteria>
  <done>Remux calls use planner container, not extension inference.</done>
</task>

<task type="auto">
  <name>T2 — VP9 copy-safe dry-run asserts -f webm remux + opus</name>
  <files>test/app/clean.test.ts</files>
  <read_first>
    - `test/app/clean.test.ts` (**`baseCleanInput`**, **`fakeWhichVideoScenario`**)
    - `test/domain/output-plan.test.ts` VP9 probe fixture fields (copy minimal **streams** + **format** shape)
  </read_first>
  <action>
    1. Add **`const vp9WebmCopySafeProbe = JSON.stringify({ ... })`** with **one video `vp9`**, **one audio `opus`**, **`format.format_name` including `webm`** — mirror **`test/domain/output-plan.test.ts`** VP9 case enough for **`planMediaOutputPrelude`** → **`video-copy-safe`**, **`plannedContainer: \"webm\"`**, **`plannedAudioCodec: \"opus\"`**.
    2. New test **`runCleanRequest video-copy-safe dry-run VP9 includes -f webm and libopus in remux step`**:
       - **`baseCleanInput({ inputPath: \"clip.webm\", dryRun: true, ... })`**
       - **`runProcess`** returns **`vp9WebmCopySafeProbe`** for **ffprobe**
       - **`outputExists`** resolves input like other video tests
       - Expect **`outcome.clean.summary.modality === \"video-copy-safe\"`**
       - Join all **`displayCommand`** strings; expect substring **`-f webm`** (or argv equivalent in rendered text from **`renderDisplayCommand`**) and **`libopus`** and **`128k`**
    3. Extend **`runCleanRequest fallback-required execute remuxes video with libx264 when allowVideoFallback`** (or add assertion in same file): capture remux-only argv if possible, else full joined args must **not** contain **`-f webm`** when final output is **MP4** fallback (existing expectations already require **`libx264`**).
  </action>
  <verify>
    <automated>bun test test/app/clean.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test name includes **`VP9`** or **`webm`** and appears in **`test/app/clean.test.ts`**.
    - `bun test test/app/clean.test.ts` exits **0**.
  </acceptance_criteria>
  <done>Dry-run proves **MULTI-06**/**07** end-to-end for VP9 row.</done>
</task>

</tasks>
