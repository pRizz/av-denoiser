---
phase: 04-ux-verification-fixtures-regression
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/inspect-summary.ts
  - src/app/clean.ts
  - src/cli/render.ts
  - test/domain/inspect-summary.test.ts
  - test/app/clean.test.ts
requirements_addressed:
  - MULTI-08
  - MULTI-09
generated_by: gsd-plan-phase
lifecycle_mode: yolo
autonomous: true
phase_lifecycle_id: 04-2026-05-03T17-20-12
generated_at: "2026-05-03T17:25:38.454Z"
must_haves:
  truths:
    - "inspect preservation notes mention HDR/side-data caveats for WebM VP9 and Matroska Theora copy-safe paths alongside existing MP4 HEVC caveat (MULTI-08)."
    - "InspectPlanSummary round-trip tests freeze MP4 whitelist **reasonCodes** literals **video-copy-{h264,hevc,av1}-mp4-v1** (MULTI-08 JSON + MULTI-12)."
    - "clean inspect failure copy for fallback-required remains unchanged unless a gap is proven; clean success JSON exposes planned modality alignment fields trusted by automation (MULTI-09)."
---

<objective>
Implement **MULTI-08** (**`buildPreservationNotesFromPlan`** HDR/side-data parity for **VP9/WebM** and **Theora/MKV**) respecting **`MAX_PRESERVATION_NOTES`**, with unit tests locking strings. Implement **MULTI-09** by extending **`CleanPlanSummary`** so **`clean --json`** (via **`cleanSummaryForJson`**) and human **`renderCleanPlanText`** surface **`plannedContainer`**, **`plannedAudioCodec`**, and **`reasonCodes`** for successful **clean** outcomes (including **dry-run**) when the **`OutputPlan`** is non-**unsupported**, so **`modality: fallback-required`** / **`videoPolicy: re-encoded`** cannot be mistaken for a pure copy without reading **`reportText`** alone.
</objective>

<threat_model>
**Asset:** local CLI output (paths, modality, codec labels). No new network or secret handling.  
**Threats addressed:** misleading trust copy (**false impression of HDR safety or stream-copy**) — mitigated via explicit caveat bullets and structured JSON parity with **`inspect`**.  
**Severity:** correctness / operator trust (**informational** for ASVS); no new injection surfaces.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-verification-fixtures-regression/04-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/domain/inspect-summary.ts
@src/app/clean.ts
@src/cli/render.ts
</context>

<tasks>

<task type="auto">
  <name>T1 — Preservation notes for WebM VP9 + Matroska Theora (**MULTI-08**)</name>
  <files>src/domain/inspect-summary.ts, test/domain/inspect-summary.test.ts</files>
  <read_first>
    - `.planning/phases/04-ux-verification-fixtures-regression/04-CONTEXT.md`
    - `src/domain/inspect-summary.ts`
    - `test/domain/inspect-summary.test.ts`
  </read_first>
  <action>
    1. In **`buildPreservationNotesFromPlan`**, inside **`case "video-copy-safe"`** for **`plannedContainer === "webm"`**, **after** the existing WebM+Opus stream-copy sentence, **push** a second bullet that states **HDR / transfer characteristics / side metadata** may not reproduce identically across players after **VP9** stream copy — same *intent* as the **MP4 HEVC** bullet (**best-effort**, bytes preserved, presentation may differ).
    2. For **`plannedContainer === "matroska"`**, **after** the existing Theora+MKV+AAC sentence, **push** an analogous **HDR/side-data** caveat bullet for **Theora** (even if rare in practice — operator honesty per **D-01**).
    3. Keep total notes within **`MAX_PRESERVATION_NOTES`**: if **`slice` would drop** the new caveat, **reorder** so **truth-critical** items survive per **04-CONTEXT D-02** (document ordering in a **short code comment** above **`case "video-copy-safe"`**: e.g. prefer **container-specific copy line** + **HDR caveat** + **FFmpeg validation reminder** before optional extras).
    4. Add **`src/domain/output-plan`** type imports only in tests (**not production**) as needed.

    5. **MULTI-08 JSON parity + MULTI-12 inspect literals:** extend **`inspect-summary.test.ts`** importing **`outputPlanToInspectSummary`**: three **`OutputPlan`** objects (**video-copy-safe**, **`plannedContainer: mp4`**) with **`reasonCodes`** **`["video-copy-h264-mp4-v1"]`**, **`["video-copy-hevc-mp4-v1"]`**, **`["video-copy-av1-mp4-v1"]`** respectively (fill minimal shared fields identical to **`inspect-summary`** tests pattern). For each **`summary`** assert: **`plannedContainer === "mp4"`**, **`reasonCodes`** length **1**, **`reasonCodes[0]` equals literal**, at least **one note includes `"Stream-copy"`**, **`preservationNotes.length >= 2`** (**MP4** path emits **paired** messaging today).
  </action>
  <verify>
    <automated>bun test test/domain/inspect-summary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "video-copy-h264-mp4-v1|video-copy-hevc-mp4-v1|video-copy-av1-mp4-v1" test/domain/inspect-summary.test.ts`
    - `rg -n "webm|matroska|HDR|side" src/domain/inspect-summary.ts`
    - `bun test test/domain/inspect-summary.test.ts` exits **0**.
  </acceptance_criteria>
  <done>VP9/WebM and Theora/MKV inspect bullets carry HDR/side-data caveat language; tests lock behavior.</done>
</task>

<task type="auto">
  <name>T2 — Clean summary + JSON parity (**MULTI-09**)</name>
  <files>src/app/clean.ts, src/cli/render.ts, test/app/clean.test.ts, test/app/inspect.test.ts</files>
  <read_first>
    - `src/app/clean.ts` — **`CleanPlanSummary`**, **`summary` construction** (~L854)
    - `src/cli/render.ts` — **`renderCleanPlanText`**, **`cleanSummaryForJson`**
    - `test/app/clean.test.ts` — existing **clean** expectations
  </read_first>
  <action>
    1. Extend **`CleanPlanSummary`** with **readonly** optional fields (use **`null`** for inapplicable cases to keep JSON stable):
       - **`plannedContainer`**: `PlannedContainer | null`
       - **`plannedAudioCodec`**: `PlannedAudioCodec | null` (import type from **`output-plan`**)
       - **`reasonCodes`**: `readonly string[]` (empty array when none)
    2. When building **`summary`** in **`runCleanRequest`** for **every** successful **plan** path (**video** and **audio-only**), populate from **`executablePlan`**:
       - **`audio-only`**: set container/audio/reason codes from plan; **`reasonCodes`** may be the plan’s existing codes.
       - **`video-copy-safe` / `fallback-required`**: same.
       - Do **not** fabricate codes for **unsupported** (unreachable in **clean** success).
    3. **`renderCleanPlanText`**: after **Modality**, add when values non-null:
       - **`Planned container`** line
       - **`Planned audio codec`** line
       - **`Reason codes`** bulleted list (or **`- n/a`** when array empty — pick one consistent style; prefer **bullets** matching **`inspect`** when length &gt; 0, else omit section or print **`n/a`** — **document choice in code comment**).
    4. **`cleanSummaryForJson`**: include the **new fields** alongside **`presetId`, `modality`, `steps`**.
    5. Add/adjust **`test/app/clean.test.ts`** (smallest harness): **dry-run success** JSON or parsed summary object reflects **`plannedContainer`** for an existing probe path (reuse **fixture JSON** patterns already in file). Include at least one assertion on **`reasonCodes`** array **non-empty** for a **`video-copy-safe`** probe that already emits **`video-copy-h264-mp4-v1`**.
  </action>
  <verify>
    <automated>bun test test/app/clean.test.ts test/app/inspect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "plannedContainer|plannedAudioCodec|reasonCodes" src/app/clean.ts src/cli/render.ts`
    - **`bun test test/app/clean.test.ts test/app/inspect.test.ts`** exits **0** (**inspect** regressions gate **MULTI-09**/ **`allowVideoFallback`** contract).
  </acceptance_criteria>
  <done>Structured **clean** output mirrors **`inspect`**-grade planning fields without weakening **fallback-required** wording.</done>
</task>

</tasks>
