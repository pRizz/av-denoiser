---
phase: 04-ux-verification-fixtures-regression
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/domain/stream-copy-feasibility.ts
  - src/domain/clean-output-verify.ts
  - test/fixtures/ffprobe/minimal-video-vp9-webm-matrix.json
  - test/fixtures/ffprobe/minimal-video-theora-matroska-matrix.json
  - test/domain/clean-output-verify.test.ts
  - test/domain/output-plan.fixture.test.ts
  - test/domain/multiregression-multi12-literals.test.ts
requirements_addressed:
  - MULTI-10
  - MULTI-11
  - MULTI-12
generated_by: gsd-plan-phase
lifecycle_mode: interactive
autonomous: true
phase_lifecycle_id: 04-plan-2026-05-03T17-25-38
generated_at: "2026-05-03T17:25:38.454Z"
must_haves:
  truths:
    - "verifyCleanOutput compares canonical video codec tags for VP9/Theora/AV1/HEVC aliases using a single feasibility-module helper (**MULTI-10**)."
    - "minimal ffprobe JSON fixtures exist for VP9→WebM and Theora→Matroska matrix planning paths (**MULTI-11**)."
    - "tests freeze MP4 MULTI-12 success literals video-copy-{h264,hevc,av1}-mp4-v1 (**MULTI-12**)."
---

<objective>
Ship **MULTI-10** (post-copy **canonical codec equality** beyond **h265/hevc**), **MULTI-11** (**ffprobe JSON fixtures** for matrix planning), and **MULTI-12** (explicit regression locks on **`video-copy-*-mp4-v1`** literals). Keep imports acyclic (**verify** consumes **feasibility** helper only).

**Depends on wave 1** only for stabilized **`clean`/JSON surfaces** — if **04-01** slips, defer **fixture-based clean JSON** assertions in this wave to **`clean-output-verify`** / fixture tests only.
</objective>

<threat_model>
**Asset:** synthetic probe fixtures on disk (**no secrets**).  
**Threats addressed:** verifier **false negatives/positives** on codec aliases → **narrow, tested** normalization.  
**Severity:** informational; correctness risk is **functional**.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-verification-fixtures-regression/04-CONTEXT.md
@src/domain/stream-copy-feasibility.ts
@src/domain/clean-output-verify.ts
@test/fixtures/ffprobe/minimal-video-audio.json
</context>

<tasks>

<task type="auto">
  <name>T1 — Canonical codec helper + verifyCleanOutput (**MULTI-10**)</name>
  <files>src/domain/stream-copy-feasibility.ts, src/domain/clean-output-verify.ts, test/domain/clean-output-verify.test.ts</files>
  <read_first>
    - `src/domain/stream-copy-feasibility.ts` — **`canonicalMp4CopyVideoCodec`**, **`canonicalVideoCodecForMatrix`**
    - `src/domain/clean-output-verify.ts`
    - `test/domain/clean-output-verify.test.ts`
  </read_first>
  <action>
    1. Add **`export function canonicalVideoCodecForVerify(codecName: string): string`** in **`stream-copy-feasibility.ts`** (implementation detail: may delegate to **`canonicalVideoCodecForMatrix`** after trimming/lowercasing **or** shared private helper — avoid duplicate tables).
       - **Mandatory aliases (document in JSDoc):**
         - **`h265` → `hevc`** (reuse existing mapping)
         - **`vp09` → `vp9`** if **`ffprobe`** emits **`vp09`**
         - **`av01` → `av1`** if **`ffprobe`** emits **`av01`**
       - **`theora`**, **`vp9`**, **`av1`**, **`hevc`**, **`h264`**: lowercase trim, no other silent merges (**D-08** narrow table).
    2. **`verifyCleanOutput`**: replace direct **`canonicalMp4CopyVideoCodec`** compare with **`canonicalVideoCodecForVerify`** for **`video-copy-safe` + copied** branch.
    3. Extend **`clean-output-verify.test.ts`**:
       - **VP9**: input **`vp09`** output **`vp9`** ⇒ **`ok`**
       - **AV1**: input **`av01`** output **`av1`** ⇒ **`ok`**
       - **VP9 vs h264** ⇒ **`video-copy-mismatch`**
       - **Theora mismatch**: **`theora`** vs **`vp9`** ⇒ **`video-copy-mismatch`**
    4. **`rg`** ensures **`canonicalMp4CopyVideoCodec` remains exported** if other modules import it OR re-export verify helper only — **do not remove** **`canonicalMp4CopyVideoCodec`** without fixing all `rg` call sites (executor must run **`rg`**).
  </action>
  <verify>
    <automated>bun test test/domain/clean-output-verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "canonicalVideoCodecForVerify" src/domain/stream-copy-feasibility.ts src/domain/clean-output-verify.ts`
    - **`bun test test/domain/clean-output-verify.test.ts`** exits **0**.
  </acceptance_criteria>
  <done>Verifier honors **narrow** **`ffprobe`** codec aliases aligned with **`stream-copy-feasibility`**.</done>
</task>

<task type="auto">
  <name>T2 — Ffprobe matrix fixtures (**MULTI-11**)</name>
  <files>test/fixtures/ffprobe/minimal-video-vp9-webm-matrix.json, test/fixtures/ffprobe/minimal-video-theora-matroska-matrix.json, test/domain/output-plan.fixture.test.ts</files>
  <read_first>
    - `test/fixtures/ffprobe/minimal-video-audio.json`
    - `src/domain/media-probe.ts` or **Zod** parser entry used by tests
    - `test/domain/output-plan.test.ts` — **`planMediaOutput`** patterns
  </read_first>
  <action>
    1. Create **`test/fixtures/ffprobe/minimal-video-vp9-webm-matrix.json`**: **`streams`** = lone **`codec_type: video`**, **`codec_name: vp9`**, plus one **default stereo audio** (same shape as **`minimal-video-audio.json`**); **`format.duration`** numeric string; **`format.format_name`** must satisfy **`planVideoStreamCopyFeasibility`** gates (use a **webm-like** substring **or** **`matroska,webm`** style string consistent with **`02`** fixtures — executor MUST confirm **`planMediaOutput` → video-copy-safe** with **`video-copy-vp9-webm-v1`** via unit test below).
    2. Create **`test/fixtures/ffprobe/minimal-video-theora-matroska-matrix.json`** parallel for **`codec_name: theora`** and **format_name** passing gates → **`video-copy-theora-matroska-v1`** (likely **`ogg`** or **`matroska`**-compatible **`format.format_name`** per **`stream-copy-feasibility`** prelude — **mirror** assertions from **`output-plan.test.ts`** **theora** case).
    3. Add **`test/domain/output-plan.fixture.test.ts`**: **`Bun.file(...).json()`** (**or **`readFileSync`**) parsing each fixture into **`planMediaOutput`**, asserting **`plannedContainer`**, **`modality`**, and **`reasonCodes[0]`** exact strings.
  </action>
  <verify>
    <automated>bun test test/domain/output-plan.fixture.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`test/fixtures/ffprobe/minimal-video-vp9-webm-matrix.json`** exists
    - **`test/fixtures/ffprobe/minimal-video-theora-matroska-matrix.json`** exists
    - `bun test test/domain/output-plan.fixture.test.ts` exits **0**.
  </acceptance_criteria>
  <done>Fixtures exercised by **`planMediaOutput`** assertions without ffmpeg binary.</done>
</task>

<task type="auto">
  <name>T3 — MULTI-12 literal regression lockfile</name>
  <files>test/domain/multiregression-multi12-literals.test.ts, src/domain/stream-copy-feasibility.ts</files>
  <read_first>
    - `src/domain/stream-copy-feasibility.ts` — **`MP4_STREAM_COPY_SUCCESS`** / **`VideoStreamCopySuccessReasonCode`**
    - `.planning/REQUIREMENTS.md` § **MULTI-12**
  </read_first>
  <action>
    1. Add **`test/domain/multiregression-multi12-literals.test.ts`**: three tests constructing **minimal probes** for **h264/hevc(or h265)** / **av1** that **`planVideoStreamCopyFeasibility`** marks **`video-copy-safe`**, asserting **`reasonCodes[0]`** equals **`video-copy-h264-mp4-v1`**, **`video-copy-hevc-mp4-v1`**, **`video-copy-av1-mp4-v1`** respectively (**string equality** freeze).
    2. Prefer **`planVideoStreamCopyFeasibility`** as the oracle — no snapshot of **`stream-copy-feasibility.ts`** source text.
  </action>
  <verify>
    <automated>bun test test/domain/multiregression-multi12-literals.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File **`test/domain/multiregression-multi12-literals.test.ts`** exists (**no spaces**).
    - `rg -n "video-copy-h264-mp4-v1|video-copy-hevc-mp4-v1|video-copy-av1-mp4-v1" test/domain/multiregression-multi12-literals.test.ts`
    - **`bun test test/domain/multiregression-multi12-literals.test.ts`** exits **0**.
  </acceptance_criteria>
  <done>**MULTI-12** feasibility literals cannot drift without touching this test.</done>
</task>

<task type="auto">
  <name>T4 — Phase repository verify (**roadmap criterion 3**)</name>
  <files>package.json</files>
  <read_first>
    - `package.json` (**`verify` script**)
  </read_first>
  <action>
    Run **after **T1**–**T3** implementations land** (**wave 2** complete): **`bun run verify`** (**Biome + `tsc` + `bun test`**) — same as roadmap success criterion **#3**.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - Terminal: **`bun run verify`** exits **0**.
  </acceptance_criteria>
  <done>Whole phase meets shipped verification bar.</done>
</task>

</tasks>
