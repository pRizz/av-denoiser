---
phase: 08-gap-closure-phase-03-remux-pipeline-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/audio-pipeline-argv.ts
  - src/app/clean.ts
  - test/domain/audio-pipeline-argv.test.ts
autonomous: true
requirements_addressed:
  - MULTI-06
  - MULTI-07
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 08-2026-05-04T11-14-07
generated_at: "2026-05-04T11:15:22.926Z"
must_haves:
  truths:
    - "Intermediate video-pipeline artifact basename uses **`pipeline-audio-out.<ext>`** where **`<ext>`** matches **`encodeDeliverableArgs`** branching: **`.mp4`** (**AAC + MP4**), **`.webm`** (**Opus + WebM**), **`.mkv`** (**Opus + Matroska**), **`.wav`** (**pcm_s16le + WAV**), else fallback **`.mp4`** (same as **`encodeDeliverableArgs`** trailing branch)."
    - "**`grep -r 'pipeline-audio-out\\.mp4'`** finds **zero** usages in **`src/`** after change (hardcoded mux-misleading path removed)."
---

<objective>
Close the **mux/filename mismatch** from **Phase 08** / **08-CONTEXT.md** **D-04–D-07**: derive **`pipelineAudioPath`** (video **`clean`** execute path in **`clean.ts`) from **`(plannedAudioCodec, plannedContainer)`** via one **pure** exporter colocated with **`encodeDeliverableArgs`**, extend unit tests, **`bun run verify`** exits **0**.

</objective>

<threat_model>
**Asset:** operator trust — temp filenames should not imply MP4 mux when FFmpeg uses **`-f webm`** / **`-f matroska`** for the encode-deliverable step.
**Threats:** helper drifts from **`encodeDeliverableArgs`**; partial refactor leaves stale **`.mp4`** string elsewhere.
**Mitigation:** single shared mapping; **`rg`** acceptance on **`pipeline-audio-out\\.mp4`**; assertions in **`audio-pipeline-argv.test.ts`** for every **`encodeDeliverableArgs`** branch tuple.
**Severity:** informational / process UX — ASVS L1 ancillary.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-gap-closure-phase-03-remux-pipeline-trust/08-CONTEXT.md
@.planning/ROADMAP.md
@src/domain/audio-pipeline-argv.ts
@src/app/clean.ts
@test/domain/audio-pipeline-argv.test.ts
</context>

<tasks>

<task type="auto">
  <name>T1 — Baseline verify</name>
  <files>package.json</files>
  <read_first>
    - package.json (`scripts.verify`)
    - src/domain/audio-pipeline-argv.ts (`encodeDeliverableArgs` trailing branches lines ~281–307)
    - src/app/clean.ts (~lines 905–914 `pipelineAudioPath`)
  </read_first>
  <action>
From repo root run **`bun run verify`**. Capture integer **N** from **`bun test`** summary (**`N pass`**) for plan **08-02**.

If non-zero exit: fix before renaming (do not proceed with superficial filename-only patch on red mainline unless failure is unrelated — document blocker in SUMMARY).

  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - Final **`bun run verify`** for this wave exits code **0** before declaring plan done.
    - Executor records **N** from stdout for **08-02**.
  </acceptance_criteria>
  <done>Green baseline before and after edits.</done>
</task>

<task type="auto">
  <name>T2 — Export `pipelineAudioOutIntermediateBasename` from audio-pipeline-argv.ts</name>
  <files>src/domain/audio-pipeline-argv.ts</files>
  <read_first>
    - src/domain/audio-pipeline-argv.ts (full **`encodeDeliverableArgs`**)
    - src/domain/output-plan.ts (`PlannedAudioCodec`, `PlannedContainer`)
  </read_first>
  <action>

1. Add **exported** pure function **`pipelineAudioOutIntermediateBasename(audioCodec: PlannedAudioCodec, container: PlannedContainer): string`** returning **exactly**:

   ```text
   "pipeline-audio-out.mp4"   when audioCodec === "aac" && container === "mp4"
   "pipeline-audio-out.mkv"   when audioCodec === "opus" && container === "matroska"
   "pipeline-audio-out.wav"   when audioCodec === "pcm_s16le" && container === "wav"
   "pipeline-audio-out.webm"  when audioCodec === "opus" && container === "webm"
   "pipeline-audio-out.mp4"   otherwise (AAC in MP4 — same as encodeDeliverableArgs fallback)
   ```

2. Optionally refactor **`encodeDeliverableArgs`** to call **`pipelineAudioOutIntermediateBasename`** only for choosing output filename is **incorrect** — **do NOT** replace output path argument with this helper blindly: **`encodeDeliverableArgs`** takes **`outputPath`** from caller.

   Prefer: keep **`encodeDeliverableArgs`** logic intact; implement **`pipelineAudioOutIntermediateBasename`** by **duplicating the same four `if` conditions + fallback** immediately above or below **`encodeDeliverableArgs`** with a **one-line comment**: `Keep in sync with encodeDeliverableArgs — Phase 08-01.`

Or extract **private** **`extensionForEncodeDeliverable(...): string`** used by **both** if you refactor without changing argv arrays (executor choice).

  </action>
  <verify>
    <automated>rg -n "pipelineAudioOutIntermediateBasename" src/domain/audio-pipeline-argv.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`rg "^export function pipelineAudioOutIntermediateBasename"`** matches **`src/domain/audio-pipeline-argv.ts`**.
    - **`encodeDeliverableArgs`** behavior unchanged: **`rg -n '\"-f\", \"webm\"' src/domain/audio-pipeline-argv.ts`** still matches **inside** **`encodeDeliverableArgs`** branch for opus+webm.
  </acceptance_criteria>
  <done>Helper exists and mirrors encode-deliverable matrix.</done>
</task>

<task type="auto">
  <name>T3 — Wire `clean.ts` video branch</name>
  <files>src/app/clean.ts</files>
  <read_first>
    - src/app/clean.ts (video modality block assigning **`pipelineAudioPath`**)
    - src/domain/audio-pipeline-argv.ts (`pipelineAudioOutIntermediateBasename`)
  </read_first>
  <action>
Replace **`const pipelineAudioPath = join(tempRoot, "pipeline-audio-out.mp4");`** with **`join(tempRoot, pipelineAudioOutIntermediateBasename(executablePlan.plannedAudioCodec, executablePlan.plannedContainer))`** once **`executablePlan`** is in scope (same block ~911).

Import **`pipelineAudioOutIntermediateBasename`** from **`../domain/audio-pipeline-argv`** (preserve existing import sort / path style).

  </action>
  <verify>
    <automated>rg pipeline-audio-out src/app/clean.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -rnF 'pipeline-audio-out.mp4' src/`** exits **1** (**no matches**).
    - **`clean.ts`** contains **`pipelineAudioOutIntermediateBasename(`**.
  </acceptance_criteria>
  <done>Execute path intermediate matches planned codec/container.</done>
</task>

<task type="auto">
  <name>T4 — Unit tests for helper</name>
  <files>test/domain/audio-pipeline-argv.test.ts</files>
  <read_first>
    - test/domain/audio-pipeline-argv.test.ts (import style / patterns)
    - src/domain/audio-pipeline-argv.ts (`pipelineAudioOutIntermediateBasename`)
  </read_first>
  <action>
Add **`bun:test`** cases importing **`pipelineAudioOutIntermediateBasename`** asserting tuples:

```text
(aac, mp4) → pipeline-audio-out.mp4
(opus, webm) → pipeline-audio-out.webm
(opus, matroska) → pipeline-audio-out.mkv
(pcm_s16le, wav) → pipeline-audio-out.wav
(aac, matroska) → pipeline-audio-out.mp4   // fallback branch
```
  </action>
  <verify>
    <automated>bun test test/domain/audio-pipeline-argv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`rg -n "pipelineAudioOutIntermediateBasename" test/domain/audio-pipeline-argv.test.ts`** exits **0** with ≥ **1** **`test(`**.
    - **`bun test test/domain/audio-pipeline-argv.test.ts`** exits **0**.
  </acceptance_criteria>
  <done>Regression lock on extension matrix.</done>
</task>

<task type="auto">
  <name>T5 — Final verify gate</name>
  <files>(none)</files>
  <read_first>
    - (none — repeat T1)
  </read_first>
  <action>
Run **`bun run verify`** from repo root; must exit **0** after **T2–T4**.

  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - **`bun run verify`** exit code **0**.
  </acceptance_criteria>
  <done>Plan 08-01 ready for SUMMARY.md + handoff to 08-02.</done>
</task>

</tasks>
