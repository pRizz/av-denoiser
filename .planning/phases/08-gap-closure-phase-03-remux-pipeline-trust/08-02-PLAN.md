---
phase: 08-gap-closure-phase-03-remux-pipeline-trust
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
files_modified:
  - .planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/v1.1-MILESTONE-AUDIT.md
  - .planning/ROADMAP.md
autonomous: true
requirements_addressed:
  - MULTI-06
  - MULTI-07
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 08-2026-05-04T11-14-07
generated_at: "2026-05-04T11:15:22.926Z"
must_haves:
  truths:
    - "`.planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md` exists with `status: passed` when **`bun run verify`** is green **after** **08-01** merges; cites **MULTI-06**, **MULTI-07**, **`-f webm`**, **`-f matroska`**, **`pipelineAudioOutIntermediateBasename`** + **`clean.ts`** wiring, **`video-clean-argv.test.ts`**, **08-01** SUMMARY optional cross-link."
    - "**REQUIREMENTS.md** toggles **MULTI-06**/**MULTI-07** to **`[x]`** and traceability rows **`Phase 3 (verified Phase 8 gap closure) | Complete`** only when **`03-VERIFICATION`** is **`passed`."
---

<objective>
Document **Phase 03** (**MULTI-06**, **MULTI-07**) with **`.planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md`**, reconcile **`.planning/REQUIREMENTS.md`**, refresh **`.planning/v1.1-MILESTONE-AUDIT.md`** gap rows (**MULTI-06**/**07**) + inventory table snippets, pointer **`.planning/ROADMAP.md` ## Next action** toward **Phase 09** — after **08-01** code lands and **`bun run verify`** is green.

</objective>

<threat_model>
**Asset:** requirements / audit honesty vs FFmpeg argv + disk artifacts.
**Threats:** **VERIFICATION** **passed** while verify red; REQ `[x]` without verification artifact.
**Mitigation:** gated **T3** on **`03-VERIFICATION`** frontmatter; cite **N** tests from **`bun run verify`**; traceability wording mirrors **MULTI-01** closure pattern (**Phase N (verified Phase M gap closure)**).
**Severity:** ASVS informational.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-gap-closure-phase-03-remux-pipeline-trust/08-CONTEXT.md
@.planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-CONTEXT.md
@.planning/phases/01-multi-container-output-model-path-derivation/01-VERIFICATION.md
@src/domain/video-clean-argv.ts
@src/domain/audio-pipeline-argv.ts
@src/app/clean.ts
@test/domain/video-clean-argv.test.ts
@test/domain/audio-pipeline-argv.test.ts
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>T1 — Verify gate (post‑08‑01)</name>
  <files>package.json</files>
  <read_first>
    - `.planning/phases/08-gap-closure-phase-03-remux-pipeline-trust/08-01-SUMMARY.md` (optional — confirms **08-01** done)
    - `package.json` (`verify` script)
  </read_first>
  <action>
Run **`bun run verify`**. Capture **N** from **`bun test`** summary line (**`N pass`**). If exit ≠ **0**, **STOP**: write **`.planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md`** with **`status: gaps_found`** explaining failure (per **Phase 06** pattern) — **omit** REQ checkbox promotion (**T4** skips).

If exit **0**: proceed with **`status: passed`** path for **T2–T5**.

  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - Last **`bun run verify`** in this plan attempt recorded with exit code (**0** for passed path).
    - Integer **N** captured for **`03-VERIFICATION.md`** body.
  </acceptance_criteria>
  <done>Evidence base for VERIFICATION prose.</done>
</task>

<task type="auto">
  <name>T2 — Write 03-VERIFICATION.md (MULTI‑06 / MULTI‑07)</name>
  <files>.planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-multi-container-output-model-path-derivation/01-VERIFICATION.md (structure clone)
    - src/domain/video-clean-argv.ts (`buildRemuxVideoWithProcessedAudioCommand` muxFormatArgs MP4 omission)
    - src/domain/audio-pipeline-argv.ts (`encodeDeliverableArgs`, **`pipelineAudioOutIntermediateBasename`**)
    - src/app/clean.ts (imports + **`pipelineAudioPath`** join)
    - test/domain/video-clean-argv.test.ts
    - test/domain/audio-pipeline-argv.test.ts (Phase **08** helper tests)
  </read_first>
  <action>
Create **`.planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md`**:

YAML frontmatter:
```yaml
---
status: passed   # use gaps_found if T1 was red — then leave REQ untouched in T4
phase: 03-ffmpeg-remux-muxers-audio-policy
generated_at: "<ISO‑8601 Z from execution>"
---
```

Body sections mirror **01-VERIFICATION**:
1. **Title:** **Phase 03 verification — FFmpeg remux: muxers & audio policy**.
2. **Automated verification** paragraph: **`bun run verify`** + exact **N** from **T1**.
3. **Must-haves vs evidence** subsection **MULTI-06 (mux argv):**
   | Must-have | Evidence |
   |-|-|
   | **`-f webm`** present on remux argv when **`plannedContainer: "webm"`** | `src/domain/video-clean-argv.ts` + `buildRemuxVideoWithProcessedAudioCommand` |
   | **`-f matroska`** when **`plannedContainer: "matroska"`** | same |
   | **MP4**: no **`‑f mp4`** added (Phase **03 CONTEXT** D-03) | same (`muxFormatArgs` empty for **`mp4`**) |
   | Tests argv shape | **`test/domain/video-clean-argv.test.ts`** (**grep** cites **`-f webm`**, **`-f matroska`** expectations) |

4. **MULTI-07 + intermediate trust (integration):**
   | Must-have | Evidence |
   |-|-|
   | Per-container audio policy **AAC**/ **Opus** argv on remux (**WebM→Opus**, **MKV/MP4→AAC**) | **`video-clean-argv.ts`** `switch (plannedAudioCodec)` |
   | **`encode-deliverable`** FFmpeg **`-f`** matches container for processed audio (**before** final remux) | **`audio-pipeline-argv.ts`** `encodeDeliverableArgs` |
   | Intermediate filename aligns with **`(plannedAudioCodec, plannedContainer)`** (audit gap closed **Phase 08**) | **`pipelineAudioOutIntermediateBasename`** + **`clean.ts`** |
   | Unit coverage | **`audio-pipeline-argv.test.ts`** + **`video-clean-argv.test.ts`** |

5. **Requirement IDs** bullets: restate closure for **MULTI-06**, **MULTI-07**.
6. **Human verification**: **None required** when `status: passed`.

  </action>
  <verify>
    <automated>test -f .planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - **`rg -n "phase: 03-ffmpeg-remux-muxers-audio-policy" .planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md`** matches.
    - **`rg -n "MULTI-06|MULTI-07|\\-f webm|\\-f matroska|pipelineAudioOutIntermediateBasename" .planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md`**
    - Body includes digit line matching **`[0-9]+ pass`** (from **T1** **N**).
  </acceptance_criteria>
  <done>Phase 03 GSD-verified artifact exists.</done>
</task>

<task type="auto">
  <name>T3 — REQUIREMENTS.md — MULTI‑06 / MULTI‑07 closure</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md (frontmatter `status`)
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>

**Only if** **`status: passed`** in **03-VERIFICATION.md**:

1. Checklist lines **MULTI-06**, **MULTI-07**: change **`[ ]` → `[x]`** (leave all other lines unchanged).

2. **Traceability table** rows **MULTI-06**, **MULTI-07**:

   ```markdown
   | MULTI-06 | Phase 3 (verified Phase 8 gap closure) | Complete |
   | MULTI-07 | Phase 3 (verified Phase 8 gap closure) | Complete |
   ```

   (Preserve row order compatible with sibling rows — executor may reorder if table reordering forbidden — keep **MULTI‑06** before **MULTI‑07**.)

3. **Coverage** footnote (**## Traceability** paragraph): Replace with wording that **`13`/13 mapped — `7` Complete** (**MULTI-01–MULTI‑07**); cite **[`03-VERIFICATION.md`](phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md)** for **MULTI‑06/07**; **`6`** **Pending** in **Phase 9** (**MULTI‑08**–**MULTI‑13**); preserve link **`v1.1-MILESTONE-AUDIT.md`**.

**If `status: gaps_found`:** do **not** edit **MULTI‑06/07** checkboxes nor trace rows.

  </action>
  <verify>
    <automated>rg -n "MULTI-06|MULTI-07" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - When verification **passed**: **`rg '^- \\[x\\] \\*\\*MULTI-06'`** matches **`.planning/REQUIREMENTS.md`**.
    - When verification **passed**: traceability contains substring **`Phase 3 (verified Phase 8 gap closure)`** adjacent to **MULTI‑06** and **MULTI‑07**.
    - When verification **passed**: **`rg --fixed-strings "7`** + **Complete"** `.planning/REQUIREMENTS.md`** exits **0** (Coverage sentence cites **seven** completes).
    - When verification **`gaps_found`**: **`rg '^- \\[ \\] \\*\\*MULTI-06'`** matches (still unchecked — manual escape in shell as needed).

  </acceptance_criteria>
  <done>REQ aligns with VERIFICATION outcome.</done>
</task>

<task type="auto">
  <name>T4 — v1.1-MILESTONE-AUDIT.md — MULTI‑06 / MULTI‑07 rows + inventory refresh</name>
  <files>.planning/v1.1-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md
    - .planning/v1.1-MILESTONE-AUDIT.md (YAML **`gaps.requirements`** MULTI‑06/MULTI‑07; **Phase verification inventory** table; **`scores`**)
    - `.planning/REQUIREMENTS.md` (after **T3** if passed path)
  </read_first>
  <action>

**Only when** **`03-VERIFICATION.md` `status: passed`** AND **T3 promoted REQ**:

1. In **`gaps.requirements`** for **`id: MULTI-06`** and **`id: MULTI-07`** YAML blocks:
   - Set **`status: satisfied`** (**replace** **`partial`**).
   - Set **`verification_status: passed`** (**replace** **`missing`**).
   - Replace **`evidence:`** with folded text citing **[`03-VERIFICATION.md`](/.planning/phases/03-ffmpeg-remux-muxers-audio-policy/03-VERIFICATION.md)**, **`pipelineAudioOutIntermediateBasename`**, REQ **`[x]`** **MULTI‑06/07** post‑**Phase 08** (omit stale “intermediate may remain `.mp4`”) — state **closed** Phase **08** unless residual caveat documented in **VERIFICATION**.

2. **`scores.requirements_strict`**: increment from **`5/13`** → **`7/13`**.

3. **`scores.phases_verified`**: set **`3/5`** (Phases **01**, **02**, **03** now have **`VERIFICATION.md`** alongside core phase dirs — **executor**: if **Phase 05** verification exists unexpectedly, reconcile count truthfully).

4. **Executive summary bullet 3**: soften or strike the **`.mp4`** intermediate clause if wording still claims unconditional mismatch (**optional** sanity edit — must not contradict code).

5. **Phase verification inventory** table (`## Phase verification inventory`): row **03** → **`03‑VERIFICATION.md — status: passed`** (after **Phase 08**).

6. **Requirements three‑source table** MULTI‑06–07 row (**`| MULTI‑06–07 |`** line): interpretation column → **Passed (03‑VERIFICATION Phase 08 closure)** (**adjust** **`[x]`** cell if stale vs **REQUIREMENTS** — REQ checkboxes `[x]` for **MULTI‑06/07**).

  </action>
  <verify>
    <automated>rg -n "id: MULTI-06|id: MULTI-07|03-VERIFICATION" .planning/v1.1-MILESTONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - **`rg -n "id: MULTI-06" -A12 .planning/v1.1-MILESTONE-AUDIT.md`** shows **`status: satisfied`** and **`verification_status: passed`** in that block (**not** **`partial`** + **`missing`** together for verification).
    - Same for **`id: MULTI-07`**.
    - **`rg -n '^  requirements_strict: \"7/13\"' .planning/v1.1-MILESTONE-AUDIT.md`** OR appropriate YAML indentation match for **`7/13`**.
    - Inventory table **`03`** line references **`VERIFICATION`** (not **`Missing`** as sole descriptor).

  </acceptance_criteria>
  <done>Audit ledger matches closures.</done>
</task>

<task type="auto">
  <name>T5 — ROADMAP.md next‑action footer</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (`## Next action`)
  </read_first>
  <action>

Update **`## Next action`** to summarize **Phase 08 closure** (**03‑VERIFICATION** + **`pipelineAudioOutIntermediateBasename`**), **`bun run verify`** green, and direct operators to **`/gsd-plan-phase 09`** (**output verify**) or **`/gsd-execute-phase 09`** depending on roadmap dependency graph — **preserve** factual accuracy: Phase **09** succeeds **Phase 08** (**ROADMAP** already states **Depends on**).

Do **not** rewrite phase bullets **08**–**09** prose above the footer unless a factual typo is uncovered.

  </action>
  <verify>
    <automated>rg "Next action" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - **`## Next action`** contains **`Phase 08`** closure language **OR** **`03-VERIFICATION` / **MULTI-06`/07**.
    - Pointer to **`Phase 09`** or **`/gsd-plan-phase 09`** / **`execute`** variant present.

  </acceptance_criteria>
  <done>Roadmap points to next milestone work.</done>
</task>

</tasks>
