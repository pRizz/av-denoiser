---
phase: 04-core-audio-pipeline-sox-cleanup
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - src/domain/cli-request.ts
  - src/cli/command.ts
  - src/app/run-command.ts
  - src/cli/render.ts
  - src/cli/main.ts
  - test/cli/command.test.ts
autonomous: true
requirements:
  - MEDIA-01
  - PIPE-02
  - PIPE-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 04-2026-05-02T00-13-28
generated_at: "2026-05-02T01:00:00.000Z"
must_haves:
  truths:
    - "Users invoke **`av-denoiser clean <input>`** with **`--preset`**, **`--noise-strength`**, **`--dry-run`**, **`--json`**, **`--output`**, **`--force`** mirroring **`inspect`** ergonomics (**D-01**, PIPE-03)."
    - "**`--dry-run`** exits success **0** printing ordered steps + warnings before tools spawn (**PIPE-02**)."
    - "**JSON mode** emits deterministic structured summary keys **`presetId`**, **`steps`**, **`warnings`** when **`--json`** combined with **`--dry-run`** (**Claude discretion**, parity recommendation)."
    - "Default preset literal **`speech-light`** applies when **`--preset` omitted (**DEFAULT_CLEAN_PRESET_ID**)."
  artifacts:
    - path: src/cli/command.ts
      provides: Commander `clean` subcommand wiring
    - path: src/domain/cli-request.ts
      provides: `{ kind: \"clean\", ... }` variant
    - path: src/cli/render.ts
      provides: render branch for clean success/failure text+json
---

<objective>
Wire **`clean`** through **`CliRequest`**, Commander definitions, **`runCliRequest`** routing, and **`render`** output paths—including **`--dry-run`** UX—without altering **`inspect`** no-execute invariant (**D-01**).

Purpose: Makes MEDIA-01 user-visible via CLI shell consistent with Phase 1 exit taxonomy.

Output: typed CLI branch + renderer updates + **`command.test.ts`** assertions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/domain/cli-request.ts
@src/cli/command.ts
@src/app/run-command.ts
@src/cli/render.ts
@src/cli/main.ts
@test/cli/command.test.ts
@src/domain/audio-pipeline-plan.ts (`DEFAULT_CLEAN_PRESET_ID`, `parsePresetId`)
@src/app/clean.ts (`runCleanRequest`)

<interfaces_hint>
Extend **`CliRequest`** union with:

```typescript
{
  readonly kind: "clean";
  readonly inputPath: string;
  readonly maybeOutputPath?: string;
  readonly force: boolean;
  readonly dryRun: boolean;
  readonly json: boolean;
  readonly presetId: PresetId;
  readonly knobs: CleanPresetKnobs;
}
```

Commander **`clean`** definition:

- `.argument("<input>", "...")`
- **`-o, --output <path>`**
- **`--force`** default false
- **`--dry-run`** default false
- **`--json`** default false
- **`--preset <id>`** optional — when absent substitute **`DEFAULT_CLEAN_PRESET_ID`**
- **`--noise-strength <0..1>`** parsed via **`Number.parseFloat`**, validated: if **`Number.isFinite`** false OR outside **[0,1]** ⇒ Commander **`InvalidArgumentError`** OR handler converts to **`invalid-input`** outcome — choose Commander-native **`Option`** coercion with **`argParser`** returning **`number`** throwing on violation.

**`runCliRequest`** case **`clean`**: map fields into **`CleanRunInput`**, await **`runCleanRequest`**, then merge **`CleanCliSuccess`** onto **`CliCommandOutcome`** as **`clean?: CleanCliSuccess`** (mirror **`inspect`** symmetry).

**Renderer:**

- Text dry-run prints headings **`Preset`**, **`Warnings`**, **`Steps`** bullet list showing **`renderDisplayCommand`** strings already embedded in summary.
- JSON mirrors **`inspect`** behavior using **`JSON.stringify(..., null, 2)`** ending newline.

**Guidance:** Update **`DEFAULT_GUIDANCE_LINES`** in **`render.ts`** + analogous note in **`renderHelpGuidance`** (“Heavy transcoding…” sentence) to mention **`clean`** availability for **audio-only** inputs — literal substring **`clean`** required twice across **`render.ts`** (grep-able).

**`renderCliRequest`** switch adds **`clean`** snippet lines.

**Tests (`test/cli/command.test.ts`):**

- Extend **`parseCliRequest`** harness if exists OR replicate **`main.ts`** **`parseCliRequest`** usage verifying **`clean`** yields **`presetId`** defaults **`speech-light`** when omitted.

- Provide assertion **`--preset speech-soft-sox`** parses preset field accordingly.

- **`--noise-strength 2`** yields parse error path (**invalid-input**) OR Commander throws caught into **`parse-error`** pipeline — freeze expected **`ExitCode.invalidInput`** **2** from **`runCli`** wrapper similar to existing tests.

Do **not** add README edits (out of scope unless failing CI — none expected).
</interfaces_hint>
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| CLI argv → numbers | NaN injection | Commander parsers clamp/throw → **`invalid-input`** |
| JSON stdout | accidental stderr leakage | Renderer reads structured summary only |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-04-01 | I | `--preset` strings | mitigate | **`parsePresetId`** rejects unknown IDs → **`invalid-input`** message listing allowed presets alphabetically |
| T-04-04-02 | E | JSON summaries | accept | Contains paths user supplied |

CLI-only phase — local trust model documented.
</threat_model>

<verification_criteria>
- `bun test test/cli/command.test.ts` exits 0.
- `bun run verify` exits 0 when repo script aggregates **`biome check`** + **`tsc --noEmit`** + **`bun test`** — if **`verify`** missing, replace aggregate with **`bun test`** only twice noted in SUMMARY.

Manual smoke optional not required plan.

Automated minimal:

```
bun test test/cli/command.test.ts
```

Additionally **`rg -n 'kind: \"clean\"' src/domain/cli-request.ts`**

</verification_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extend CliRequest + Commander clean command</name>
  <files>src/domain/cli-request.ts, src/cli/command.ts, test/cli/command.test.ts</files>
  <read_first>
    - src/domain/cli-request.ts
    - src/cli/command.ts
    - test/cli/command.test.ts
    - src/domain/audio-pipeline-plan.ts
    - src/cli/main.ts (`parseCliRequest` wiring)
  </read_first>
  <action>
    Add **`clean`** Commander registration mirroring **`inspect`** style callbacks populating **`CliRequest`** variant.

    Implement **`noise-strength`** validation explicit:

    - Accept fractional syntax **`0.35`** etc.

    - Reject non-finite / out-of-range before **`handleRequest`**.

    Map preset strings via **`parsePresetId`** — when **`null`**, still **`handleRequest`**? Prefer prevent by Commander **`choices`** restricted to **`speech-light`**, **`speech-soft-sox`** OR manual validation throwing Error surfaced as parse-error — freeze behavior in tests.

    Ensure **`DEFAULT_CLEAN_PRESET_ID`** used when **`--preset`** omitted.

    Update tests accordingly with minimal additional cases (≤ **4** new assertions blocks).

    Maintain **`program.exitOverride`** compatibility exactly as **`inspect`** expects from **`main.ts`** harness.

    Export nothing unnecessary publicly from **`cli/command.ts`** beyond existing **`createCommandProgram`**.
  </action>
  <verify>
    <automated>bun test test/cli/command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\\bclean\\b" src/cli/command.ts | head`
    - Tests demonstrate default preset: **`rg -n 'speech-light' test/cli/command.test.ts`**
    - Invalid noise: `rg -n 'noise-strength' test/cli/command.test.ts`**
  </acceptance_criteria>
  <done>CLI parses clean requests deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Route clean through run-command + render + guidance copy</name>
  <files>src/app/run-command.ts, src/cli/render.ts, src/cli/main.ts</files>
  <read_first>
    - src/app/run-command.ts
    - src/cli/render.ts
    - src/cli/main.ts
    - src/app/clean.ts
    - src/app/inspect.ts (typing symmetry)
  </read_first>
  <action>
    Extend **`CliRequestDeps`** (**`run-command.ts`**) with **`readonly clean?: Partial<CleanDeps>`** importing **`CleanDeps`** type from **`clean.ts`**.

    Extend **`CliCommandOutcome`** union intersection pattern with optional **`clean?: CleanCliSuccess`** struct exported from **`clean.ts`** (import type).

    Implement **`runCliRequest`** branch **`clean`** by constructing **`CleanRunInput`** literals from the **`CliRequest`** fields (**pick/copy identical names**) then delegating **`runCleanRequest(cleanInput, deps.clean)`** with **`Partial<CleanDeps>`** injection analogous **`inspect`** hook.

    Renderer updates:

    - **`renderCommandOutcome`** handles **`request.kind === "clean"`** success JSON/text analogous **`inspect`** conditional.

    - Failures reuse **`appendFailureDetails`** wrapper consistent messaging prefix **`av-denoiser clean failed.`**

    Guidance strings (`DEFAULT_GUIDANCE_LINES`, **`renderHelpGuidance`** phase note, **`renderCliRequest`** **`clean`** case) mention **`clean`** capability (**≥ 2** occurrences **`clean`** literal across **`render.ts`** summary-level grep).

    **`runCli`** unaffected aside from typings if **`RenderableOutcome`** exported union widened accordingly.

    Avoid importing **`PresetId`** into **`render.ts`** unnecessarily — consume summary strings only.

    Ensure **`CleanCliSuccess`** includes whatever **`render`** needs (**boolean flags **`json`**, **`dryRun`**, **`summary`**) mirroring **`InspectCliSuccess`** shapes.

    Keep **`doctor`** precedence sections untouched aside from typing widen.

    Run **`bun test`** whole suite if fast (<10s locally); otherwise scoped **`command.test`** + **`clean.test`** — executor chooses **`bun test`** full if CI parity needed.

    Document commands for SUMMARY:

    ```
    bun test test/cli/command.test.ts
    bun test test/app/clean.test.ts
    ```
  </action>
  <verify>
    <automated>bun test test/cli/command.test.ts && bun test test/app/clean.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'case \"clean\"' src/app/run-command.ts`
    - `rg -n 'clean failed' src/cli/render.ts`
    - `rg -n 'clean' src/cli/render.ts | wc -l` reports **≥ 5** lines (shell acceptance noted in SUMMARY evidence).
  </acceptance_criteria>
  <done>End-to-end typed CLI shell exposes clean summaries + failures.</done>
</task>

</tasks>

<success_criteria>
- **`av-denoiser clean`** surfaces dry-run planning without ffmpeg invocation (`dryRun` path mock-tested upstream).
- **`CliRequest` exhaustive switch** maintained (`runCliRequest`, **`renderCliRequest`**).

</success_criteria>

<output>
After completion, create `.planning/phases/04-core-audio-pipeline-sox-cleanup/04-04-SUMMARY.md`
</output>
