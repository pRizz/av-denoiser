---
phase: 04-core-audio-pipeline-sox-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/audio-pipeline-plan.ts
  - test/domain/audio-pipeline-plan.test.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-05
  - PIPE-06
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 04-2026-05-02T00-13-28
generated_at: "2026-05-02T01:00:00.000Z"
must_haves:
  truths:
    - "Typed preset IDs (`speech-light`, `speech-soft-sox`) expand only into known FFmpeg/SoX step kinds—no raw user filtergraphs (PIPE-01, D-04)."
    - "`expandPreset` returns ordered logical steps plus `pipelineWarnings[]` with stable string IDs suitable for `--dry-run` / JSON (PIPE-02, PIPE-06, D-05, D-12)."
    - "Bounded knobs (noise strength clamped to [0,1]) change expanded FFmpeg afftdn parameters without accepting arbitrary strings (PIPE-03, D-06)."
    - "Intermediate contract between steps is WAV PCM s16le semantics documented on extract step metadata (PIPE-05, D-08)."
  artifacts:
    - path: src/domain/audio-pipeline-plan.ts
      provides: PresetId registry + expandPreset + warnings + knobs types
      exports:
        - PresetId
        - PipelineWarning
        - CleanPresetKnobs
        - ExpandedLogicalPipeline
        - expandPreset
    - path: test/domain/audio-pipeline-plan.test.ts
      provides: deterministic expansion + warning assertions
---

<objective>
Implement the **functional-core preset model** for Phase 4: discriminated **`PresetId` literals**, **`expandPreset`** into ordered **`LogicalPipelineStep`** values, bounded **`CleanPresetKnobs`**, and **`pipelineWarnings[]`** with stable IDs.

Purpose: Locks PIPE transparency and risk signaling before argv builders or spawning tools.

Output: `src/domain/audio-pipeline-plan.ts` + focused unit tests (no FFmpeg/SoX execution).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-audio-pipeline-sox-cleanup/04-CONTEXT.md
@.planning/phases/04-core-audio-pipeline-sox-cleanup/04-RESEARCH.md
@.planning/REQUIREMENTS.md
@src/domain/output-plan.ts

<interfaces_hint>
Reuse **`PlannedAudioCodec`** + **`PlannedContainer`** from `output-plan.ts` as inputs to expansion so encode metadata matches **`planMediaOutput`** (D-09).

Freeze **`PresetId`** as exactly:

```typescript
export type PresetId = "speech-light" | "speech-soft-sox";
```

**`speech-light`:** FFmpeg-only recipe for CI-friendly paths — logical steps:

1. `{ tool: "ffmpeg", step: { kind: "extract-pcm-wav" } }`
2. `{ tool: "ffmpeg", step: { kind: "afftdn", noiseStrength: number /* copied from knobs after clamp */ } }`
3. `{ tool: "ffmpeg", step: { kind: "encode-deliverable", audioCodec: PlannedAudioCodec, container: PlannedContainer } }`

**`speech-soft-sox`:** Same as **`speech-light`** but inserts **after** afftdn and **before** encode:

```typescript
{ tool: "sox", step: { kind: "gentle-dynamics" } }
```

Declare **`requiresSox: true`** on this preset’s registry metadata (exported helper **`presetRequiresSox(presetId)`** returning boolean).

Warnings (**minimum** IDs present in expansions):

- Both presets emit **`warn-heavy-cpu-ffmpeg-afftdn`** (title contains “CPU” or “heavy” — exact strings asserted in tests).
- **`speech-soft-sox`** additionally emits **`warn-sox-dynamics-artifact-risk`**.
</interfaces_hint>
</context>

<threat_model>
## Trust boundaries (CLI local-trust model)

| Boundary | Risk | Disposition |
|----------|------|---------------|
| Preset/knob strings | Injection into future argv if unchecked | mitigate |
| Expansion output consumed by builders | Logic bugs selecting wrong audio map | mitigate |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-01-01 | I | `expandPreset` inputs | mitigate | Validate knobs only via bounded numeric clamp + closed `PresetId` union at CLI boundary (Zod plan 04); domain trusts typed `PresetId` |
| T-04-01-02 | E | Warning text | accept | Static catalog strings only; no stderr |

No filesystem or subprocess exposure in this plan.
</threat_model>

<verification_criteria>
- `bun test test/domain/audio-pipeline-plan.test.ts` exits 0.
- `rg -n 'type PresetId' src/domain/audio-pipeline-plan.ts` shows both literals `"speech-light"` and `"speech-soft-sox"`.
- `rg -n 'warn-heavy-cpu-ffmpeg-afftdn' test/domain/audio-pipeline-plan.test.ts src/domain/audio-pipeline-plan.ts`.
</verification_criteria>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for preset expansion + warnings</name>
  <files>test/domain/audio-pipeline-plan.test.ts</files>
  <read_first>
    - src/domain/output-plan.ts
    - test/domain/output-plan.test.ts
    - .planning/phases/04-core-audio-pipeline-sox-cleanup/04-CONTEXT.md (D-04 .. D-12)
    - test/domain/audio-pipeline-plan.test.ts (create)
  </read_first>
  <behavior>
    - `expandPreset({ presetId: "speech-light", knobs: { noiseStrength: 0.5 }, plannedAudioCodec: "aac", plannedContainer: "mp4" })` returns **3** FFmpeg logical steps ending with `encode-deliverable` carrying **`aac`/`mp4`** exactly.
    - `presetRequiresSox("speech-light") === false`; `presetRequiresSox("speech-soft-sox") === true`.
    - Warning IDs include **`warn-heavy-cpu-ffmpeg-afftdn`** for both presets; **`speech-soft-sox`** also includes **`warn-sox-dynamics-artifact-risk`**.
    - Clamping: `noiseStrength` **1.5** clamps so afftdn step receives **`1`** (assert numeric equality on expanded step field).
  </behavior>
  <action>
    Create **`test/domain/audio-pipeline-plan.test.ts`** importing **`expandPreset`** / **`presetRequiresSox`** from **`src/domain/audio-pipeline-plan.ts`** (module missing ⇒ RED). Use **`describe`/`test`** patterns consistent with **`test/domain/output-plan.test.ts`**.

    Export **`DEFAULT_CLEAN_PRESET_ID`**: literal **`"speech-light"`** constant must be asserted equal in one test (executor will wire CLI default).
  </action>
  <verify>
    <automated>bun test test/domain/audio-pipeline-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - RED verified: **`bun test test/domain/audio-pipeline-plan.test.ts`** fails until Task 2 lands.
    - File contains substring **`warn-heavy-cpu-ffmpeg-afftdn`** at least twice (speech-light + speech-soft-sox branches).
  </acceptance_criteria>
  <done>Tests encode expected ordering, Sox requirement flags, warnings, and clamp semantics.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement audio pipeline plan core</name>
  <files>src/domain/audio-pipeline-plan.ts</files>
  <read_first>
    - src/domain/audio-pipeline-plan.ts
    - test/domain/audio-pipeline-plan.test.ts
    - src/domain/output-plan.ts (`PlannedAudioCodec`, `PlannedContainer`)
    - .planning/phases/04-core-audio-pipeline-sox-cleanup/04-RESEARCH.md (PCM/WAV note)
  </read_first>
  <action>
    Implement **`src/domain/audio-pipeline-plan.ts`** exporting:

    - **`export type PresetId = "speech-light" | "speech-soft-sox"`**
    - **`export const DEFAULT_CLEAN_PRESET_ID: PresetId = "speech-light"`**
    - **`export type CleanPresetKnobs = { readonly noiseStrength: number }`**
    - **`export type PipelineWarning = { readonly id: string; readonly title: string; readonly detail?: string }`**
    - **`LogicalPipelineStep`** discriminated union covering FFmpeg substeps **`extract-pcm-wav`**, **`afftdn`**, **`encode-deliverable`** and SoX **`gentle-dynamics`** (`detail` documents PCM WAV interchange).

    **`expandPreset(input)`** returns **`{ readonly steps: readonly LogicalPipelineStep[]; readonly warnings: readonly PipelineWarning[] }`**:

    - Clamp **`noiseStrength`** to **`[0, 1]`** inclusive before embedding on **`afftdn`** steps.

    **`presetRequiresSox(id)`**: **`true`** iff **`id === "speech-soft-sox"`**.

    Attach warning catalog entries (human titles ok; IDs fixed literals above).

    Add **`parsePresetId(raw: string)`** returning **`PresetId | null`** without throwing (pure predicate helper for CLI layer).

    Do **not** import adapters or **`node:fs`** — domain only per D-13 prelude.
  </action>
  <verify>
    <automated>bun test test/domain/audio-pipeline-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test test/domain/audio-pipeline-plan.test.ts` exits **0**.
    - `rg -n "gentle-dynamics" src/domain/audio-pipeline-plan.ts`
    - `rg -n "parsePresetId" src/domain/audio-pipeline-plan.ts`
  </acceptance_criteria>
  <done>GREEN: pure preset expansion module compiles under strict TS and satisfies tests.</done>
</task>

</tasks>

<success_criteria>
- Plan 01 completes when **`expandPreset`** + **`presetRequiresSox`** + **`parsePresetId`** exist, tests pass, and warning/step ordering matches CONTEXT **`speech-light` / `speech-soft-sox`** semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-audio-pipeline-sox-cleanup/04-01-SUMMARY.md`
</output>
