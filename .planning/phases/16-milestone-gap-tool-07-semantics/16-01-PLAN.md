---
phase: 16-milestone-gap-tool-07-semantics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/08-optional-heavy-editor-integrations/08-VERIFICATION.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - TOOL-07
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 16-2026-05-03T15-05-00Z
generated_at: "2026-05-03T15:05:00.000Z"
must_haves:
  truths:
    - "**TOOL-07** checklist is **`[x]`** with narrowed prose declaring FFmpeg **`ladspa`** as the runnable Kdenlive/MLT-derived path and **doctor-only `melt` visibility**."
    - "`08-VERIFICATION.md` success row **5** + **TOOL-07** REQ row quote the same semantics (**no undocumented `melt` pipeline step`)."
    - "ROADMAP Phase **8** success criterion **#5**, Phase **16** completion checkbox, Plans row, Overview bullet, Progress table, Requirement Coverage **`TOOL-07`** Notes align with narrowed REQ."
    - "`v1.0-MILESTONE-AUDIT.md` documents Phase **16** resolution of **`gaps.integration`/TOOL-07** ambiguity (markdown body append)."
    - "`bun run verify` exits **0** (documentation-only)."
---

<objective>

Close **`gaps.integration`** / REQ semantics (**TOOL-07**) by **`narrowed-requirement`** alignment (**not** a new runnable **`melt` bridge**):

- Executable path remains **FFmpeg `ladspa`** + **doctor **`melt`** probe** (`src/domain/audio-pipeline-argv.ts`, `src/adapters/tool-discovery.ts`, …).
- **Human requirements + verification prose** MUST stop implying **`melt` cleanup execution** unless/until a separate phase introduces it.

Alternative (**opt-in runnable `melt`/FFmpeg bridge**) is explicitly **out of scope** for this plan —requires replan/new phase if pursued.

</objective>

<threat_model>
**Surfaces:** Planning markdown edits only (**no `src/`** changes in default path).

| Threat | Severity | Mitigation |
|--------|-----------|------------|
| Mis-declaring REQ satisfied while behavior diverges | Medium | Acceptance criteria **`grep`** exact substrings mirrored in **REQUIREMENTS**, **08-VERIFICATION**, **ROADMAP** Phase **8 #5**. |
| Stale audit readers believe integration gap open | Low | Mandatory append block in **`v1.0-MILESTONE-AUDIT.md`** citing Phase **16** closure wording. |

No secrets, inbound network scope, or new subprocess beyond **`bun run verify`** verification task.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-milestone-gap-tool-07-semantics/16-RESEARCH.md
@.planning/phases/08-optional-heavy-editor-integrations/08-VERIFICATION.md
@.planning/phases/08-optional-heavy-editor-integrations/08-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@docs/doctor.md
</context>

<tasks>

<task type="manual">
  <name>Rewrite TOOL-07 checklist item + traceability row</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (sections **`## v1 Requirements`**, **`## Traceability`**)
    - `.planning/phases/16-milestone-gap-tool-07-semantics/16-RESEARCH.md`
  </read_first>
  <action>
    1. In **`## v1 Requirements` → Verification and Trust**, replace the entire **`TOOL-07`** bullet line **exactly** with:
       `- [x] **TOOL-07**: User can run **Kdenlive/MLT–derived** cleanup through a **headless FFmpeg ladspa filter step** using `--ladspa-plugin-path`, `--ladspa-label`, and optional `--ladspa-controls` when FFmpeg exposes ladspa (doctor confirms capability). Doctor reports ladspa readiness and optionally surfaces melt probe output (`melt -version`) for MLT/Kdenlive ecosystem parity without orchestrating melt as a cleanup step. **TOOL-08** applies when prerequisites are insufficient or unavailable.`

    2. In **`## Traceability`** (3-column **`| Requirement | Phase | Status |`** table), replace **`TOOL-07`** row with **`| TOOL-07 | Phase 16 | Complete |`** verbatim.

    3. Replace **Traceability** intro paragraph (currently referencing **TOOL-07** awaiting Phase **16**) with ≤**2** sentences stating **Phase **16** closed** narrowed **TOOL-07** checklist + trace (**no** “Pending” / “awaits” wording).

    4. Set *Last updated* line ISO date (**YYYY-MM-DD**) to executor run date documenting Phase **16** closure.
  </action>
  <acceptance_criteria>
    - `grep -nF '**TOOL-07**:' .planning/REQUIREMENTS.md` prints a line containing **`- [x]`** substring before **TOOL-07**.
    - Same **`TOOL-07`** checklist line substring **`without orchestrating melt`** appears (case-sensitive).
    - Table row substring **`| TOOL-07 | Phase 16 | Complete |`** appears in REQUIREMENTS traceability pipe table.
    - `grep -E '^- \[.\].*\*\*TOOL-07\*\*' .planning/REQUIREMENTS.md | grep -q '^- \[x\]'` exits **0**.
  </acceptance_criteria>
  <verify>
    <automated>
bash -eu -o pipefail -c 'cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && REQ=.planning/REQUIREMENTS.md && grep -Fq "| TOOL-07 | Phase 16 | Complete |" "$REQ" && REQ_LINE=$(grep -F "**TOOL-07**:" "$REQ" | head -1) && printf "%s" "$REQ_LINE" | grep -q "^- \[x\]" && printf "%s" "$REQ_LINE" | grep -q "without orchestrating melt" && if awk "NR>=114 && NR<=125" "$REQ" | grep -qi awaits; then echo "still contains awaits in trace intro" >&2; exit 1; fi'
    </automated>
  </verify>
</task>

<task type="manual">
  <name>Align 08-VERIFICATION success criterion #5 + TOOL-07 row</name>
  <files>.planning/phases/08-optional-heavy-editor-integrations/08-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/08-optional-heavy-editor-integrations/08-VERIFICATION.md`
    - `.planning/REQUIREMENTS.md` (**TOOL-07** finalized line — after prior task merges)
    - `.planning/phases/08-optional-heavy-editor-integrations/08-CONTEXT.md`
  </read_first>
  <action>
    1. In **`## Goal achievement` → Roadmap success criteria** table Row **`|` 5 `|`**:

       Replace ONLY the prose in the middle column (**Success criterion** text) — keep **`| 5 |`** prefix — so it **paraphrases** the narrowed **TOOL-07** meaning; **mandatory verbatim substrings** in that cell:

       `FFmpeg ladspa`
       **`melt`** (backticks markdown code span)
       substring **`doctor`**
       substring **`without orchestrating`**

       (Remove any claim that **`melt` itself** performs cleanup in the sequential pipeline.)

    2. In **Requirements coverage** table row **`|` TOOL-07 `|`** status column (**✓ SATISFIED retained**), revise evidence description so it cites **runnable FFmpeg `ladspa` argv** (**`ladspa-apply`**, **`buildLogicalStepCommand`**) **and** **`melt` discovery**/doctor (**not melt execution**).

    3. **Frontmatter `verified` ISO timestamp**: refresh to Phase **16** executor wall-clock (**replace old value** entirely).

    4. Optionally append single sentence beneath **Residual / Gaps summary** referencing **TOOL-07** narrowed semantics closed **Phase **16**** (≤**2** sentences) if executor judges prior **CI vs real machine** caveat still suffices without contradiction.
  </action>
  <acceptance_criteria>
    - Row **`| 5 |`** exists in Goal achievement markdown table (`grep -n '| 5 |' FILE | head -1` non-empty **stdout** against `08-VERIFICATION.md`).
    - Goal row substring **`FFmpeg ladspa`** present in same doc.
    - Goal row substring **`` `melt` ``** markdown code-span pattern present (**grep** `\`melt\`` on file exits **0`).
    - Substring **`without orchestrating`** present in Goal achievement section (between `## Goal achievement` and next `###` heading).
    - **`| TOOL-07 |`** row still **`✓ SATISFIED`** literal occurrence.
    - YAML frontmatter key **`verified:`** line updated from pre-executor value (manual eyeball acceptable if automated diff unavailable — otherwise `rg '^verified:'` shows **≠** **`2026-05-02T12:25:00.000Z`** if that was stale).
  </acceptance_criteria>
  <verify>
    <automated>
bash -eu -o pipefail -c 'cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && V=.planning/phases/08-optional-heavy-editor-integrations/08-VERIFICATION.md && awk "/## Goal achievement/,/^### Requirements coverage/" "$V" | grep -Fq "| 5 |" && awk "/## Goal achievement/,/^### Requirements coverage/" "$V" | grep -Fq "FFmpeg ladspa" && awk "/## Goal achievement/,/^### Requirements coverage/" "$V" | grep -q "\`melt\`" && awk "/## Goal achievement/,/^### Requirements coverage/" "$V" | grep -Fq "without orchestrating" && awk "/## Goal achievement/,/^### Requirements coverage/" "$V" | grep -Fq "doctor" && grep -Fq "✓ SATISFIED" "$V" && grep -Fq "**TOOL-07**" "$V"'
    </automated>
  </verify>
</task>

<task type="manual">
  <name>Sync ROADMAP Phase 8 success #5 + Phase 16 closure</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` (Phase **8** Success Criteria block, Phase **16** overview + details + **Progress** + **Requirement Coverage**)
    - `.planning/REQUIREMENTS.md` (**TOOL-07** finalized line after task 1)
  </read_first>
  <action>
    1. Under **`### Phase 8` → Success Criteria**, replace numbered item **5** prose so its meaning matches narrowed **TOOL-07** (**FFmpeg ladspa runnable path**; **`melt`** diagnostic visibility **without orchestrated melt cleanup**). One paragraph ≤**3** sentences acceptable.

    2. **`## Overview`**: checkbox **`- [ ]` Phase 16** → **`- [x]`**, append **`(completed YYYY-MM-DD)`** executor date.

    3. **`### Phase 16` details**: **`Plans`** line — mark **`16-01-PLAN.md`** checklist **`- [x]`** replacing **`- [ ]`**.

    4. **`## Progress`** table row **`| 16. …`**:** **Plans Complete** **`1/1`**, **Status `Complete`**, **Completed column** executor **YYYY-MM-DD**.

    5. **`## Requirement Coverage`**: **`| TOOL-07 |`** row **Notes** cell — replace with substantive note: **`TOOL-07` narrowed REQ + `08-VERIFICATION` aligned Phase **16**** (implementation remains Phase **8** `ladspa`/doctor).

    6. **Coverage** rollup sentence (if stale): **42**/42 checklist rows complete after Phase **16** (drop “41 + pending” phrasing if present).
  </action>
  <acceptance_criteria>
    - `awk '/^### Phase 8/,/^### Phase 9/' .planning/ROADMAP.md | grep -q '^  5\\.'`
    - Phase **16** **`16-01-PLAN.md`** roadmap plans line prefixed **`- [x]`**.
    - Progress table pipe row for **`| 16.`** contains **`Complete`** substring and **`2026`** year in completed column (**grep** heuristic).
    - Requirement Coverage **`| TOOL-07 |`** line contains substring **`Phase 16`** and **`narrowed`** (case-insensitive) OR substring **`narrowed REQ`** verbatim.
  </acceptance_criteria>
  <verify>
    <automated>
bash -eu -o pipefail -c 'cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && R=.planning/ROADMAP.md && awk "/^### Phase 8/,/^### Phase 9/" "$R" | grep -Eq "^  5\\.|^\\s{0,4}5\\." && grep -Fq -- "- [x] **Phase 16:" "$R" && grep "16-01-PLAN.md" "$R" | grep -q "^- \\[x\\]" && grep -E "^\\| 16\\. " "$R" | grep -Fq "Complete" && grep -F "**TOOL-07** narrowed REQ" "$R"'
    </automated>
  </verify>
</task>

<task type="manual">
  <name>Milestone audit follow-up appendix</name>
  <files>.planning/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>
    - `.planning/v1.0-MILESTONE-AUDIT.md` (**Recommended next steps**, **Residual** section)
    - `.planning/phases/16-milestone-gap-tool-07-semantics/16-RESEARCH.md`
  </read_first>
  <action>
    Append BEFORE final horizontal rule / report footer (**or** insert immediately after **`## Recommended next steps`**) markdown subsection:

    **`## Phase 16 resolution (TOOL-07 / gaps.integration)`**

    2–4 sentences stating: **semantic alignment** adopted = **narrowed REQ** (**FFmpeg `ladspa`** as runnable integration; **`melt` doctor-only); ** **`gaps.integration`** closed by documentation (**no new code** assumption); date **executor YYYY-MM-DD**.

    Optionally strikethrough bullet **2.** under **Recommended next steps** referencing **TOOL-07 decision** (`~~...~~`).
  </action>
  <acceptance_criteria>
    - `grep -Fq "## Phase 16 resolution" .planning/v1.0-MILESTONE-AUDIT.md`
    - Substring **`gaps.integration`** OR **`narrowed`** appears inside that appendix heading’s following **30** lines (`awk '/## Phase 16 resolution/,/^## /'` window).
    - **`bun run verify`** unaffected (audit file not part of biome ignore conflicts — if fails, relocate appendix placement).
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Repository verify gate</name>
  <files></files>
  <read_first>
    - `package.json` scripts section (**verify** target)
  </read_first>
  <action>
    From repo root: **`bun run verify`** — exit code **0**.

    No **`src/`** or **`test/`** edits anticipated for narrowed-doc path.

    After success optionally run:

    **`node \"$HOME/.claude/get-shit-done/bin/gsd-tools.cjs\" state complete-phase --phase 16`** (only if milestone workflow dictates — ELSE skip; standard post-execution uses **`complete-phase`** from execute workflow, not planner).
  </action>
  <acceptance_criteria>
    - `bun run verify` exits **0** from repo root.
  </acceptance_criteria>
  <verify>
    <automated>bun run verify</automated>
  </verify>
</task>

</tasks>

## PLANNING COMPLETE
