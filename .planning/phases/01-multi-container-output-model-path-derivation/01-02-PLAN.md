---
phase: 01-multi-container-output-model-path-derivation
plan: 02
type: execute
wave: 1
depends_on:
  - "01"
files_modified:
  - src/app/inspect.ts
  - src/app/clean.ts
  - src/app/batch.ts
  - src/domain/batch-output-path.ts
  - test/domain/batch-output-path.test.ts
  - test/app/inspect.test.ts
  - src/domain/video-clean-argv.ts
autonomous: true
requirements_addressed:
  - MULTI-01
  - MULTI-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 01-2026-05-03T15-39-17
generated_at: "2026-05-03T16:05:00.000Z"
must_haves:
  truths:
    - "`runInspectRequest` / `runCleanRequest` derive implicit extension from probe + prelude before resolving default output paths when `maybeOutputPath` is unset."
    - "Batch allocates collision-safe stems using implicit extensions consistent with per-input probe prelude (MULTI-02)."
---

<objective>
Wire **inspect**, **clean**, and **batch** to the Phase **01** domain hooks: reorder **implicit default output** derivation so **`resolveOutputPath`** receives **`implicitOutputExtWithDot`** from **`implicitDefaultOutputExtWithDot`**. Extend **`allocateBatchOutputPaths`** with **`getImplicitExtWithDot`**; **`runBatchRequest`** probes each expanded input once before allocation. **`plannedContainer` literals `matroska` / `webm`** exercised in domain tests only in plan **01-01**; apps treat real media as today (**`mp4`**).
Output: **`bun run verify`** green; **`…avdn.mp4`** for default **MOV** video cleanup (CONTEXT).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/inspect.ts
@src/app/clean.ts
@src/app/batch.ts
@src/domain/batch-output-path.ts
@src/domain/output-path.ts
@src/domain/output-plan.ts
@test/domain/batch-output-path.test.ts
@test/app/inspect.test.ts
</context>

<tasks>

<task type="auto">
  <name>T1 — inspect + clean ordering</name>
  <files>src/app/inspect.ts, src/app/clean.ts, test/app/inspect.test.ts</files>
  <read_first>
    - src/app/inspect.ts
    - src/app/clean.ts
    - src/domain/output-plan.ts
    - src/domain/output-path.ts
    - .planning/phases/01-multi-container-output-model-path-derivation/01-CONTEXT.md
    - test/app/inspect.test.ts
  </read_first>
  <action>
    After **`runFfprobeProbe`**, compute **`planMediaOutputPrelude`** BEFORE **`resolveOutputPath`**.

    - If prelude **`kind === unsupported`**: preserve current semantics (fail paths that do **not** need a misleading default extension).

    For supported modalities:
    1. Build **`implicitOutputExtWithDot`** via **`implicitDefaultOutputExtWithDot`** (`resolvedInputPath`, modality, **`plannedContainer`**).
    2. Call **`resolveOutputPath`** with **`implicitOutputExtWithDot`** only when **`maybeExplicitOutput`** is unset (**`inspect`/`clean`**).

    Re-merge prelude + **`pathOutcome`** into **`OutputPlan`** via **`planMediaOutput`** refactor or **`attachPathsToPlan`** helper—preserve **`OutputPlan` discriminated unions**.

    Update tests: default **`…avdn.mp4`** for **`.mov` + video`** when **`plannedContainer`** is **`mp4`** (replace expectations that mirrored input **`.mov`** suffix).
  </action>
  <verify>
    <automated>bun test test/app/inspect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "implicitOutputExtWithDot|implicitDefaultOutputExtWithDot|planMediaOutputPrelude" src/app/inspect.ts src/app/clean.ts`
    - `bun test test/app/inspect.test.ts` exits **0**.
  </acceptance_criteria>
  <done>inspect/clean derive implicit `.avdn` extension before path resolution.</done>
</task>

<task type="auto">
  <name>T2 — batch allocator + probes</name>
  <files>src/app/batch.ts, src/domain/batch-output-path.ts, test/domain/batch-output-path.test.ts</files>
  <read_first>
    - src/app/batch.ts
    - src/domain/batch-output-path.ts
    - src/domain/output-plan.ts
    - test/domain/batch-output-path.test.ts
    - src/adapters/ffprobe.ts
  </read_first>
  <action>
    1. Add **`getImplicitExtWithDot?: (resolvedInputPath: string) => string`** to **`AllocateBatchOutputPathsInput`**. When omitted, identical to current behavior (**input extension**).

    When set, build output basename as **`${stem}.${seg}${ext}`** where **`${ext}`** is callback result (must include **`"."`**; document invariant).

    2. **`runBatchRequest`**: after **`expandBatchInputs`**, for each **`rawInput`** resolve **`ffprobe`** + **`planMediaOutputPrelude`**. On prelude **unsupported**, fail batch entry with **`planning-failure`** message materially consistent with **`clean`** (reuse **`describeFfprobeFailure` / invalid-input`** as applicable—executor picks exact branch).

    3. Build **`Map`** from **`canonicalAbsolutePath(cwd, rawInput)` → ext string** passed into **`allocateBatchOutputPaths`**.

    4. Extend **`batch-output-path` tests**: **`getImplicitExtWithDot` always returns `"." + "mp4"`** ⇒ **`mov` → `.avdn.mp4`**.

    Note **N probes** cost in **`01-02-SUMMARY.md`**.
  </action>
  <verify>
    <automated>bun test test/domain/batch-output-path.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "getImplicitExtWithDot" src/domain/batch-output-path.ts`
    - `rg -n "planMediaOutputPrelude|getImplicitExtWithDot" src/app/batch.ts`
    - `bun test test/domain/batch-output-path.test.ts` exits **0**.
  </acceptance_criteria>
  <done>batch default stems use prelude-derived extensions.</done>
</task>

<task type="auto">
  <name>T3 — Exhaustive PlannedContainer touches</name>
  <files>src/domain/video-clean-argv.ts, src/domain/audio-pipeline-argv.ts</files>
  <read_first>
    - src/domain/video-clean-argv.ts
    - src/domain/audio-pipeline-argv.ts
    - src/domain/output-plan.ts
  </read_first>
  <action>
    Satisfy **`tsc`** exhaustiveness for **`plannedContainer`** **`webm`**: **`audio-pipeline-argv`** branch must handle **`webm`** as **temporary `fallback-required` / unreachable** shim (document **MULTI Phase 03**)—**no FFmpeg `-f webm`** without Phase **03** PLAN.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - **`bun run verify`** exits **0**.
  </acceptance_criteria>
  <done>repo compiles cleanly with widened container union.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | DOS | Batch N successive probes | accept | Mirrors N sequential **`clean`** invocations baseline; parallelism caps unchanged here. |

</threat_model>

<verification>
`bun run verify`
</verification>

<success_criteria>
Integrated CLI + batch honors MULTI implicit extension policy; regressions guarded by **`bun run verify`**.
</success_criteria>

<output>
After completion, create **`.planning/phases/01-multi-container-output-model-path-derivation/01-02-SUMMARY.md`**.
</output>
