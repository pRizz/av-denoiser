---
phase: 01-multi-container-output-model-path-derivation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/output-plan.ts
  - src/domain/output-path.ts
  - src/domain/stream-copy-feasibility.ts
  - test/domain/output-plan.test.ts
  - test/domain/output-path.test.ts
autonomous: true
requirements_addressed:
  - MULTI-01
  - MULTI-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 01-2026-05-03T15-39-17
generated_at: "2026-05-03T16:00:00.000Z"
must_haves:
  truths:
    - "`PlannedContainer` includes literal `webm` alongside mp4/matroska/wav; feasibility still rejects non-mp4 video with the existing reason token semantics."
    - "Implicit default output extension is derived from `plannedContainer` for video-bearing modalities; audio-only preserves the input suffix when planned container is mp4."
    - "`resolveOutputPath` accepts optional implicit extension injection used only when the user did not pass an explicit output path."
  artifacts:
    - path: src/domain/output-plan.ts
      provides: "preview plan fields + plannedContainer literal webm"
    - path: src/domain/output-path.ts
      provides: "defaultOutputPathBesideInput / resolveOutputPath extension hook"
---

<objective>
Implement **MULTI-01** / **MULTI-02** in the **domain layer**: typed **`webm`**, pure **implicit extension** derivation from **`plannedContainer`** and modality (CONTEXT D-01–D-07), refactor **`planMediaOutput`** so path resolution can consume the derived extension, and extend **`resolveOutputPath`** / **`defaultOutputPathBesideInput`** accordingly. **`evaluateStreamCopyFeasibility`** compiles against the widened union without behavior change beyond type coverage.
Output: green targeted tests (`output-plan`, `output-path`) and rationale comment for **future** **`video-copy-<codec>-<container>-v<n>`** naming (CONTEXT D-06) in **`stream-copy-feasibility`** or **`output-plan`**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-multi-container-output-model-path-derivation/01-CONTEXT.md
@.planning/phases/01-multi-container-output-model-path-derivation/01-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/domain/output-plan.ts
@src/domain/output-path.ts
@src/domain/stream-copy-feasibility.ts
</context>

<tasks>

<task type="auto">
  <name>T1 — Planned container + prelude split</name>
  <files>src/domain/output-plan.ts, test/domain/output-plan.test.ts</files>
  <read_first>
    - .planning/phases/01-multi-container-output-model-path-derivation/01-CONTEXT.md (D-01–D-05)
    - src/domain/output-plan.ts
    - src/domain/stream-copy-feasibility.ts
    - test/domain/output-plan.test.ts
  </read_first>
  <action>
    1. Change `export type PlannedContainer` to include **`"webm"`** alongside existing literals.
    2. Extract a pure function **`planMediaOutputPrelude`** (exported or file-private if tests only cover `planMediaOutput`) that takes **`MediaProbe`** and returns either **`{ kind: \"unsupported\"; reasonCodes; ...stub fields }`** OR **`{ kind: \"ok\"; modality`; `plannedAudioCodec`; `plannedContainer`; `reasonCodes` from stream copy or stubs; selected audio stream index }** mirroring today's **`planMediaOutput`** branching **without** reading `pathOutcome`.
    3. Keep **`plannedContainer: \"mp4\"`** for every **real** prelude result that reaches video branches in this phase (CONTEXT D-02); add **`// PHASE 02`** comment where VP9/Theora will flip container.
    4. **`planMediaOutput`** becomes: optional path handling delegated to **`resolveOutputPath`** caller (plan 01-02)—in **this plan** only expose **`planMediaOutputPrelude`** plus a helper **`implicitDefaultOutputExtWithDot`** (exported, same module or `output-path.ts`) implementing:
       - **`audio-only` + `plannedContainer === \"mp4\"`**: **`extname`** of **`resolvedInputPath`** when non-empty, else **`\".m4a\"`** (pick one deterministic fallback documented in docstring).
       - **video (`video-copy-safe` | `fallback-required`)**: **`.mp4`** | **`.mkv`** | **`.webm`** | **`.wav`**` from **`plannedContainer`** map.
       - **`unsupported`**: prelude only—no implicit ext.
    5. Add unit tests asserting **`implicitDefaultOutputExtWithDot`** returns **`.mp4`** for a minimal **video** prelude with **`plannedContainer` forced** via **package-private test hook** OR by calling **`implicitDefaultOutputExtWithDot`** with explicit `(modality, plannedContainer, resolvedInputPath)` signature—**exported function** `(args: ImplicitDefaultExtArgs)` is clearest.

    Acceptance: **Zero** widening of **`evaluateStreamCopyFeasibility`** success paths in this task—still MP4-only for copy-safe video.
  </action>
  <verify>
    <automated>bun test test/domain/output-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '\"webm\"' src/domain/output-plan.ts` proves union includes webm string literal type.
    - `rg -n "planMediaOutputPrelude|implicitDefaultOutputExtWithDot" src/domain/output-plan.ts`
    - `bun test test/domain/output-plan.test.ts` exits 0.
    - At least one test documents **video + mp4 container → `.mp4`** default even when `resolvedInputPath` ends with **`.mov`** (pass path string fixture into helper).
    - At least one test asserts **audio-only** path keeps **`.m4a`** (or preserves input `.wav`) matching CONTEXT.
  </acceptance_criteria>
  <done>Domain prelude + implicit extension helpers exist with tests.</done>
</task>

<task type="auto">
  <name>T2 — output-path hooks</name>
  <files>src/domain/output-path.ts, test/domain/output-path.test.ts</files>
  <read_first>
    - src/domain/output-path.ts
    - test/domain/output-path.test.ts
    - src/domain/output-plan.ts (new exports)
    - .planning/phases/01-multi-container-output-model-path-derivation/01-CONTEXT.md D-03–D-04
  </read_first>
  <action>
    1. Extend **`ResolveOutputPathInput`** with optional **`implicitOutputExtWithDot?: string`** (must start with `"."`; validate with early guard returning failure kind **`invalid_implicit_ext`** document in type OR throw-free `kind: "implicit-ext-invalid"` — prefer **`undefined` = inherit legacy input ext** behavior for backward compat).
       - When **`maybeExplicitOutput`** is **undefined**, **`defaultOutputPathBesideInput`** uses **`implicitOutputExtWithDot`** instead of **`extname`** when provided.
       - When **`implicitOutputExtWithDot` is malformed** (`""`, missing dot): treat as **`undefined`** (preserve legacy) **or** return **`output-invalid-implicit-extension`** failure—pick one branch and test it.
    2. Preserve **`explicit -o`** behavior: **`implicitOutputExtWithDot` ignored when explicit path provided** (CONTEXT D-04).
    3. Add **`defaultOutputPathBesideInput`** overload or opts object second parameter **`{ implicitExtWithDot?: string }`** backwards compatible (`defaultOutputPathBesideInput(p)` unchanged).
    4. Unit tests cover: default `.mov` → with implicit **`.mp4`** yields **`basename` matching `stem.avdn.mp4`**; explicit output path unchanged.

    Do **not** wire **`inspect`**/**`clean`** here (plan **01-02**).
  </action>
  <verify>
    <automated>bun test test/domain/output-path.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "implicitOutputExtWithDot|implicitExtWithDot" src/domain/output-path.ts`
    - `rg -n "avdn\\.mp4" test/domain/output-path.test.ts`
    - `bun test test/domain/output-path.test.ts` exits 0.
  </acceptance_criteria>
  <done>Path derivation hook implements MULTI-02 at domain boundary.</done>
</task>

<task type="auto">
  <name>T3 — Reason-code taxonomy note + feasibility types</name>
  <files>src/domain/stream-copy-feasibility.ts</files>
  <read_first>
    - src/domain/stream-copy-feasibility.ts
    - .planning/phases/01-multi-container-output-model-path-derivation/01-CONTEXT.md D-06–D-07
  </read_first>
  <action>
    - Add concise **comment block** (≤12 lines) above **`evaluateStreamCopyFeasibility`** documenting drafted naming: **`video-copy-<codec>-<container>-v<n>`**, **`video-fallback-*`**, no silent rename of **`video-copy-h264-mp4-v1`** family (MULTI-12).
    - Ensure **`PlannedContainer`** import resolves with **`webm`**; **`plannedContainer !== \"mp4\"`** branch unchanged (same **`reasonCodes` array copy** semantics).
  </action>
  <verify>
    <automated>bun test test/domain/output-plan.test.ts test/domain/output-path.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "video-copy-.*codec.*container|video-copy-" src/domain/stream-copy-feasibility.ts`
    - `bunx tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <done>Taxonomy documented; types compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User paths → filesystem | Resolved paths remain `resolve`/`normalize`; no argv shell join (`TRUST` pattern unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `implicitOutputExtWithDot` injection | mitigate | Restrict to constrained enum-driven strings from planner only; callers must map from `plannedContainer`, not raw user MIME text. |

</threat_model>

<verification>
`bun test test/domain/output-plan.test.ts test/domain/output-path.test.ts` then `bun run verify` must pass **after plan 01-02** merges; minimally after wave 2 if split.
</verification>

<success_criteria>
Domain delivers MULTI-01/MULTI-02 hooks with tests and no feasibility regression for shipped MP4 copy-safe codecs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-container-output-model-path-derivation/01-01-SUMMARY.md`.
</output>
