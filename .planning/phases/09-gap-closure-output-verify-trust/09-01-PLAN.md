---
phase: 09-gap-closure-output-verify-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/clean-output-verify.ts
  - src/app/clean.ts
  - test/domain/clean-output-verify.test.ts
autonomous: true
requirements_addressed:
  - MULTI-10
  - MULTI-13
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 09-2026-05-04T11-21-22
generated_at: "2026-05-04T11:22:54.204Z"
must_haves:
  truths:
    - "**`verifyCleanOutput`** accepts **`plannedModality: \"fallback-required\"`** with **`claimedVideoCopied: false`** and asserts the **output** primary video stream **`codec_name`** canonicalizes to **`\"hevc\"`** via **`canonicalVideoCodecForVerify`** (covers **`h265`**, **`hev1`**, **`hevc`** probe spellings)."
    - "**`CleanVerifyFailureReason`** gains **`video-reencode-codec-mismatch`** (or executor-chosen name documented in **`09-02`** verification table) with **`detail`** mentioning expected **HEVC** / **libx265** posture."
    - "**`bun test`** includes at least two cases: fallback path **output `hevc`** vs input **`h264`** ⇒ **ok**; fallback path output **`h264`** ⇒ **failure** with the new reason."
---

<objective>
Implement **Phase 09 / 09-CONTEXT** **D-02–D-03**: extend post-run verification so **`fallback-required`** video re-encode runs (**`claimedVideoCopied: false`**) assert **output** video codec canonicalizes to **HEVC**, wired from **`finalizeCleanSuccess`**; unit-test **`verifyCleanOutput`**; **`bun run verify`** exits **0**.

</objective>

<threat_model>
**Asset:** operator trust — post-run verification must not falsely **pass** when FFmpeg emitted non-HEVC video after **`reencode-hevc`** planning.
**Threats:** verifier skips branch when modality/future refactor drifts; ambiguous failure messages hide_codec bugs.
**Mitigation:** explicit **`plannedModality` + `claimedVideoCopied`** gate mirroring **`clean.ts`**; grep-verifiable tests and failure **`detail`** substrings.
**Severity:** informational — ASVS L1 ancillary (local CLI integrity).

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-gap-closure-output-verify-trust/09-CONTEXT.md
@.planning/ROADMAP.md
@src/domain/clean-output-verify.ts
@src/app/clean.ts
@test/domain/clean-output-verify.test.ts
@src/domain/stream-copy-feasibility.ts (`canonicalVideoCodecForVerify`)
</context>

<tasks>

<task type="auto">
  <name>T1 — Baseline verify</name>
  <files>package.json</files>
  <read_first>
    - package.json (`scripts.verify`)
    - src/domain/clean-output-verify.ts (full file)
    - src/app/clean.ts (`finalizeCleanSuccess` call site ~1033–1056)
  </read_first>
  <action>
From repo root run **`bun run verify`**. Record **`bun test`** **pass** integer **N** for **`09-02`** verification doc baseline comparison.

If non-zero exit: fix unrelated failures before codec verifier work or document blocker in **`09-01-SUMMARY.md`**.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - **`bun run verify`** exits code **0** before edits.
    - Executor notes baseline **N** for **`09-02`** (integer from **`bun test`** summary line **`N pass`**).
  </acceptance_criteria>
  <done>Green baseline captured.</done>
</task>

<task type="auto">
  <name>T2 — Extend `CleanVerifyFailureReason` + fallback branch</name>
  <files>src/domain/clean-output-verify.ts</files>
  <read_first>
    - src/domain/clean-output-verify.ts
    - src/domain/stream-copy-feasibility.ts (`canonicalVideoCodecForVerify`, **`canonicalMp4CopyVideoCodec`** HEVC aliases)
  </read_first>
  <action>

1. Add **`video-reencode-codec-mismatch`** to **`CleanVerifyFailureReason`** union.

2. After the existing **`video-copy-safe` && `claimedVideoCopied`** block (before final **`return { kind: \"ok\" }`**), add:

   **`if (params.plannedModality === \"fallback-required\" && !params.claimedVideoCopied)`** then:
   - Resolve **`outVideo`** via existing **`firstVideoStream(params.outputProbe)`**.
   - If **`outVideo === null`**: return **`failure`**, **`missing-video-stream`**, detail **`missing-video-stream: expected video stream on output after fallback re-encode`**.
   - Read **`outCodec`** from **`outVideo.codec_name?.trim() ?? \"\"`**.
   - Empty **`outCodec`**: return **`video-reencode-codec-mismatch`** with detail mentioning missing **`codec_name`** on output video.
   - Compute **`canonicalVideoCodecForVerify(outCodec)`** — if **not** **`\"hevc\"`**, return **`video-reencode-codec-mismatch`** with **`detail`** including raw **`outCodec`** and literal substring **`expected canonical hevc`** (exact spelling for **`rg`**).

Do **not** compare output codec to **input** for this branch.

  </action>
  <verify>
    <automated>rg -n "video-reencode-codec-mismatch" src/domain/clean-output-verify.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`rg \"video-reencode-codec-mismatch\" src/domain/clean-output-verify.ts`** matches **≥ 2** lines (union + use site).
    - **`rg \"fallback-required\" src/domain/clean-output-verify.ts`** shows new branch adjacent to copy-safe logic.
  </acceptance_criteria>
  <done>Domain verifier implements HEVC expectation for fallback video.</done>
</task>

<task type="auto">
  <name>T3 — Unit tests for fallback HEVC verification</name>
  <files>test/domain/clean-output-verify.test.ts</files>
  <read_first>
    - test/domain/clean-output-verify.test.ts
    - src/domain/clean-output-verify.ts (post-change)
  </read_first>
  <action>

Add **`describe`** subgroup or tests under **`verifyCleanOutput`**:

1. **`fallback-required re-encode: input h264 output hevc => ok`** — **`plannedModality: \"fallback-required\"`**, **`claimedVideoCopied: false`**, input video **`h264`**, output video **`hevc`**, same duration within tolerance ⇒ **`result.kind === \"ok\"`**.

2. **`fallback-required re-encode: output h264 => video-reencode-codec-mismatch`** — same modality flags, output **`h264`** ⇒ **`failure`**, **`reason === \"video-reencode-codec-mismatch\"`**.

3. **`fallback-required re-encode: output hev1 probe string => ok`** — output **`hev1`** ⇒ **`ok`** (canonical verify path).

Optional fourth: missing output video stream ⇒ **`missing-video-stream`** (if easy with empty streams array — skip if types disallow).

  </action>
  <verify>
    <automated>bun test test/domain/clean-output-verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`bun test test/domain/clean-output-verify.test.ts`** exits **0**.
    - File contains literal substring **`fallback-required re-encode`** in **`test()`** description strings **≥ 2** occurrences.
  </acceptance_criteria>
  <done>Regression coverage for MULTI-10/MULTI-13 verifier path.</done>
</task>

<task type="auto">
  <name>T4 — Full verify + summary stub</name>
  <files>.planning/phases/09-gap-closure-output-verify-trust/09-01-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (pattern if exists)
    - .planning/phases/08-gap-closure-phase-03-remux-pipeline-trust/08-01-SUMMARY.md (example)
  </read_first>
  <action>
Run **`bun run verify`** from repo root until exit **0**.

Write **`09-01-SUMMARY.md`** listing: **`CleanVerifyFailureReason`** extension, branch predicate (**`fallback-required` && !`claimedVideoCopied`**), new tests names, final **`bun test`** pass count **N**.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - **`bun run verify`** exits **0**.
    - **`09-01-SUMMARY.md`** exists and contains **`MULTI-10`** and **`MULTI-13`** substrings.
  </acceptance_criteria>
  <done>Wave 1 closure artifact ready for doc wave.</done>
</task>

</tasks>

## PLANNING COMPLETE
