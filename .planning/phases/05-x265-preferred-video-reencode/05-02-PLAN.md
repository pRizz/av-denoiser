---
phase: 05-x265-preferred-video-reencode
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01-PLAN.md
files_modified:
  - src/domain/inspect-summary.ts
  - src/domain/clean-run-report.ts
  - test/domain/inspect-summary.test.ts
  - test/domain/clean-run-report.test.ts
  - test/domain/clean-output-verify.test.ts
  - test/app/clean.test.ts
  - src/cli/command.ts
  - README.md
requirements_addressed:
  - MULTI-13
generated_by: gsd-plan-phase
lifecycle_mode: yolo
autonomous: true
phase_lifecycle_id: 05-2026-05-03T17-34-41
generated_at: "2026-05-03T17:40:27.171Z"
must_haves:
  truths:
    - "fallback-required preservation notes mention HEVC (libx265) re-encode and slower encodes vs AVC."
    - "clean run report text for video re-encode identifies HEVC/libx265 for operators (not generic re-encoded only if CONTEXT requires explicit encoder naming)."
    - "clean --allow-video-fallback help mentions HEVC re-encode / encode time where help text still fits."
    - "Tests cover verify/canonical path for additional HEVC probe synonym if added, or document why existing h265/hevc tests satisfy MULTI-13 D-14."
---

<objective>
Implement **05-CONTEXT D-10, D-11, D-14** and roadmap success **#2–#4**: operator-visible **HEVC/libx265** truthfulness (**`inspect-summary`**, **`clean-run-report`** text, **`command.ts`** help), **README** performance note, and **`verifyCleanOutput`** / canonical tests as needed — after **05-01** argv swap lands.
</objective>

<threat_model>
**Asset:** user-facing preservation and help text.  
**Threats:** misleading **H.264** implication after **libx265** default.  
**Mitigation:** explicit **HEVC** wording; keep text within **MAX_PRESERVATION_NOTES** truncation rules.  
**Severity:** trust / UX — ASVS informational.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-x265-preferred-video-reencode/05-CONTEXT.md
@src/domain/inspect-summary.ts
@src/domain/clean-run-report.ts
@src/cli/command.ts
</context>

<tasks>

<task type="auto">
  <name>T1 — Inspect preservation notes (fallback-required)</name>
  <files>src/domain/inspect-summary.ts,test/domain/inspect-summary.test.ts</files>
  <read_first>
    - `src/domain/inspect-summary.ts` (**`buildPreservationNotesFromPlan`**, **`fallback-required`** case)
    - `test/domain/inspect-summary.test.ts`
    - `.planning/phases/05-x265-preferred-video-reencode/05-CONTEXT.md` (**D-10**)
  </read_first>
  <action>
    1. In **`case "fallback-required"`**, after the existing matrix/fallback bullets, **`notes.push`** one concise bullet that states:
       - approved execution re-encodes **video** to **HEVC** using **`libx265`** in **MP4** with quality-first settings (**slow** preset, **CRF 28** per **05-CONTEXT**), and
       - encodes are **slower / heavier** than typical **AVC (`libx264`)** fallbacks — keep under **~220 chars** if possible.
    2. Extend **`test/domain/inspect-summary.test.ts`** with a **`fallback-required`** plan fixture asserting **at least one** preservation note substring **`libx265`** OR **`HEVC`** (pick one deterministic phrase from the new copy).
  </action>
  <verify>
    <automated>bun test test/domain/inspect-summary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`rg -n "libx265|HEVC" src/domain/inspect-summary.ts`**
    - **`bun test test/domain/inspect-summary.test.ts`** exits **0**.
  </acceptance_criteria>
  <done>**Inspect** surfaces honest fallback encoder story.</done>
</task>

<task type="auto">
  <name>T2 — Clean run report: HEVC label for re-encoded video</name>
  <files>src/domain/clean-run-report.ts,test/domain/clean-run-report.test.ts,test/app/clean.test.ts</files>
  <read_first>
    - `src/domain/clean-run-report.ts`
    - `src/app/clean.ts` (where **`videoPolicy: "re-encoded"`** is set)
    - Existing tests that snapshot **`renderCleanRunReportText`**
  </read_first>
  <action>
    1. Update **`renderCleanRunReportText`** so when **`report.videoPolicy === "re-encoded"`**, the first line reads **`Video: re-encoded (HEVC, libx265)`** (exact string) instead of plain **`re-encoded`**.
    2. Keep **`VideoPolicyLabel`** type unchanged (**`"re-encoded"`** still the stored value).
    3. Add **`test/domain/clean-run-report.test.ts`** with one test: minimal **`CleanRunReport`** (all required fields) where **`videoPolicy: "re-encoded"`** — **`renderCleanRunReportText`** output line 1 equals **`Video: re-encoded (HEVC, libx265)`**.
    4. Update **`test/app/clean.test.ts`** expectations if any assert the old **`Video: re-encoded`** line.
  </action>
  <verify>
    <automated>bun test test/app/clean.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`bun test test/domain/clean-run-report.test.ts`** exits **0**.
    - **`bun test test/app/clean.test.ts`** exits **0** when run from repo root.
  </acceptance_criteria>
  <done>Text report matches **05-CONTEXT D-10**.</done>
</task>

<task type="auto">
  <name>T3 — CLI help: --allow-video-fallback on clean</name>
  <files>src/cli/command.ts</files>
  <read_first>
    - `src/cli/command.ts` (**`clean`** command **`--allow-video-fallback`** option)
    - `.planning/phases/05-x265-preferred-video-reencode/05-CONTEXT.md` (**Specific ideas** / performance)
  </read_first>
  <action>
    1. Extend the **`--allow-video-fallback`** **description** string on **`clean`** to mention that fallback **re-encodes video to HEVC (libx265)** and is **slower** than stream-copy / AVC — keep **one** help line if Commander wraps; otherwise **two** short sentences max.
  </action>
  <verify>
    <automated>bun test test/cli/command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`rg -n "libx265|HEVC" src/cli/command.ts`**
    - **`bun test test/cli/command.test.ts`** exits **0** (or skip file if no tests assert help — then run **`bun test`** subset that covers **`command.ts`**).
  </acceptance_criteria>
  <done>**--help** hints encoder posture.</done>
</task>

<task type="auto">
  <name>T4 — verifyCleanOutput / README</name>
  <files>test/domain/clean-output-verify.test.ts,README.md</files>
  <read_first>
    - `test/domain/clean-output-verify.test.ts`
    - `src/domain/stream-copy-feasibility.ts` (**`canonicalVideoCodecForVerify`**)
    - `README.md`
    - `.planning/phases/05-x265-preferred-video-reencode/05-CONTEXT.md` (**D-14**)
  </read_first>
  <action>
    1. **`clean-output-verify.test.ts`**: Add a test proving **`canonicalVideoCodecForVerify`** normalizes any **additional** HEVC **ffprobe** **`codec_name`** variant you document (e.g. if **`hev1`** appears in real **libx265** outputs — **only** if executor confirms; otherwise add a test comment referencing **05-CONTEXT D-14** + **existing** **`h265`/`hevc`** test as satisfying **MULTI-13** verify surface **without** new alias).
    2. **`README.md`**: Add **one short** bullet under usage / troubleshooting: **`--allow-video-fallback`** **re-encodes video to HEVC (`libx265`)** — **slower** than **AVC** / copy-first paths.
  </action>
  <verify>
    <automated>bun test test/domain/clean-output-verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`README.md`** contains **`libx265`** and **`HEVC`** or **`HEVC`** and **`slower`** in the same short paragraph/bullet about fallback.
    - **`bun test test/domain/clean-output-verify.test.ts`** exits **0**.
  </acceptance_criteria>
  <done>Docs + verify tests aligned with **MULTI-13**.</done>
</task>

</tasks>
