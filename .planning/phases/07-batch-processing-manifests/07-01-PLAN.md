---
phase: 07-batch-processing-manifests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/batch-manifest.ts
  - src/domain/batch-output-path.ts
  - test/domain/batch-output-path.test.ts
autonomous: true
requirements:
  - BATCH-04
  - BATCH-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07-2026-05-02T01-07-27
generated_at: "2026-05-02T01:08:33.055Z"
must_haves:
  truths:
    - "**`allocateBatchOutputPaths`** (or the landed name) is **pure**, takes **ordered resolved input paths** + **`maybeOutputDir`** + **`doesOutputExist`**, and assigns **unique** planned outputs using **`-2`**, **`-3`** … **stem disambiguation before the `.avdn.` segment** when basename collisions occur under **`--output-dir`** semantics described in **07-CONTEXT D-08–D-09**."
    - "**`BatchManifestV1`** (or equivalent) exports a **`schemaVersion: 1`** document shape and **per-item record** fields sufficient for **audit**: input, resolved output, outcome, failure kind, short message, and a **planned-summary / argv snapshot** placeholder typed as **`unknown` or a narrow interface** until wired to **`CleanPlanSummary`** in plan **07-03**."
    - **`bun test test/domain/batch-output-path.test.ts`** exits **0** and covers **no collision**, **two-way basename collision**, and **triple collision**."
  artifacts:
    - path: src/domain/batch-output-path.ts
      provides: BATCH-04 collision-safe path allocation
    - path: src/domain/batch-manifest.ts
      provides: BATCH-05 JSON schema types + helpers
---

<objective>
Land **pure-domain** batch output allocation and **manifest document types** so later plans can wire **`batch`** CLI + **`runCleanRequest`** without entangling filesystem side effects in planning logic (**BATCH-04**, **BATCH-05** foundations).

Output: **`src/domain/batch-output-path.ts`**, **`src/domain/batch-manifest.ts`**, **`bun test`** — **no** Commander / **`runCleanRequest`** changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-batch-processing-manifests/07-CONTEXT.md (D-08–D-10)
@src/domain/output-path.ts (`DEFAULT_OUTPUT_SUFFIX_SEGMENT`, `resolveOutputPath` default stem rule)
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Path allocation | Predictable writes only — logic must not traverse arbitrary roots | Pure functions take **already-resolved** path strings; callers canonicalize with **`resolve`/`normalize`** |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-01-01 | T | crafted simultaneous outputs | mitigate | Unit tests lock collision suffix behavior; no shell interpolation |

</threat_model>

<verification_criteria>
- `bun test test/domain/batch-output-path.test.ts` exits **0**.
- `rg -n "schemaVersion" src/domain/batch-manifest.ts`
- `rg -n "allocateBatchOutputPaths|batch-output-path" src/domain/batch-output-path.ts`
</verification_criteria>

<tasks>

<task type="auto">
  <name>Batch manifest types (v1)</name>
  <files>src/domain/batch-manifest.ts</files>
  <read_first>
    - .planning/phases/07-batch-processing-manifests/07-CONTEXT.md
    - src/domain/command-outcome.ts
    - src/app/clean.ts (`CleanPlanSummary` shape — import type only)
  </read_first>
  <action>
    Add **`src/domain/batch-manifest.ts`** exporting:

    - **`export const BATCH_MANIFEST_SCHEMA_VERSION = 1`** (or literal **`1`** on document root).
    - **`BatchManifestOutcome`** union: at minimum **`"success"`**, **`"failure"`**, **`"skipped"`**.
    - **`BatchManifestItemV1`** readonly object with fields: **`inputPath`**, **`resolvedOutputPath`** (string — may be empty when planning failed before resolution), **`outcome`**, **`maybeFailureKind`** (`invalid-input` \| `missing-tools` \| `planning-failure` \| `processing-failure` \| `fallback-required` \| **`null`**), **`message`** (string), **`plannedSummary`** (**`unknown`** or optional **`CleanPlanSummary`** import if circularity avoided — prefer **`unknown`** in this plan), **`argvSnapshot`** (**`readonly string[][]`** default empty — concrete argv arrays filled in **07-03**).

    - **`BatchManifestDocumentV1`** with **`schemaVersion: 1`**, **`generatedAt` ISO string**, **`items: readonly BatchManifestItemV1[]`**, **`maybeDoctorFacts`** (**`unknown`** placeholder for tool versions snapshot).

    Export **`emptyBatchManifestDraft(nowIso: string): BatchManifestDocumentV1`** helper.

  </action>
  <acceptance_criteria>
    - **`rg "BatchManifestDocumentV1" src/domain/batch-manifest.ts`** exits **0**.
    - **`rg "schemaVersion" src/domain/batch-manifest.ts`** matches at least **1** line.
    - **`bun run typecheck`** exits **0** (workspace-wide — may run after file added).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Pure batch output path allocation</name>
  <files>src/domain/batch-output-path.ts test/domain/batch-output-path.test.ts</files>
  <read_first>
    - src/domain/output-path.ts (`resolveDefaultOutputPath` behavior — duplicate stem rule **`stem.avdn.ext`** using **`DEFAULT_OUTPUT_SUFFIX_SEGMENT`**)
  </read_first>
  <action>
    Implement **`allocateBatchOutputPaths`** (exported) with signature equivalent to:

    **`function allocateBatchOutputPaths(input: { readonly cwd: string; readonly orderedInputPaths: readonly string[]; readonly maybeOutputDir?: string; readonly doesOutputExist: (p: string) => boolean; readonly force: boolean }): readonly { readonly inputPath: string; readonly resolvedOutputPath: string }[]`**

    Rules:

    1. For each **`orderedInputPaths`** entry, compute the **same default relative naming** as **`resolveOutputPath`** when **`maybeExplicitOutput`** is absent: **`dirname(input)` + stem + `.` + **`DEFAULT_OUTPUT_SUFFIX_SEGMENT`** + ext** (reuse logic by calling **`resolveOutputPath`** per file **without** `maybeExplicitOutput`, **or** duplicate **`resolveDefaultOutputPath`** math — prefer **calling **`resolveOutputPath`**** with **`cwd`**, **`inputPath`**, **`force`**, **`doesOutputExist`** to stay consistent).

    2. When **`maybeOutputDir`** is **`string`**, rewrite output to **`join(outputDir, basename(proposedOutput))`** (basename of the **resolved default output**, not raw input basename-only).

    3. **Collision repair**: if **`doesOutputExist`** returns true for a candidate **or** an earlier item in the batch already claimed the same **`resolvedOutputPath`**, mutate stem to **`${baseStem}-${n}`** where **`n`** starts at **2** and increments until unique (**`clip.avdn.m4a`** → **`clip-2.avdn.m4a`**). Apply **before** existence check loop terminates.

    4. When **`force`** is true, **`doesOutputExist`** may still return true — collision **between batch siblings** must not rely solely on disk (**in-memory set** of claimed outputs).

    Tests (**`test/domain/batch-output-path.test.ts`**):

    - **A:** Two distinct inputs mapping to **same** basename under **`maybeOutputDir`** → second path ends with **`-2`** segment before **`.avdn.`**.
    - **B:** Three-way collision → **`-3`** appears on third.
    - **C:** Single input → no numeric suffix.

  </action>
  <acceptance_criteria>
    - **`bun test test/domain/batch-output-path.test.ts`** exits **0**.
    - File **`src/domain/batch-output-path.ts`** exports **`allocateBatchOutputPaths`**.
  </acceptance_criteria>
</task>

</tasks>
