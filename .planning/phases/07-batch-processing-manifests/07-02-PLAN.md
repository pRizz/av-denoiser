---
phase: 07-batch-processing-manifests
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01-PLAN.md
files_modified:
  - package.json
  - bun.lock
  - src/domain/cli-request.ts
  - src/cli/command.ts
  - src/adapters/batch-input-expand.ts
  - test/cli/command.test.ts
autonomous: true
requirements:
  - BATCH-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07-2026-05-02T01-07-27
generated_at: "2026-05-02T01:08:33.055Z"
must_haves:
  truths:
    - **`CliRequest`** includes **`batch`** kind carrying **mutually validated** batch fields mirroring **`clean`** (**`presetId`**, **`knobs`**, **`force`**, **`dryRun`**, **`json`**, **`allowVideoFallback`**) plus **`inputPaths`**, **`globs`**, **`maybeFromDir`**, **`acceptGlobRisk`**, **`maybeOutputDir`**, **`maybeManifestPath`**, **`concurrency`**, **`failFast`**."
    - **`expandBatchInputs`** (adapter export) uses **`fast-glob`** **only when** **`acceptGlobRisk === true`**; otherwise throws **invalid-input** style error if **`globs.length > 0`**."
    - **`test/cli/command.test.ts`** parses **`av-denoiser batch --input a.wav --input b.wav --dry-run`** into **`batch`** request with **two** paths."
  artifacts:
    - path: src/cli/command.ts
      provides: `batch` Commander wiring
    - path: src/adapters/batch-input-expand.ts
      provides: glob + directory expansion
---

<objective>
Add **`p-limit`** + **`fast-glob`**, implement **`batch`** CLI parsing into **`CliRequest`**, and expand **paths / globs / directory** into a deterministic file list (**BATCH-01**).

Output: dependencies installed, **`batch`** command, adapter expansion — **no** batch execution loop yet (**07-03**).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/cli/command.ts (`clean` flags — duplicate preset/noise/dry-run/json/force/fallback)
@src/domain/cli-request.ts
@.planning/phases/07-batch-processing-manifests/07-CONTEXT.md (D-03, D-04)
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Glob patterns | Accidental massive expansion | Require **`--accept-glob-risk`**; document in **`--help`** |
| Directory crawl | User surprises | **`--from-dir`** only with explicit flag — no implicit recursion |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-02-01 | E | hostile glob | mitigate | Expansion runs as **current user**; doc trust boundary (local CLI) |

</threat_model>

<verification_criteria>
- `rg "\"p-limit\"" package.json` and `rg "\"fast-glob\"" package.json` each match **1** line.
- `rg "kind: \"batch\"" src/domain/cli-request.ts`
- `bun test test/cli/command.test.ts` exits **0**.
</verification_criteria>

<tasks>

<task type="auto">
  <name>Dependencies: p-limit + fast-glob</name>
  <files>package.json bun.lock</files>
  <read_first>
    - package.json
    - .planning/research/STACK.md
  </read_first>
  <action>
    Add **`"p-limit": "^7.3.0"`** and **`"fast-glob": "^3.3.3"`** under **`dependencies`** (exact caret alignment with STACK table). Run **`bun install`**.
  </action>
  <acceptance_criteria>
    - **`package.json`** contains **`p-limit`** and **`fast-glob`**.
    - **`bun.lock`** references both packages.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>CliRequest batch variant</name>
  <files>src/domain/cli-request.ts</files>
  <read_first>
    - src/domain/cli-request.ts
    - src/domain/audio-pipeline-plan.ts (`PresetId`, `CleanPresetKnobs`)
  </read_first>
  <action>
    Extend **`CliRequest`** discriminated union with:

    **`{ readonly kind: "batch"; readonly inputPaths: readonly string[]; readonly globs: readonly string[]; readonly maybeFromDir?: string; readonly acceptGlobRisk: boolean; readonly maybeOutputDir?: string; readonly maybeManifestPath?: string; readonly concurrency: number; readonly failFast: boolean; readonly force: boolean; readonly dryRun: boolean; readonly json: boolean; readonly presetId: PresetId; readonly knobs: CleanPresetKnobs; readonly allowVideoFallback: boolean }`**

    Omit separate **`maybeOutputPath`** per file at CLI level — outputs come from **07-01 allocator** + per-input defaults (**CONTEXT D-08**).

  </action>
  <acceptance_criteria>
    - **`rg "kind: \\\"batch\\\"" src/domain/cli-request.ts`** OR **`rg '\\"batch\\"' src/domain/cli-request.ts`** finds **`batch`** in union.
    - **`bun run typecheck`** exits **0**.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>batch-input-expand adapter</name>
  <files>src/adapters/batch-input-expand.ts</files>
  <read_first>
    - src/adapters/ffprobe.ts (path style only)
  </read_first>
  <action>
    Create **`expandBatchInputs`** exported async function:

    - **Inputs**: **`cwd`**, **`inputPaths`**, **`globs`**, **`maybeFromDir`**, **`acceptGlobRisk`**.
    - **If **`globs.length > 0`** and **`!acceptGlobRisk`**: throw **`Error`** with message containing **`glob`** and **`accept`** (executor maps to invalid-input).
    - **Glob**: use **`fast-glob`** with **`cwd`**, **`absolute: true`**, **`unique: true`** (or manual **`new Set`**).
    - **`fromDir`**: **fs **/`readdir`** with **`withFileTypes`** recursive option **or** single-level — **CONTEXT requires recursion implied by “directory ingestion”** → use **`fast-glob`** **`join(fromDir, '**/*')`** filtered by extension allowlist **`.wav,.mp4,.m4a,.mov,.mkv,.flac,.aac`** (lowercase compare) — document constant **`BATCH_FROM_DIR_EXTENSIONS`** at module top.
    - Concatenate **explicit paths** (resolved absolute) + glob results + dir results; **`sort`** lexicographically; **dedupe**.

  </action>
  <acceptance_criteria>
    - File exists **`src/adapters/batch-input-expand.ts`** exporting **`expandBatchInputs`**.
    - **`rg "fast-glob" src/adapters/batch-input-expand.ts`**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Commander: batch subcommand</name>
  <files>src/cli/command.ts test/cli/command.test.ts</files>
  <read_first>
    - src/cli/command.ts (`clean` command block)
    - src/cli/main.ts (`parseCliRequest` pattern)
  </read_first>
  <action>
    Add **`batch`** command:

    - Repeatable **`--input <path>`** collecting **`string[]`**.
    - Variadic **optional positionals** — if Commander conflicts, use **`--input`** only **OR** **`.argument('[files...]')`** — pick **one** documented style; recommended **repeatable `--input`** plus optional **`--`** passthrough **not** required.
    - **`--glob <pattern>`** repeatable.
    - **`--from-dir <dir>`**.
    - **`--accept-glob-risk`** boolean **false** default.
    - **`--output-dir <dir>`**, **`--manifest <path>`**, **`--concurrency <n>`** default **1**, **`--fail-fast`** boolean.
    - Mirror **`clean`**: **`-o`** **must NOT** appear (batch uses **`--output-dir`** only per CONTEXT).
    - **`--preset`**, **`--noise-strength`**, **`--dry-run`**, **`--json`**, **`--force`**, **`--allow-video-fallback`** — reuse **`parseNoiseStrength`** / **`parsePresetId`** helpers.

    Validation in **`action`**: **`concurrency`** integer **≥ 1**; if **`0`** or NaN → throw **`Error`**.

    **`test/cli/command.test.ts`**: add **`parseCliRequest(['batch','--input','a.wav','--input','b.wav','--dry-run'])`** expects **`kind === 'batch'`** and **`inputPaths.length === 2`**.

  </action>
  <acceptance_criteria>
    - **`rg "\\.command\\(\"batch\"" src/cli/command.ts`**
    - **`bun test test/cli/command.test.ts`** exits **0**.
  </acceptance_criteria>
</task>

</tasks>
