---
phase: 07-batch-processing-manifests
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
files_modified:
  - src/app/batch.ts
  - src/app/run-command.ts
  - src/cli/render.ts
  - src/cli/main.ts
  - src/domain/command-outcome.ts
  - test/app/batch.test.ts
  - test/cli/main.test.ts
  - test/domain/command-outcome-batch-exit.test.ts
autonomous: true
requirements:
  - BATCH-02
  - BATCH-03
  - BATCH-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 07-2026-05-02T01-07-27
generated_at: "2026-05-02T01:08:33.055Z"
must_haves:
  truths:
    - "**`runBatchRequest`** expands inputs → allocates outputs (**07-01**) → runs **`runCleanRequest`** per file with **`concurrency`** capped by **`p-limit`**; **`failFast`** stops enqueueing after first failing item returns (**pending tasks may not run**)."
    - "**Manifest JSON** written to **`maybeManifestPath`** or default **`batch-manifest.json`** in **`cwd`** (document exact default in **`--help`**) with **`schemaVersion: 1`** and **per-file** rows including **planned snapshot** (reuse **`CleanPlanSummary.steps[].displayCommand`** strings)."
    - "**Aggregate exit**: when **`batch`** completes with **`outcome.kind === 'success'`** and **`worstExitCode > 0`**, **`runCli`** returns **`worstExitCode`** (**BATCH-03**)."
    - **`batch --dry-run`** invokes **`runCleanRequest`** with **`dryRun: true`** for **every** input (**CONTEXT D-12**)."
  artifacts:
    - path: src/app/batch.ts
      provides: batch orchestration + manifest assembly
    - path: src/cli/render.ts
      provides: human + `--json` batch summaries
---

<objective>
Implement **`runBatchRequest`** orchestration, **`run-command`** routing, **manifest persistence**, **aggregate exit codes**, and **rendering** so **`batch`** satisfies **BATCH-02**, **BATCH-03**, **BATCH-05**, and dry-run parity (**07-CONTEXT D-12**).

Output: **`src/app/batch.ts`**, **`render`** updates, **`main`** exit mapping, tests with **injectable **`CleanDeps`**** (no real FFmpeg requirement for core batch logic tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/app/run-command.ts
@src/app/clean.ts (`runCleanRequest`, `CleanRunInput`, `CleanDeps`)
@src/adapters/batch-input-expand.ts
@src/domain/batch-manifest.ts
@src/domain/batch-output-path.ts
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Manifest contents | Embeds local paths | Document manifest as **local audit artifact** |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-03-01 | I | crash mid-batch leaves stale manifest | mitigate | **Atomic write**: build JSON string then **`Bun.write`** once at end (**v1**) |

</threat_model>

<verification_criteria>
- `rg "runBatchRequest" src/app/batch.ts`
- `rg "batch" src/app/run-command.ts`
- `bun test test/app/batch.test.ts` exits **0**
- `bun run verify` exits **0**
</verification_criteria>

<tasks>

<task type="auto">
  <name>Aggregate exit code helper</name>
  <files>src/domain/command-outcome.ts test/domain/command-outcome-batch-exit.test.ts</files>
  <read_first>
    - src/domain/command-outcome.ts
    - src/domain/exit-codes.ts
  </read_first>
  <action>
    Export **`aggregateBatchExitCodes(codes: readonly ExitCodeValue[]): ExitCodeValue`** — returns **0** when all values **0**, otherwise **`Math.max(...codes)`**.

    Add **`test/domain/command-outcome-batch-exit.test.ts`** with **3** cases: **[] → 0**, **[5, 2] → 5**, **[0, 0] → 0**.
  </action>
  <acceptance_criteria>
    - **`rg "aggregateBatchExitCodes" src/domain/command-outcome.ts`**
    - **`bun test test/domain/command-outcome-batch-exit.test.ts`** exits **0**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>runBatchRequest orchestration</name>
  <files>src/app/batch.ts src/app/run-command.ts</files>
  <read_first>
    - src/app/clean.ts (`runCleanRequest`)
    - src/adapters/batch-input-expand.ts
    - src/domain/batch-output-path.ts
    - src/domain/batch-manifest.ts
  </read_first>
  <action>
    Implement **`runBatchRequest`** in **`src/app/batch.ts`**:

    1. **`expandBatchInputs`** → sorted absolute paths.

    2. **`allocateBatchOutputPaths`** with **`cwd`**, **`doesOutputExist`** from **`CleanDeps`**.

    3. **`pLimit(req.concurrency)`** schedules **`runCleanRequest`** per file with **`maybeOutputPath`** set from allocator; pass **`json: false`** into **`runCleanRequest`** so summaries populate manifest.

    4. Track **`mapOutcomeToExitCode`** per finished item; **`failFast`**: after first **non-zero**, do **not** schedule remaining jobs (**await in-flight** optional).

    5. Build **`BatchManifestDocumentV1`**; **`maybeDoctorFacts`**: **`deps.discoverTools`** once when defined.

    6. Write **`req.maybeManifestPath ?? join(cwd, 'batch-manifest.json')`** via **`Bun.write`**.

    Return **`CliCommandOutcome`** **`success`** with **`batchResult`** field typed in **`run-command.ts`** / **`CliCommandOutcome`**: **`{ manifestPath, worstExitCode, document }`** **OR** embed **`worstExitCode`** on outcome for **`main.ts`**.

    Wire **`run-command`** **`case "batch"`**.

  </action>
  <acceptance_criteria>
    - **`rg 'case \"batch\"' src/app/run-command.ts`**
    - **`src/app/batch.ts`** exports **`runBatchRequest`**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Batch rendering + main exit mapping</name>
  <files>src/cli/render.ts src/cli/main.ts test/cli/main.test.ts</files>
  <read_first>
    - src/cli/render.ts (`renderCommandOutcome`)
    - src/cli/main.ts (`runCli`)
  </read_first>
  <action>
    Extend **`renderCommandOutcome`** for **`request.kind === 'batch'`**: human table vs **`JSON.stringify`** when **`req.json`**.

    **`runCli`**: when **`request.kind === 'batch'`** and **`outcome`** exposes **`worstExitCode`**, use **`worstExitCode`** as process exit even if **`outcome.kind === 'success'`**.

    Add **≥1** assertion in **`test/cli/main.test.ts`** for non-zero batch aggregate exit **OR** document **`render`** batch JSON snapshot test if **`runCli`** lacks injection hooks.

  </action>
  <acceptance_criteria>
    - **`rg "batch" src/cli/render.ts`**
    - **`bun test test/cli/main.test.ts`** exits **0**
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>batch.test.ts (mocked clean)</name>
  <files>test/app/batch.test.ts</files>
  <read_first>
    - test/app/guided-clean.test.ts
    - src/app/batch.ts
  </read_first>
  <action>
    **`test/app/batch.test.ts`**: mock **`runCleanRequest`**; temp **`cwd`**; assert manifest **`items.length`**; **`failFast`** reduces **`runCleanRequest`** call count when second fails.

  </action>
  <acceptance_criteria>
    - **`bun test test/app/batch.test.ts`** exits **0**
    - **`≥ 2`** **`test(`** blocks in file
  </acceptance_criteria>
</task>

</tasks>
