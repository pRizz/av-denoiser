---
phase: 15-milestone-gap-requirements-trace-sync
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - editorial-v1-requirements-sync
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 15-2026-05-03T13-28-25Z
generated_at: "2026-05-03T13:28:25.000Z"
must_haves:
  truths:
    - "Every v1 checklist line in REQUIREMENTS uses `[x]` when implementation + verification artifact exist; **`TOOL-07` checklist stays `[ ]`** until Phase **16** closes semantic gap."
    - "Traceability table **Status** is **`Complete`** for all rows **`TOOL-07` excluded** (**`TOOL-07` remains `Phase 16` + `Pending`**)."
    - "Each traceability row’s **Phase** column equals ROADMAP **Requirement Coverage** (same REQ-ID ordering as current table)."
    - "`bun run verify` exits **0** after edits (documentation-only)."
---

<objective>
Reconcile **[`.planning/REQUIREMENTS.md`](../../../../REQUIREMENTS.md)** with authoritative shipped evidence: ROADMAP **Requirement Coverage** and **passed** **`01`**–`**08`** `*-VERIFICATION.md` tables. Editorial only — **no `src/`** changes intended; **defer `TOOL-07` REQ text refinement** to **Phase 16**.
</objective>

<threat_model>
**Surface:** `.planning/REQUIREMENTS.md` prose only.

| Threat | Severity | Mitigation |
|--------|-----------|------------|
| Marking REQ satisfied incorrectly | Medium | Mirror ROADMAP column + citation to existing `*-VERIFICATION.md`; keep **TOOL-07** open pending **16**. |
| Drift vs executor mis-read | Low | Acceptance criteria below use **verbatim grep** substring checks.

No secrets, subprocess, or network scope added.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-VERIFICATION.md
@.planning/phases/02-media-probing-output-planning/02-VERIFICATION.md
@.planning/phases/03-video-preservation-fallback-control/03-VERIFICATION.md
@.planning/phases/04-core-audio-pipeline-sox-cleanup/04-VERIFICATION.md
@.planning/phases/05-final-media-output-reporting/05-VERIFICATION.md
@.planning/phases/06-guided-repeatable-workflows/06-VERIFICATION.md
@.planning/phases/07-batch-processing-manifests/07-VERIFICATION.md
@.planning/phases/08-optional-heavy-editor-integrations/08-VERIFICATION.md
</context>

<tasks>

<task type="manual">
  <name>Normalize v1 checklist checkboxes</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md`
    - `.planning/ROADMAP.md` (section **Requirement Coverage** + Phase **15** goal)
    - `.planning/v1.0-MILESTONE-AUDIT.md` (tech debt **docs-governance**)
  </read_first>
  <action>
    In **`## v1 Requirements`** checklist groups (**CLI**, **Media**, … **Verification and Trust**), set **`[x]`** for **every v1 REQ** whose ROADMAP **Requirement Coverage** row maps implementation to phases **1–14** completed work **except**:

    - Leave **`- [ ]`** only for **`TOOL-07`** (explicit Phase **16** follow-up closes audit integration row).

    Do **not** rewrite REQ prose (Phase **16** owns **TOOL-07** wording). Do **not** touch **`## v2 Requirements`** bodies except if a coverage footer references v1 totals.
  </action>
  <acceptance_criteria>
    - `grep -n '\*\*TOOL-07\*\*'` on REQUIREMENTS shows checklist line prefixed **`- [ ]`** (unchecked).
    - `grep -n '\*\*CLI-04\*\*'` checklist line prefixed **`- [x]`** after edit.
    - `grep -n '\*\*MEDIA-02\*\*'` checklist line prefixed **`- [x]`**.
    - `grep -n '\*\*VIDEO-04\*\*'` checklist line prefixed **`- [x]`**.
    - `grep -n '\*\*TOOL-01\*\*'` checklist line prefixed **`- [x]`**.
    - `grep -n '\*\*TOOL-03\*\*'` checklist line prefixed **`- [x]`**.
    - `grep -n '\*\*UX-01\*\*'` checklist line prefixed **`- [x]`**.
    - Exactly **one** v1 checklist item (**TOOL-07**) remains **`- [ ]`** among lines matching `^- \[ \].*\*\*[A-Z]+-[0-9]+\*\*` in the v1 sections (count via `grep` between `## v1 Requirements` and `## v2 Requirements`).
  </acceptance_criteria>
  <verify>
    <automated>
bash -eu -o pipefail -c 'cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && REQ=".planning/REQUIREMENTS.md" && awk "/^## v1 Requirements/,/^## v2 Requirements/" "$REQ" | grep "\*\*TOOL-07\*\*" | grep -q "^- \[ \]" && awk "/^## v1 Requirements/,/^## v2 Requirements/" "$REQ" | grep "\*\*CLI-04\*\*" | grep -q "^- \[x\]" && awk "/^## v1 Requirements/,/^## v2 Requirements/" "$REQ" | grep "\*\*MEDIA-02\*\*" | grep -q "^- \[x\]" && awk "/^## v1 Requirements/,/^## v2 Requirements/" "$REQ" | grep "\*\*VIDEO-04\*\*" | grep -q "^- \[x\]" && awk "/^## v1 Requirements/,/^## v2 Requirements/" "$REQ" | grep "\*\*TOOL-01\*\*" | grep -q "^- \[x\]" && awk "/^## v1 Requirements/,/^## v2 Requirements/" "$REQ" | grep "\*\*TOOL-03\*\*" | grep -q "^- \[x\]" && awk "/^## v1 Requirements/,/^## v2 Requirements/" "$REQ" | grep "\*\*UX-01\*\*" | grep -q "^- \[x\]" && test "$(awk "/^## v1 Requirements/,/^## v2 Requirements/" "$REQ" | grep -E "^- \[ \].*\*\*[A-Za-z]+-[0-9]+\*\*" | wc -l | tr -d " ")" -eq 1'
    </automated>
  </verify>
</task>

<task type="manual">
  <name>Sync traceability table Status column</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (section **Traceability**)
    - `.planning/ROADMAP.md` (**Requirement Coverage**)
  </read_first>
  <action>
    In the **`| Requirement | Phase | Status |`** table under **`## Traceability`**:

    1. For **each** REQ-ID **`CLI-01` … `TRUST-04`**:
       - Set **Phase** cell to exactly match `.planning/ROADMAP.md` **Requirement Coverage** table (**same phase label**, e.g. `Phase 14`, `Phase 10`).
       - Set **Status** → **`Complete`** for **all IDs except `TOOL-07`**.
    2. For **`TOOL-07`**: **Phase `Phase 16`**, **Status `Pending`** (unchanged if already so).
    3. Preserve intro paragraph referencing gap phases **15–17** unless it contradicts finalized checklist rules — then amend one sentence maximum to state **`TOOL-07` awaits Phase **16**.
  </action>
  <acceptance_criteria>
    - Markdown **pipe-delimited** table row **`| CLI-04 |`** includes **`Phase 14`** then **`Complete`** as adjacent cells on the same line.
    - Table row **`| MEDIA-01 |`** reads **`Phase 10`** then **`Complete`**.
    - Table row **`| UX-05 |`** reads **`Phase 14`** then **`Complete`**.
    - Table row **`| BATCH-01 |`** through **`| BATCH-04 |`** each **`Phase 14`** + **`Complete`**.
    - Table row **`| BATCH-05 |`** reads **`Phase 13`** + **`Complete`**.
    - Table row **`| TOOL-07 |`** reads **`Phase 16`** + **`Pending`** (grep line contains both substrings).
    - Exactly **one** traceability data row has status **Pending** (**`TOOL-07`**): `grep -E '^[|] [A-Za-z]+-[0-9]+' .planning/REQUIREMENTS.md | grep -F '| Pending |' | wc -l` prints **`1`**.
  </acceptance_criteria>
  <verify>
    <automated>
bash -eu -o pipefail -c 'cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && REQ=".planning/REQUIREMENTS.md" && grep -Fq "| CLI-04 | Phase 14 | Complete |" "$REQ" && grep -Fq "| MEDIA-01 | Phase 10 | Complete |" "$REQ" && grep -Fq "| UX-05 | Phase 14 | Complete |" "$REQ" && grep -Fq "| BATCH-01 | Phase 14 | Complete |" "$REQ" && grep -Fq "| BATCH-05 | Phase 13 | Complete |" "$REQ" && grep -Fq "| TOOL-07 | Phase 16 | Pending |" "$REQ" && test "$(grep -E "^[|] [A-Za-z]+-[0-9]+" "$REQ" | grep -F "| Pending |" | wc -l | tr -d " ")" -eq 1'
    </automated>
  </verify>
</task>

<task type="manual">
  <name>Refresh coverage footer + last updated</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (footer bullets + horizontal rule meta)
  </read_first>
  <action>
    1. Update **`**Coverage:**`** bullets to reflect:
       - **`42`** mapped v1 requirements.
       - Phases **`1`**–`**14`** delivered; phases **`15`**–`**17`** roadmap follow-ups with **`TOOL-07`** carried by **Phase 16**.
    2. Update trailing *Last updated:* line ISO date **today** documenting **Phase 15 plan executed** checklist + trace sync (after executor finishes).
  </action>
  <acceptance_criteria>
    - Footer contains substring **`42`** in coverage context.
    - Footer contains substring **`TOOL-07`** referencing **Phase **16**** or equivalent explicit deferral wording.
    - `*Last updated:*` line contains **≥** substring **`2026-05-03`** as calendar date (**YYYY-MM-DD**).
  </acceptance_criteria>
  <verify>
    <automated>
bash -eu -o pipefail -c 'cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && REQ=".planning/REQUIREMENTS.md" && awk "/^\*\*Coverage:\*\*/,/^---\$/" "$REQ" | grep -q "42" && tail -n 6 "$REQ" | grep -q "TOOL-07" && tail -n 6 "$REQ" | grep -Eq "Phase.?16|\*\*16\*\*" && grep -Eq "^\*Last updated:.*2026-05-(0[3-9]|[12][0-9]|3[01])" "$REQ"'
    </automated>
  </verify>
</task>

<task type="manual">
  <name>Repository verify gate</name>
  <files></files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (post-edit sanity)
  </read_first>
  <action>
    From repo root run **`bun run verify`**; expect exit code **0**. No **`src/`** or **`test/`** edits in this phase.
  </action>
  <acceptance_criteria>
    - Shell: `cd` repo root → `bun run verify`; exit **`0`**.
  </acceptance_criteria>
  <verify><automated>bun run verify</automated></verify>
</task>

</tasks>

## PLANNING COMPLETE
