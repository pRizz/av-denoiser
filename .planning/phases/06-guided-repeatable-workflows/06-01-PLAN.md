---
phase: 06-guided-repeatable-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/domain/guided-clean-selection.ts
  - src/domain/guided-clean-equivalent.ts
  - test/domain/guided-clean-equivalent.test.ts
autonomous: true
requirements:
  - CLI-04
generated_by: gsd-plan-phase
lifecycle_mode: direct-fallback
phase_lifecycle_id: 06-2026-05-02T00-47-46
generated_at: "2026-05-02T00:47:46.439Z"
must_haves:
  truths:
    - "**`GuidedCleanSelections`** type exists and lists every **`clean`** flag the wizard must bind (**`inputPath`**, **`maybeOutputPath`**, **`force`**, **`dryRun`**, **`presetId`**, **`noiseStrength`**, **`allowVideoFallback`**) — names grep-stable."
    - "**`argvTokensForEquivalentClean`** returns **`readonly string[]`** that **`join(\" \")`** reproduces an **`av-denoiser clean ...`** invocation equivalent to **`CleanRunInput`** built from the same selections."
    - "**`bun test test/domain/guided-clean-equivalent.test.ts`** exits **0** with at least **3** distinct equivalence cases (minimal flags, **`--preset speech-soft-sox`**, **`--allow-video-fallback --force`** combo)."
  artifacts:
    - path: src/domain/guided-clean-equivalent.ts
      provides: pure argv mapping for CLI-04 / UX-04
---

<objective>
Land **`@clack/prompts`** dependency and **pure-domain** types + argv builder so guided mode can print/copy an **exact non-interactive replay** of user choices (**CLI-04** foundation).

Output: domain modules + **`bun test`** coverage — **no** interactive prompts in this plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/domain/product.ts (`cliName`)
@src/cli/command.ts (`clean` flags — mirror ordering and spelling)
@src/domain/audio-pipeline-plan.ts (`PresetId`, defaults)
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Paths embedded in argv string | User-supplied paths echoed — must not invoke shell | Builder returns **argv tokens**, not **`sh -c`**; quoting rules documented for paths-with-spaces |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-01-01 | E | crafted paths break quoting | mitigate | Document **`spawn`**-safe consumption; shell-style quoting helper **double-quotes** paths containing whitespace |

</threat_model>

<verification_criteria>
- `bun install` succeeds after **`package.json`** adds **`@clack/prompts`**.
- `bun test test/domain/guided-clean-equivalent.test.ts` exits **0**.
- `rg -n "GuidedCleanSelections" src/domain/guided-clean-selection.ts`
- `rg -n "argvTokensForEquivalentClean" src/domain/guided-clean-equivalent.ts`
</verification_criteria>

<tasks>

<task type="auto">
  <name>Dependency: @clack/prompts</name>
  <files>package.json bun.lock</files>
  <read_first>
    - package.json
    - .planning/research/STACK.md
  </read_first>
  <action>
    Add **`"@clack/prompts": "^1.2.0"`** (or **`^1.2`** consistent with lockfile policy) under **`dependencies`**. Run **`bun install`** so **`bun.lock`** updates.
  </action>
  <acceptance_criteria>
    - **`rg "@clack/prompts" package.json`** matches **1** line.
    - **`bun.lock`** contains **`@clack/prompts`** after install.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>GuidedCleanSelections record</name>
  <files>src/domain/guided-clean-selection.ts</files>
  <read_first>
    - src/app/clean.ts (`CleanRunInput`)
    - src/domain/audio-pipeline-plan.ts
  </read_first>
  <action>
    Export **`GuidedCleanSelections`** as **`readonly`** fields mirroring **`CleanRunInput`** except omit **`json`** (guided UX defaults to human summary — JSON stays flag-driven only).

    Include **`noiseStrength: number`** aligned with **`CleanPresetKnobs`**.
  </action>
  <acceptance_criteria>
    - File exports **`export type GuidedCleanSelections`**.
    - **`rg "noiseStrength" src/domain/guided-clean-selection.ts`** exits **0**.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Pure argv equivalence helper + tests</name>
  <files>src/domain/guided-clean-equivalent.ts test/domain/guided-clean-equivalent.test.ts</files>
  <read_first>
    - src/domain/product.ts
    - src/cli/command.ts
    - src/domain/guided-clean-selection.ts
  </read_first>
  <action>
    Implement **`argvTokensForEquivalentClean(selections: GuidedCleanSelections): readonly string[]`**:

    - Token **0**: **`cliName`** from **`product.ts`** (**`av-denoiser`**).
    - Token **1**: **`clean`**.
    - Then **`inputPath`** as **single token** — if path contains whitespace or quotes, wrap using **posix-safe double-quote escaping** (`"` → `\"`).
    - Emit **`-o`** / explicit output only when **`maybeOutputPath`** is **`string`** (non-empty).
    - Emit **`--force`** only when **`force === true`**.
    - Emit **`--dry-run`** only when **`dryRun === true`**.
    - Emit **`--allow-video-fallback`** only when **`allowVideoFallback === true`**.
    - Emit **`--preset &lt;presetId&gt;`** always (explicit preset — match **`command.ts`** default **`speech-light`** behavior by still emitting **`--preset speech-light`** when that is selected so replay is literal).

    Emit **`--noise-strength &lt;value&gt;`** using **same numeric string Commander would produce** (trim insignificant zeros acceptable if **`Number`** round-trips **`parseNoiseStrength`** rules — tests lock observable argv).

    Tests (**minimum 3**):

    1. Minimal **`speech-light`**, no optional flags, relative input **`./in.wav`**.
    2. **`speech-soft-sox`**, **`--force`**, **`--allow-video-fallback`**, explicit **`-o out.mkv`**.
    3. Path with **space** proves quoting expands correctly when joined for display.

  </action>
  <acceptance_criteria>
    - **`bun test test/domain/guided-clean-equivalent.test.ts`** exits **0**.
    - **`rg "argvTokensForEquivalentClean" src/domain/guided-clean-equivalent.ts`**
  </acceptance_criteria>
</task>

</tasks>
