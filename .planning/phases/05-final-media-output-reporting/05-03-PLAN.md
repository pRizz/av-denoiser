---
phase: 05-final-media-output-reporting
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/app/clean.ts
  - test/app/clean.test.ts
autonomous: true
requirements:
  - MEDIA-02
  - TOOL-01
  - TRUST-02
  - TRUST-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 05-2026-05-02T00-37-51
generated_at: "2026-05-02T02:00:00.000Z"
must_haves:
  truths:
    - "**`video-copy-safe`** inputs execute **extract → sequential preset steps (PCM/audio tail) → remux** without claiming success until **`verifyCleanOutput`** returns **`ok`** (**TRUST-03**)."
    - "**`fallback-required`** executes only when **`CleanRunInput.allowVideoFallback === true`** mirroring **`inspect`** gate (**05-CONTEXT D-02**)."
    - "**Encode-deliverable** for video modalities writes **intermediate audio file** (suffix consistent with **`plannedContainer`**) then **`buildRemuxVideoCopyCommand`** merges into **`resolvedOutputPath`** (**RESEARCH Option A**)."
    - "Failures surface **`processing-failure`** with **`ffmpeg:`**/**`sox:`** prefixed messages + **`MAX_CLEAN_STDERR_SNIPPET`** truncation (**TRUST-02** continuity)."
  artifacts:
    - path: src/app/clean.ts
      provides: video-aware clean orchestration + verification + report assembly
---

<objective>
Extend **`runCleanRequest`** to execute **MEDIA-02**/**TOOL-01**: handle **`video-copy-safe`** and approved **`fallback-required`** paths using **`video-clean-argv`** extract/remux around the existing sequential pipeline, populate **`CleanRunReport`**, and run **`verifyCleanOutput`** on the final file.

Purpose: Functional-core orchestration without CLI changes (next wave).

Output: `src/app/clean.ts`, `test/app/clean.test.ts` updates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/clean.ts
@test/app/clean.test.ts
@src/domain/video-clean-argv.ts
@src/domain/clean-output-verify.ts
@src/domain/clean-run-report.ts
@src/domain/output-plan.ts
@src/adapters/ffprobe.ts

<interfaces_hint>
Extend **`CleanRunInput`** with **`allowVideoFallback: boolean`** (default **`false`** when wired from CLI next wave).

**Drop-list reporting (**D-10**):** derive **`droppedStreamsLabels`** from **`MediaProbe.streams`** excluding **primary video** + **selected audio** (e.g. **`subtitle:`**, **`audio:#`** strings).

**Execution sketch (executor MUST follow):**

1. **`planMediaOutput`** unchanged.
2. **`unsupported`** ⇒ **`invalid-input`** (update copy to remove stale **audio-only** wording where inaccurate—still mention **`inspect`**).
3. **`video-copy-safe`** OR (**`fallback-required`** && **`allowVideoFallback`**):
   - Allocate **`tempRoot`** as today.
   - **`extractPath = join(tempRoot, \"extracted.wav\")`** — build/run **`buildExtractPrimaryAudioWavCommand`**.
   - Run logical steps loop with **`inputMediaPath`**/**`resolvedInputPath` surrogate** = **`extractPath`** for **`buildLogicalStepCommand`** contexts (freeze approach in **`05-03-SUMMARY.md`**).
   - When encode-deliverable fires, target **`join(tempRoot, \"final-audio-deliverable.m4a\")`** or **`.wav`** per planner choice—must not overwrite **`resolvedOutputPath`** before remux.
   - Run **`buildRemuxVideoCopyCommand`** → **`resolvedOutputPath`**.
4. **`fallback-required`** && **`!allowVideoFallback`** ⇒ existing **`fallback-required`** outcome (message updated to mention **`clean --allow-video-fallback`** substring).

**Post-success:** **`runFfprobeProbe`** on output + **`verifyCleanOutput`**; failure ⇒ **`processing-failure`** with reason **`detail`** surfaced.

**Dry-run:** summaries include **Extract**/**Remux** pseudo-steps (`displayCommand` via **`renderDisplayCommand`**) ahead of preset steps OR appended—choose one ordering and document; must list **≥ 2** new display lines containing **`ffmpeg`** for video modality dry-run.

</interfaces_hint>
</context>

<threat_model>
## Trust boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Temp dirs | leaking paths | Cleanup **success** path **`rmSync`** recursive like Phase 4 |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-03-01 | D | partial output file | mitigate | Remove **`resolvedOutputPath`** on failure before returning **optional**—Claude discretion; minimum: report failure clearly |

</threat_model>

<verification_criteria>
- `bun test test/app/clean.test.ts` exits **0**.
- `rg -n "allowVideoFallback" src/app/clean.ts`
- `rg -n "verifyCleanOutput" src/app/clean.ts`
</verification_criteria>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend clean tests for video path mocks</name>
  <files>test/app/clean.test.ts</files>
  <read_first>
    - src/app/clean.ts
    - src/domain/output-plan.ts
    - test/app/inspect.test.ts
  </read_first>
  <action>
    Add **`video-copy-safe`** happy-path test (mock **`runProcess`** sequence):

    - Expect **≥ 1** extract invocation argv containing **`-vn`**.
    - Expect **≥ 1** remux argv containing **`-c:v`** **`copy`**.
    - **`dryRun: true`** video modality yields summary **`steps`** length **≥** prior audio-only count + **2** (extract + remux) OR embeds extract/remux display rows—freeze assertion comment.

    Update obsolete test expecting **`Phase 5`** failure substring for **`video-copy-safe`** — replace with success OR skipped expectation per new behavior.

    Add **`fallback-required`** + **`allowVideoFallback: false`** still **`fallback-required`** kind.

    Add **`allowVideoFallback: true`** mock path executing without **`fallback-required`** early exit (use **`MediaProbe`** fixture forcing **`fallback-required`** modality consistent with **`planMediaOutput`** rules).

  </action>
  <verify>
    <automated>bun test test/app/clean.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`rg -n "video-copy-safe" test/app/clean.test.ts`**
    - **`rg -n "allowVideoFallback" test/app/clean.test.ts`**
    - **`rg -n "\\-c:v" test/app/clean.test.ts`** OR **`rg -n "copy" test/app/clean.test.ts`**
  </acceptance_criteria>
  <done>Tests describe new FFmpeg call ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Implement video orchestration + verify + report assembly</name>
  <files>src/app/clean.ts</files>
  <read_first>
    - test/app/clean.test.ts
    - src/domain/video-clean-argv.ts
    - src/domain/clean-output-verify.ts
    - src/domain/clean-run-report.ts
    - src/domain/audio-pipeline-argv.ts
  </read_first>
  <action>
    Implement branching per **`interfaces_hint`**.

    **`CleanCliSuccess`** gains optional **`reportText?: string`** OR extend **`CleanPlanSummary`** — choose minimal shape; success responses must include **`renderCleanRunReportText`** output stored for CLI wave.

    Wire **`verifyCleanOutput`** with **`outputExists`**, **`outputFileSize`** lambdas using **`fs.existsSync`** + **`statSync`** size.

    **`claimedVideoCopied`**: **`true`** when executed branch used **`-c:v copy`** remux (video-copy-safe path); **`fallback-required`** executions set **`re-encoded`** or honest **`copied`** per actual argv—must match **`verifyCleanOutput`** expectations.

  </action>
  <verify>
    <automated>bun test test/app/clean.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **`bun test test/app/clean.test.ts`** exits **0**
    - **`rg -n "buildExtractPrimaryAudioWavCommand" src/app/clean.ts`**
    - **`rg -n "buildRemuxVideoCopyCommand" src/app/clean.ts`**
  </acceptance_criteria>
  <done>Video clean executes under mocks + verification invoked.</done>
</task>

</tasks>

<success_criteria>
- Single-file video cleaning works in mocked harness mirroring production ordering.
- Verification gate exists on success path.

</success_criteria>

<output>
After completion, create `.planning/phases/05-final-media-output-reporting/05-03-SUMMARY.md`
</output>
