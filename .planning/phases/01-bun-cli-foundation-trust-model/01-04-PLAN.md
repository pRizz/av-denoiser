---
phase: 01-bun-cli-foundation-trust-model
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - src/cli/command.ts
  - src/cli/render.ts
  - src/app/run-command.ts
  - src/app/doctor.ts
  - test/cli/main.test.ts
  - test/cli/command.test.ts
  - test/app/doctor.test.ts
  - README.md
autonomous: true
requirements:
  - CLI-02
  - CLI-03
  - TRUST-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 01-2026-05-01T23-00-58
generated_at: "2026-05-01T23:45:00.000Z"
must_haves:
  truths:
    - "`doctor` command prints structured readiness facts and exits nonzero when required tools are absent."
    - "README lists stable exit code integers matching `ExitCode` exports."
    - "`bun run verify` passes as the aggregate Phase 1 gate."
  artifacts:
    - path: src/cli/command.ts
      provides: "Commander wiring for doctor/help/default"
      contains: "doctor"
    - path: README.md
      provides: "User-facing exit code documentation"
      contains: "Exit"
    - path: package.json
      provides: "verify script"
      contains: "verify"
---

<objective>
Integrate and document the user-visible CLI shell: `doctor` preflight, stable exit mapping at the process boundary, and repo verification coverage—closing `CLI-02`, documenting `CLI-03`, and satisfying `TRUST-04`.

Purpose: Users and CI must observe the same contracts implemented in code (D-02, D-03, D-11).
Output: Tests/README aligned with existing Commander + render pipeline; minimal code edits only if mismatches appear.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/cli/command.ts
@src/cli/render.ts
@src/cli/main.ts
@src/app/run-command.ts
@src/app/doctor.ts
@test/cli/main.test.ts
@test/cli/command.test.ts
@test/app/doctor.test.ts
@README.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI integration tests for doctor + exit mapping</name>
  <files>test/cli/main.test.ts, test/cli/command.test.ts, test/app/doctor.test.ts, src/cli/command.ts, src/cli/render.ts, src/app/run-command.ts</files>
  <read_first>
    - src/cli/main.ts
    - src/cli/command.ts
    - src/app/run-command.ts
    - test/cli/main.test.ts
    - test/app/doctor.test.ts
  </read_first>
  <action>
    Audit Commander wiring per D-03 (syntax-only commander; outcomes from `runCliRequest`). Extend tests only where gaps exist: ensure `parseCliRequest(['doctor'])` yields `{ kind: 'doctor' }`, help flags resolve to `show-help`, unknown commands surface `parse-error`/`invalid-input` outcomes with exit code 2. Confirm `runCliRequest({ kind: 'doctor' }, injected deps)` tests already prove missing required tools map to `ExitCode.missingTools`—add assertions on rendered output substrings from `renderCommandOutcome` if stderr messaging is untested. Do **not** add media processing commands.
  </action>
  <verify>
    <automated>bun test test/cli/main.test.ts test/cli/command.test.ts test/app/doctor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'doctor' src/cli/command.ts` succeeds.
    - `rg 'runCliRequest' src/app/run-command.ts test/app/doctor.test.ts` succeeds.
    - `bun test test/cli/main.test.ts test/cli/command.test.ts test/app/doctor.test.ts` exits 0.
  </acceptance_criteria>
  <done>CLI-02 behavior is covered by automated tests at the request/router layer.</done>
</task>

<task type="auto">
  <name>Task 2: Document exit codes + lock verification gate</name>
  <files>README.md, package.json, src/domain/exit-codes.ts</files>
  <read_first>
    - README.md
    - src/domain/exit-codes.ts
    - package.json
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Append a concise **Exit codes** section to `README.md` listing each `ExitCode` name with integer value and one-line meaning matching `src/domain/exit-codes.ts` and REQUIREMENTS `CLI-03`. Include `bun run verify` as the canonical CI/local gate referencing scripts already in `package.json`. Run `bun run verify` after doc edits to satisfy TRUST-04 aggregate coverage (Biome scoped per D-14 + typecheck + full test suite including domain/adapters/cli tests).
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Exit codes' README.md` exits 0.
    - `rg -n 'success|internalError|invalidInput|missingTools|planningFailure|processingFailure|fallbackRequired|\\b0\\b|\\b1\\b|\\b2\\b|\\b3\\b|\\b4\\b|\\b5\\b|\\b6\\b' README.md` matches every row for codes 0–6 (spot-check: seven numeric codes appear in the Exit codes section).
    - `rg '\"verify\"' package.json` exits 0.
  </acceptance_criteria>
  <done>CLI-03 is user-documented; TRUST-04 aggregate verification passes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw `process.argv` → Commander | Untrusted user CLI tokens cross here; must never be interpolated into shell execution strings. |
| CLI stderr/stdout → user terminal | Diagnostics must avoid dumping full environment secrets by default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Elevation of privilege | CLI argument handling | mitigate | High severity: keep Commander `exitOverride` path swallowing default exits; route failures through `runCli` mapping only; never `exec`/`spawn` with shell and user args together. |
| T-01-04-02 | Tampering | Doctor rendering | mitigate | Render from structured `DoctorReport` values (already aggregated); do not execute tool binaries inside render functions. |
| T-01-04-03 | Information disclosure | Diagnostics | mitigate | Keep README guidance concise; refer users to `doctor` rather than echoing raw tool stderr in docs. |
</threat_model>

<verification>
Run `bun run verify`.
</verification>

<success_criteria>
Doctor command is trustworthy and tested; exit codes are documented; aggregate verification proves parsers/tests/cli wiring jointly satisfy Phase 1 readiness claims.
</success_criteria>
