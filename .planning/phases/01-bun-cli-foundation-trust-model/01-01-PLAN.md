---
phase: 01-bun-cli-foundation-trust-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - tsconfig.json
  - biome.json
  - src/index.ts
  - src/cli/main.ts
  - src/cli/command.ts
  - src/cli/render.ts
  - src/domain/cli-request.ts
autonomous: true
requirements:
  - CLI-01
  - TRUST-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-05-01T21-21-03
generated_at: 2026-05-01T21:28:04Z
must_haves:
  truths:
    - "User can install dependencies with Bun and run the TypeScript CLI locally."
    - "User can see honest help and default output that points to doctor without claiming media processing support."
    - "Executor can run repo-native format, lint, typecheck, test, and aggregate verification commands."
  artifacts:
    - path: package.json
      provides: "Bun package metadata, bin entry, dependency declarations, and repo-owned scripts"
      contains: '"verify"'
    - path: tsconfig.json
      provides: "Strict Bun-friendly TypeScript compiler settings"
      contains: '"noUncheckedIndexedAccess": true'
    - path: biome.json
      provides: "Biome formatter and linter configuration"
      contains: '"organizeImports"'
    - path: src/cli/main.ts
      provides: "Executable Bun CLI entrypoint"
      contains: '#!/usr/bin/env bun'
    - path: src/domain/cli-request.ts
      provides: "Typed command request model"
      contains: 'kind: "doctor"'
  key_links:
    - from: package.json
      to: src/cli/main.ts
      via: "bin entry and cli/doctor scripts"
      pattern: '"av-denoiser": "./src/cli/main.ts"'
    - from: src/cli/main.ts
      to: src/cli/command.ts
      via: "program construction"
      pattern: 'createCommandProgram'
    - from: src/cli/command.ts
      to: src/domain/cli-request.ts
      via: "typed request callback"
      pattern: 'CliRequest'
---

<objective>
Create the Bun/TypeScript project shell, strict verification surface, and typed CLI entrypoint required for Phase 1.

Purpose: This gives later trust-model work a safe executable surface, a package/script contract, and a typed request model per D-01, D-02, D-03, D-07, D-08, D-10, and D-11.
Output: `package.json`, `bun.lock`, strict TypeScript and Biome configs, and an initial CLI that can show help/default guidance and route `doctor` into a typed request.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-CONTEXT.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md

<interfaces>
Create these contracts in this plan so downstream plans can implement against them without rediscovery.

From `src/domain/cli-request.ts`:
```typescript
export type CliRequest =
  | { readonly kind: "show-default" }
  | { readonly kind: "show-help" }
  | { readonly kind: "doctor" };

export type CliRequestResult =
  | { readonly kind: "parsed"; readonly request: CliRequest }
  | { readonly kind: "parse-error"; readonly message: string };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Bun package and verification configuration</name>
  <files>package.json, bun.lock, tsconfig.json, biome.json</files>
  <read_first>
    - package.json (create if absent)
    - bun.lock (generated by `bun install`)
    - tsconfig.json (create if absent)
    - biome.json (create if absent)
    - .planning/phases/01-bun-cli-foundation-trust-model/01-CONTEXT.md
    - .planning/phases/01-bun-cli-foundation-trust-model/01-RESEARCH.md
    - AGENTS.bright-builds.md
  </read_first>
  <action>
    Initialize the Bun package surface per D-01 and D-11. Create `package.json` with `"name": "av-denoiser"`, `"type": "module"`, `"bin": { "av-denoiser": "./src/cli/main.ts" }`, scripts `"cli": "bun run src/cli/main.ts"`, `"doctor": "bun run src/cli/main.ts doctor"`, `"format": "biome format --write ."`, `"format:check": "biome format ."`, `"lint": "biome lint ."`, `"check:biome": "biome ci ."`, `"typecheck": "tsc --noEmit"`, `"test": "bun test"`, and `"verify": "bun run check:biome && bun run typecheck && bun test"`. Install runtime packages with `bun add commander @commander-js/extra-typings zod` and dev packages with `bun add -d typescript @types/bun @biomejs/biome`, producing `bun.lock`. Create `tsconfig.json` using Bun-friendly strict settings: `"module": "Preserve"`, `"moduleResolution": "bundler"`, `"types": ["bun"]`, `"strict": true`, `"noUncheckedIndexedAccess": true`, `"noFallthroughCasesInSwitch": true`, and include `src/**/*.ts` plus `test/**/*.ts`. Create `biome.json` enabling formatter, linter recommended rules, and organize imports. Expected command output: `bun install` completes without dependency errors and `bun run typecheck` can run even before tests exist.
  </action>
  <verify>
    <automated>bun install && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg '"av-denoiser": "./src/cli/main.ts"' package.json` finds the bin entry.
    - `rg '"verify": "bun run check:biome && bun run typecheck && bun test"' package.json` finds the aggregate verification script.
    - `rg '"noUncheckedIndexedAccess": true' tsconfig.json` confirms strict index handling.
    - `test -f bun.lock` confirms Bun lockfile generation.
  </acceptance_criteria>
  <done>The repository has a Bun-owned install/script/check surface with committed package, lockfile, TypeScript, and Biome configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed CLI entrypoint and request contracts</name>
  <files>src/index.ts, src/cli/main.ts, src/cli/command.ts, src/cli/render.ts, src/domain/cli-request.ts</files>
  <read_first>
    - src/index.ts (create if absent)
    - src/cli/main.ts (create if absent)
    - src/cli/command.ts (create if absent)
    - src/cli/render.ts (create if absent)
    - src/domain/cli-request.ts (create if absent)
    - package.json
    - .planning/phases/01-bun-cli-foundation-trust-model/01-CONTEXT.md
    - .planning/phases/01-bun-cli-foundation-trust-model/01-RESEARCH.md
  </read_first>
  <action>
    Create the initial CLI contract per D-01, D-02, and D-03. Add `#!/usr/bin/env bun` to `src/cli/main.ts`; it should import `createCommandProgram` from `src/cli/command.ts`, collect a `CliRequest`, and render default/help text through `src/cli/render.ts` without media-processing flags. Use Commander from `@commander-js/extra-typings` in `createCommandProgram` with program name `av-denoiser`, description `Clean noisy audio in local media files through a safe typed pipeline`, and a `doctor` subcommand without strict-mode or media-processing options. Define `CliRequest` and `CliRequestResult` exactly as listed in the `<interfaces>` block. The default invocation should render text containing `Run "av-denoiser doctor" to inspect local tool readiness.` and `Media processing commands are not available in this phase.` Export `CliRequest`, `CliRequestResult`, `createCommandProgram`, and render helpers from `src/index.ts`.
  </action>
  <verify>
    <automated>bun run cli && bun run src/cli/main.ts --help && bun run doctor</automated>
  </verify>
  <acceptance_criteria>
    - `rg '#!/usr/bin/env bun' src/cli/main.ts` confirms the executable shebang.
    - `rg 'type CliRequest' src/domain/cli-request.ts` and `rg 'kind: "doctor"' src/domain/cli-request.ts` confirm the typed request model.
    - `bun run cli` exits `0` and prints `Run "av-denoiser doctor" to inspect local tool readiness.`
    - `bun run src/cli/main.ts --help` exits `0` and includes `doctor`.
  </acceptance_criteria>
  <done>The CLI can be executed through Bun, emits honest default/help output, and routes command syntax into a typed request instead of media behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Prove scaffold verification runs</name>
  <files>package.json, tsconfig.json, biome.json, src/index.ts, src/cli/main.ts, src/cli/command.ts, src/cli/render.ts, src/domain/cli-request.ts</files>
  <read_first>
    - package.json
    - tsconfig.json
    - biome.json
    - src/index.ts
    - src/cli/main.ts
    - src/cli/command.ts
    - src/cli/render.ts
    - src/domain/cli-request.ts
  </read_first>
  <action>
    Run the repo-owned verification scripts added in Task 1 and fix only issues introduced by this plan. Expected command outputs: `bun run format:check` reports formatted files, `bun run lint` exits `0`, `bun run typecheck` exits `0`, and `bun test` exits `0` with no failing tests. If Biome reports formatting differences, run `bun run format` once, then rerun `bun run verify`.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `bun run verify` exits `0`.
    - `bun run doctor` exits `0` or `3` only after Plan 02 adds required-tool status; during this plan it must not throw an uncaught exception.
    - `rg 'Media processing commands are not available in this phase' src/cli/render.ts` confirms the default output does not advertise deferred commands.
  </acceptance_criteria>
  <done>Scaffold files pass formatting, linting, typechecking, and the current test runner surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv -> Commander parser | User-provided command syntax enters the process. |
| package scripts -> local shell | Developer verification commands run through Bun scripts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `src/cli/command.ts` | mitigate | Parse command syntax into `CliRequest` values only; do not execute media tools or shell strings in command handlers. |
| T-01-02 | Repudiation | `package.json` scripts | mitigate | Add stable repo-owned scripts so verification has named commands instead of ad hoc shell fragments. |
| T-01-03 | Information Disclosure | `src/cli/render.ts` | accept | Default/help output contains product guidance only and no environment, path, or secret inspection. |
</threat_model>

<verification>
Run `bun run verify`. Confirm `bun run cli`, `bun run src/cli/main.ts --help`, and `bun run doctor` execute through `src/cli/main.ts` without uncaught exceptions.
</verification>

<success_criteria>
Phase 1 Plan 01 succeeds when a clean checkout can install with Bun, run the TypeScript CLI, see default/help output, and execute the repo-owned verification command surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bun-cli-foundation-trust-model/01-01-SUMMARY.md`.
</output>
