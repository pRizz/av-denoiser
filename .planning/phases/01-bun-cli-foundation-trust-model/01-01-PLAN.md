---
phase: 01-bun-cli-foundation-trust-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - biome.json
  - src/cli/main.ts
  - src/index.ts
autonomous: true
requirements:
  - CLI-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 01-2026-05-01T23-00-58
generated_at: "2026-05-01T23:45:00.000Z"
must_haves:
  truths:
    - "`bun install` succeeds and documented scripts run the CLI entrypoint without Node/ts-node."
    - "TypeScript strict settings and Biome scope match Phase 1 boundaries (repo-owned code + tests only)."
    - "`package.json` exposes `bin.av-denoiser` → `src/cli/main.ts` per D-01."
  artifacts:
    - path: package.json
      provides: "bin mapping and verify-capable scripts"
      contains: "\"bin\""
    - path: tsconfig.json
      provides: "strict Bun TS compiler defaults"
      contains: "\"strict\""
    - path: biome.json
      provides: "scoped lint/format includes"
      contains: "\"src/**\""
---

<objective>
Re-verify and minimally align the Bun package surface, compiler settings, Biome scope, and library exports so `CLI-01` remains satisfied without regressing later-phase modules.

Purpose: Phase 1 already landed `src/cli/main.ts` and tooling; this plan prevents greenfield drift and locks install/run contracts per D-01, D-11, D-14.
Output: No behavioral surprises—only justified edits to package/tsconfig/biome/exports if gaps are found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-CONTEXT.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-RESEARCH.md
@AGENTS.md
@package.json
@tsconfig.json
@biome.json
@src/cli/main.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit package + toolchain shape against CONTEXT</name>
  <files>package.json, tsconfig.json, biome.json</files>
  <read_first>
    - .planning/phases/01-bun-cli-foundation-trust-model/01-CONTEXT.md
    - package.json
    - tsconfig.json
    - biome.json
  </read_first>
  <action>
    Compare `package.json` scripts (`cli`, `doctor`, `verify`, `check:biome`, `typecheck`, `test`) and `bin.av-denoiser` entry to D-01/D-11. Confirm `tsconfig.json` uses Bun-friendly options (`module` Preserve, `moduleResolution` bundler, `types` bun, `strict`, `noUncheckedIndexedAccess`). Confirm `biome.json` `files.includes` covers only `package.json`, `tsconfig.json`, `biome.json`, `src/**`, `test/**` per D-14 (no `.planning/**`). If anything diverges from CONTEXT without intentional repo evolution, restore alignment with the smallest diff; do not add `@clack/prompts` (deferred per RESEARCH).
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg '"av-denoiser"' package.json && rg 'src/cli/main.ts' package.json` shows bin mapping.
    - `rg 'verify' package.json` shows aggregate script chaining Biome + typecheck + test.
    - `rg 'noUncheckedIndexedAccess' tsconfig.json` is present.
    - `rg '\\.planning' biome.json` returns exit code 1 (no match in includes).
  </acceptance_criteria>
  <done>Package and toolchain files match Phase 1 locked decisions or carry a one-line comment in commit message explaining intentional deviation.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-run CLI entrypoints</name>
  <files>src/cli/main.ts, src/index.ts</files>
  <read_first>
    - src/cli/main.ts
    - src/index.ts
    - package.json
  </read_first>
  <action>
    Confirm `src/cli/main.ts` retains Bun shebang and `import.meta.main` guard. Confirm `src/index.ts` re-exports stable surfaces consumed by tests (`createCommandProgram`, domain types, adapters) without introducing duplicate CLI entrypoints. Run non-interactive smoke: `bun run cli --help` and `bun run cli` (default) must exit 0—if behavior regresses, fix `runCli`/`render` paths per D-02 (honest default message) without adding processing commands.
  </action>
  <verify>
    <automated>bun run cli --help >/dev/null && bun run cli >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `head -n 1 src/cli/main.ts | rg 'env bun'` succeeds.
    - `rg 'import\\.meta\\.main' src/cli/main.ts` succeeds.
    - `bun run cli --help 2>&1 | rg -i 'Usage:|Commands:|doctor'` exits 0.
  </acceptance_criteria>
  <done>CLI-01 install/run/help smoke passes via package scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → package scripts | Scripts must remain static argv templates; no interpolation of user paths into `package.json` script strings. |
| Repository tree → Biome | Lint/format inputs are confined to trusted repo paths (`src/`, `test/`), not arbitrary user HOME files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `package.json` scripts | mitigate | Keep scripts fixed literals; user-supplied paths enter only through Bun CLIs (`bun run cli …`), not shell concatenation inside npm scripts. |
| T-01-01-02 | Information disclosure | Toolchain metadata | accept | `tsconfig`/`biome` reveal stack choices only; no secrets belong in these files (ASVS L1 local tooling). |
| T-01-01-03 | Elevation of privilege | Misconfigured bin entry | mitigate | `bin` must point at `src/cli/main.ts` with Bun shebang so globally linked installs do not invoke a shell wrapper with injected commands. |
</threat_model>

<verification>
Run `bun run verify` after edits.
</verification>

<success_criteria>
CLI-01 holds: dependency install, Bun TS entrypoint, help/default invocation, and aggregate verification scripts remain coherent with CONTEXT decisions.
</success_criteria>
