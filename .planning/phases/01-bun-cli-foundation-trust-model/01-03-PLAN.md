---
phase: 01-bun-cli-foundation-trust-model
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/adapters/process-runner.ts
  - src/adapters/tool-discovery.ts
  - src/app/doctor.ts
  - src/app/run-command.ts
  - src/cli/main.ts
  - src/cli/command.ts
  - src/cli/render.ts
  - src/index.ts
  - docs/exit-codes.md
  - docs/doctor.md
  - test/adapters/tool-discovery.test.ts
  - test/app/doctor.test.ts
  - test/cli/command.test.ts
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - TRUST-01
  - TRUST-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-05-01T21-21-03
generated_at: 2026-05-01T21:28:04Z
must_haves:
  truths:
    - "User can run `bun run doctor` and receive a human-readable report of required and optional tool readiness."
    - "User receives documented exit code meanings in CLI behavior and checked-in documentation."
    - "External tools are invoked only through the safe process runner abstraction."
    - "Repo-native verification covers parser, domain, command-builder, adapter, and CLI behavior."
  artifacts:
    - path: src/adapters/process-runner.ts
      provides: "Single Bun.spawn-based process runner"
      contains: "Bun.spawn([command.executable, ...command.args]"
    - path: src/adapters/tool-discovery.ts
      provides: "Bun.which plus version-probe adapter for doctor"
      contains: "defaultToolDefinitions"
    - path: src/app/doctor.ts
      provides: "Doctor orchestration over tool discovery facts"
      contains: "createDoctorReport"
    - path: src/app/run-command.ts
      provides: "Typed request dispatcher"
      contains: "runCliRequest"
    - path: docs/exit-codes.md
      provides: "User-facing exit code documentation"
      contains: "fallbackRequired"
    - path: docs/doctor.md
      provides: "User-facing doctor documentation"
      contains: "required tools"
    - path: test/cli/command.test.ts
      provides: "CLI request and exit behavior coverage"
      contains: "unknown option"
  key_links:
    - from: src/cli/main.ts
      to: src/app/run-command.ts
      via: "typed CLI request dispatch"
      pattern: "runCliRequest"
    - from: src/app/doctor.ts
      to: src/adapters/tool-discovery.ts
      via: "tool fact collection"
      pattern: "discoverTools"
    - from: src/adapters/tool-discovery.ts
      to: src/adapters/process-runner.ts
      via: "version probe execution"
      pattern: "ProcessRunner"
    - from: docs/exit-codes.md
      to: src/domain/exit-codes.ts
      via: "same names and numeric values"
      pattern: "missingTools.*3"
---

<objective>
Wire the trust model into the CLI: safe process runner, doctor tool discovery, documented exit codes, and full verification.

Purpose: This completes Phase 1 outcomes by connecting pure trust-model types to Bun adapters and user-visible CLI/docs without adding real media processing, per D-01 through D-13.
Output: `doctor` command, safe runner adapter, tool discovery adapter, CLI rendering, user-facing docs, and verification coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-CONTEXT.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-RESEARCH.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-01-SUMMARY.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-02-SUMMARY.md
@.planning/research/PITFALLS.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md

<interfaces>
Plan 02 provides `ExitCode`, `ProcessCommand`, `ToolAvailability`, and `DoctorReport`.

This plan must create adapter contracts with dependency injection so tests can avoid machine-dependent tools:
```typescript
export type ProcessRunner = (command: ProcessCommand) => Promise<ProcessResult>;
export type ToolDiscoveryDeps = {
  readonly maybeWhich: (name: string) => string | null;
  readonly runProcess: ProcessRunner;
};
export async function discoverTools(deps?: Partial<ToolDiscoveryDeps>): Promise<DoctorReport>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add safe process runner and deterministic tool discovery</name>
  <files>src/adapters/process-runner.ts, src/adapters/tool-discovery.ts, src/index.ts, test/adapters/tool-discovery.test.ts</files>
  <read_first>
    - src/adapters/process-runner.ts (create if absent)
    - src/adapters/tool-discovery.ts (create if absent)
    - src/index.ts
    - test/adapters/tool-discovery.test.ts (create if absent)
    - src/domain/process-command.ts
    - src/domain/doctor-report.ts
    - .planning/phases/01-bun-cli-foundation-trust-model/01-RESEARCH.md
    - .planning/research/PITFALLS.md
  </read_first>
  <behavior>
    - Test 1: a fake `maybeWhich` returning `null` for `ffmpeg` produces a missing required `ffmpeg` fact.
    - Test 2: a fake runner returning `stdout: "ffmpeg version 8.1"` produces an available `ffmpeg` fact with version `ffmpeg version 8.1`.
    - Test 3: optional missing `sox_ng`, `sox`, `demucs`, `audacity`, and `melt` facts are included and marked optional.
    - Test 4: tool discovery includes `not-checked-yet` capability facts such as `ffmpeg.filters` and `sox.effects` without running those checks.
  </behavior>
  <action>
    Implement `ProcessResult` and `runProcessCommand` in `src/adapters/process-runner.ts` using `Bun.spawn([command.executable, ...command.args], { stdout: "pipe", stderr: "pipe", cwd: command.cwd, env: command.env, timeout: command.timeoutMs, stdin: command.stdin ?? "ignore" })`. Do not pass a string command or `shell: true`. Implement `discoverTools` in `src/adapters/tool-discovery.ts` using `Bun.which` by default and the safe runner for `--version` probes. Use exact version arguments: `ffmpeg -version`, `ffprobe -version`, `sox_ng --version`, `sox --version`, `demucs --help`, `audacity --version`, and `melt -version`. Only parse the first non-empty stdout or stderr line as the version string. Add injected fake dependencies in tests so routine tests do not require optional tools. Expected command output before implementation: `bun test test/adapters/tool-discovery.test.ts` fails because `discoverTools` is missing. Expected command output after implementation: the test exits `0`, and `rg 'Bun.spawn\\(\\[command.executable, ...command.args\\]' src/adapters/process-runner.ts` finds the safe invocation.
  </action>
  <verify>
    <automated>bun test test/adapters/tool-discovery.test.ts && ! rg 'shell:\\s*true|exec\\(' src/adapters src/app src/cli</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'Bun.spawn\\(\\[command.executable, ...command.args\\]' src/adapters/process-runner.ts` confirms argv-array execution.
    - `rg 'Bun.which' src/adapters/tool-discovery.ts` confirms Bun-native PATH lookup.
    - `rg 'ffmpeg.filters' src/adapters/tool-discovery.ts` confirms unverified capability reporting.
    - `! rg 'shell:\\s*true|exec\\(' src/adapters src/app src/cli` confirms no shell execution shortcuts.
  </acceptance_criteria>
  <done>The only effectful process execution adapter uses Bun argv arrays, and doctor tool discovery is tested with deterministic fakes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire doctor and exit outcomes into the CLI</name>
  <files>src/app/doctor.ts, src/app/run-command.ts, src/cli/main.ts, src/cli/command.ts, src/cli/render.ts, src/index.ts, test/app/doctor.test.ts, test/cli/command.test.ts</files>
  <read_first>
    - src/app/doctor.ts (create if absent)
    - src/app/run-command.ts (create if absent)
    - src/cli/main.ts
    - src/cli/command.ts
    - src/cli/render.ts
    - src/index.ts
    - test/app/doctor.test.ts (create if absent)
    - test/cli/command.test.ts (create if absent)
    - src/domain/exit-codes.ts
    - src/domain/command-outcome.ts
    - src/domain/doctor-report.ts
  </read_first>
  <behavior>
    - Test 1: `runCliRequest({ kind: "doctor", strict: false })` with fake discovery containing available `ffmpeg` and `ffprobe` returns a success outcome.
    - Test 2: `runCliRequest({ kind: "doctor", strict: false })` with missing required `ffmpeg` returns a missing-tools outcome.
    - Test 3: unknown CLI options are mapped to `ExitCode.invalidInput` instead of an uncaught Commander exit.
    - Test 4: `bun run cli` prints default guidance and exits `0`; `bun run src/cli/main.ts --unknown` exits `2`.
  </behavior>
  <action>
    Implement `createDoctorReport` in `src/app/doctor.ts` as a thin orchestration function over `discoverTools`. Implement `runCliRequest` in `src/app/run-command.ts` to handle `show-default`, `show-help`, and `doctor` requests, returning `CommandOutcome` only. Update `src/cli/main.ts` so it catches Commander parse errors, maps them through `ExitCode.invalidInput`, renders one diagnostic, and exits through `process.exit(code)` exactly once at the top level. Update `src/cli/render.ts` to render a doctor report with sections `Required tools`, `Optional tools`, and `Capabilities not checked in this phase`, plus exit-code names when a command fails. Do not add processing flags, input/output media flags, denoise presets, batch flags, or guided prompts. Expected command outputs: `bun run src/cli/main.ts --unknown` exits `2`; `bun run cli` exits `0` and includes `Run "av-denoiser doctor" to inspect local tool readiness.`; `bun run doctor` exits `0` when `ffmpeg` and `ffprobe` are found, otherwise exits `3`.
  </action>
  <verify>
    <automated>bun test test/app/doctor.test.ts test/cli/command.test.ts && bun run cli && (bun run src/cli/main.ts --unknown; test "$?" -eq 2)</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'runCliRequest' src/app/run-command.ts src/cli/main.ts` confirms typed request dispatch.
    - `rg 'Required tools' src/cli/render.ts` and `rg 'Optional tools' src/cli/render.ts` confirm structured doctor rendering.
    - `rg 'ExitCode.invalidInput' src/cli/main.ts` confirms Commander parse failures enter the exit taxonomy.
    - `! rg 'preset|batch|remux|denoise|guided' src/cli/command.ts` confirms deferred media/workflow surfaces are not exposed.
  </acceptance_criteria>
  <done>The executable CLI uses typed outcomes for default, help, invalid input, and doctor behavior with stable exit codes.</done>
</task>

<task type="auto">
  <name>Task 3: Document exit codes, doctor behavior, and run full verification</name>
  <files>docs/exit-codes.md, docs/doctor.md, package.json, src/adapters/process-runner.ts, src/adapters/tool-discovery.ts, src/app/doctor.ts, src/app/run-command.ts, src/cli/main.ts, src/cli/command.ts, src/cli/render.ts, src/index.ts, test/adapters/tool-discovery.test.ts, test/app/doctor.test.ts, test/cli/command.test.ts</files>
  <read_first>
    - docs/exit-codes.md (create if absent)
    - docs/doctor.md (create if absent)
    - package.json
    - src/domain/exit-codes.ts
    - src/domain/doctor-report.ts
    - src/adapters/process-runner.ts
    - src/adapters/tool-discovery.ts
    - src/app/doctor.ts
    - src/app/run-command.ts
    - src/cli/main.ts
    - src/cli/command.ts
    - src/cli/render.ts
    - test/adapters/tool-discovery.test.ts
    - test/app/doctor.test.ts
    - test/cli/command.test.ts
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Create `docs/exit-codes.md` with a table documenting exact names and values: `success` `0`, `internalError` `1`, `invalidInput` `2`, `missingTools` `3`, `planningFailure` `4`, `processingFailure` `5`, and `fallbackRequired` `6`. Create `docs/doctor.md` explaining that `doctor` reports `ffmpeg` and `ffprobe` as required, `sox_ng`, `sox`, `demucs`, `audacity`, and `melt` as optional, and capability rows such as `not-checked-yet` as intentionally unverified. Document command behavior: `bun run doctor` exits `0` when required tools are available and `3` when required tools are missing; `bun run src/cli/main.ts --unknown` exits `2`. Run `bun run format`, then `bun run verify`. Expected final command output: `bun run verify` exits `0`.
  </action>
  <verify>
    <automated>bun run format && bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'fallbackRequired\\s*\\|\\s*6' docs/exit-codes.md` confirms fallback exit documentation.
    - `rg 'ffmpeg.*required|ffprobe.*required' docs/doctor.md` confirms required tool documentation.
    - `rg 'not-checked-yet' docs/doctor.md` confirms capability humility documentation.
    - `bun run verify` exits `0`.
  </acceptance_criteria>
  <done>Exit codes and doctor behavior are documented, all Phase 1 tests and verification scripts pass, and no deferred media command surface has been introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv -> app dispatcher | User command syntax becomes typed application requests. |
| Tool discovery -> operating system | The CLI locates executables on PATH and runs version commands. |
| Process runner -> external executable | Project code invokes external media tools through `Bun.spawn`. |
| Diagnostics -> user terminal/docs | Local paths, versions, and exit statuses become visible output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering / Elevation of Privilege | `src/adapters/process-runner.ts` | mitigate | Use `Bun.spawn([command.executable, ...command.args])` only; test and grep for absence of `shell: true` and `exec(`. |
| T-01-09 | Spoofing | `src/adapters/tool-discovery.ts` | mitigate | Use `Bun.which` and include resolved executable paths in doctor facts so users can see which binary was checked. |
| T-01-10 | Repudiation | `src/cli/main.ts` | mitigate | Route invalid input, missing tools, and internal errors through named exit codes and document them in `docs/exit-codes.md`. |
| T-01-11 | Information Disclosure | `src/cli/render.ts` | mitigate | Render concise paths, versions, and capability statuses only; do not print full environment variables or unbounded stderr. |
| T-01-12 | Denial of Service | `src/adapters/process-runner.ts` | mitigate | Support `timeoutMs` in `ProcessCommand` and use a bounded version-probe timeout for doctor checks. |
</threat_model>

<verification>
Run `bun run format && bun run verify`, then manually inspect `bun run cli`, `bun run src/cli/main.ts --help`, `bun run doctor`, and `bun run src/cli/main.ts --unknown` exit behavior.
</verification>

<success_criteria>
Phase 1 Plan 03 succeeds when `doctor` reports tool readiness through structured facts, the CLI uses stable documented exit codes, all external command execution goes through the safe runner, and the aggregate verification command passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bun-cli-foundation-trust-model/01-03-SUMMARY.md`.
</output>
