---
phase: 02-media-probing-output-planning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/media-probe.ts
  - src/adapters/ffprobe.ts
  - src/index.ts
  - test/domain/media-probe.test.ts
  - test/adapters/ffprobe.test.ts
  - test/fixtures/ffprobe/minimal-audio.json
  - test/fixtures/ffprobe/minimal-video-audio.json
autonomous: true
requirements:
  - MEDIA-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 02-2026-05-01T22-28-29
generated_at: 2026-05-01T23:00:00Z
must_haves:
  truths:
    - "Structured probe results are derived from FFprobe JSON using Zod at the boundary before any planning logic runs."
    - "FFprobe is invoked only through argv-array ProcessCommand specs compatible with the Phase 1 process runner."
  artifacts:
    - path: src/domain/media-probe.ts
      provides: "Parsed media probe types and JSON parser entrypoint"
      contains: "parseFfprobeJson"
    - path: src/adapters/ffprobe.ts
      provides: "Build and run ffprobe JSON commands"
      contains: "createFfprobeJsonCommand"
    - path: test/fixtures/ffprobe/minimal-audio.json
      provides: "Fixture for parser tests"
      contains: "\"streams\""
---

<objective>
Deliver typed FFprobe JSON parsing and a thin adapter that runs `ffprobe` with `-print_format json -show_format -show_streams` (per D-01, D-02, D-03).

Purpose: Satisfy MEDIA-03 at the data layer so later plans can plan outputs from facts, not raw strings.
Output: Domain module `media-probe`, adapter `ffprobe`, fixtures, and Bun tests with injected process stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-media-probing-output-planning/02-CONTEXT.md
@.planning/phases/02-media-probing-output-planning/02-RESEARCH.md
@.planning/phases/01-bun-cli-foundation-trust-model/01-CONTEXT.md
@src/domain/process-command.ts
@src/adapters/process-runner.ts
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod schemas and parseFfprobeJson</name>
  <files>src/domain/media-probe.ts, src/index.ts, test/domain/media-probe.test.ts, test/fixtures/ffprobe/minimal-audio.json, test/fixtures/ffprobe/minimal-video-audio.json</files>
  <read_first>
    - src/domain/process-command.ts
    - .planning/phases/02-media-probing-output-planning/02-CONTEXT.md
    - .planning/phases/02-media-probing-output-planning/02-RESEARCH.md
    - package.json
  </read_first>
  <behavior>
    - Tests load fixture JSON from `test/fixtures/ffprobe/*.json` and assert `parseFfprobeJson` returns typed streams with `codec_type`, `codec_name`, `index` for each stream.
    - Test rejects invalid JSON and missing `streams` array with discriminated error kinds (no thrown strings from domain).
    - Test preserves optional fields used later: `format.duration`, stream `disposition`, `channels`, `sample_rate`.
  </behavior>
  <action>
    Add `src/domain/media-probe.ts` exporting Zod schemas and `MediaProbe` / `FfprobeParseError` tagged types. Implement `parseFfprobeJson(raw: string)` returning `{ ok: true, value } | { ok: false, error }` per D-02 (no exceptions for expected parse failures). Add two fixtures: audio-only file (single audio stream), video+audio (one video + one audio). Export new symbols from `src/index.ts`. Use Zod 4 patterns already in dependencies.
  </action>
  <verify>
    <automated>bun test test/domain/media-probe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'parseFfprobeJson' src/domain/media-probe.ts` exists.
    - `rg 'z\\.' src/domain/media-probe.ts` confirms Zod usage.
    - `rg 'test/fixtures/ffprobe' test/domain/media-probe.test.ts` confirms fixtures are read.
    - `test/fixtures/ffprobe/minimal-audio.json` contains valid ffprobe-like `"streams"` array with at least one audio stream object.
  </acceptance_criteria>
  <done>Probe JSON parses into explicit domain types with fixture-backed tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FFprobe adapter argv builder</name>
  <files>src/adapters/ffprobe.ts, test/adapters/ffprobe.test.ts, src/index.ts</files>
  <read_first>
    - src/domain/process-command.ts
    - src/adapters/process-runner.ts
    - src/domain/media-probe.ts
    - .planning/phases/02-media-probing-output-planning/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test: `createFfprobeJsonCommand({ ffprobePath: "/bin/ffprobe", inputPath: "in put.m4a" })` yields executable `/bin/ffprobe` and args including `-v`, `error`, `-print_format`, `json`, `-show_format`, `-show_streams`, and the input path as a single argv element.
    - Test: adapter function accepts injected `runProcess` returning fixed stdout JSON string; result matches `parseFfprobeJson` success path.
  </behavior>
  <action>
    Implement `createFfprobeJsonCommand(input: { readonly ffprobePath: string; readonly inputPath: string }): ProcessCommandResult` using `createProcessCommand` per D-03. Implement `runFfprobeProbe(deps)` that builds command, runs `runProcess`, maps stdout to `parseFfprobeJson`. No shell strings. Export from `src/index.ts` if needed by Plan 03 wiring.
  </action>
  <verify>
    <automated>bun test test/adapters/ffprobe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'createFfprobeJsonCommand' src/adapters/ffprobe.ts` exists.
    - `rg '-print_format' src/adapters/ffprobe.ts` confirms JSON format flag.
    - `rg 'createProcessCommand' src/adapters/ffprobe.ts` confirms argv-only construction.
    - `test/adapters/ffprobe.test.ts` contains a stub `runProcess` test double (no real ffprobe required).
  </acceptance_criteria>
  <done>FFprobe invocation is expressed as typed ProcessCommand and parseable via injection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input path -> ffprobe argv | File paths become argv elements only; no shell interpolation. |
| FFprobe stdout -> Zod parser | Untrusted JSON from external tool crosses into domain after schema validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Spoofing | `createFfprobeJsonCommand` | mitigate | Executable path comes from discovery/config later; argv uses literal `-print_format` / `-show_streams` flags, not user-controlled flag strings. |
| T-02-01-02 | Tampering | Parser | mitigate | Zod rejects unexpected shapes; domain returns structured parse errors instead of partial silent defaults for required fields. |
| T-02-01-03 | Denial of Service | Process runner | accept | Timeouts remain inherited from `ProcessCommand.timeoutMs` when set by caller; Plan 03 can set bounded probe timeouts. |
</threat_model>

<verification>
Run `bun test test/domain/media-probe.test.ts test/adapters/ffprobe.test.ts` and `bun run verify`.
</verification>

<success_criteria>
MEDIA-03 is satisfied at the parser/adapter layer: common container JSON shapes become structured `MediaProbe` values suitable for planning.
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-probing-output-planning/02-01-SUMMARY.md`.
</output>
