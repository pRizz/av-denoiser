---
phase: 02-media-probing-output-planning
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/domain/cli-request.ts
  - src/app/run-command.ts
  - src/cli/command.ts
  - src/cli/render.ts
  - src/app/inspect.ts
  - src/index.ts
  - test/app/inspect.test.ts
  - test/cli/command.test.ts
autonomous: true
requirements:
  - MEDIA-05
  - VIDEO-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 02-2026-05-01T22-28-29
generated_at: 2026-05-01T23:00:00Z
must_haves:
  truths:
    - "Users can run a CLI command that probes a file and prints a human-readable summary of modality, paths, chosen audio stream, and explicit codec/container defaults."
    - "Planned audio codec and container are visible in output, not implied by FFmpeg help text alone."
  artifacts:
    - path: src/cli/command.ts
      provides: "inspect subcommand"
      contains: "inspect"
    - path: src/app/inspect.ts
      provides: "Orchestration: discover ffprobe path, probe, plan"
      contains: "runInspectRequest"
    - path: src/cli/render.ts
      provides: "Human-readable plan summary"
      contains: "renderInspectPlan"
---

<objective>
Expose Phase 2 planning through the CLI (`inspect` subcommand), render human-readable summaries (D-08), and wire explicit codec/container fields (D-09, D-10) plus planning failures to `CommandOutcome` / exit codes.

Purpose: Close MEDIA-05 visibility and VIDEO-05 explicit defaults for users before any processing phase.
Output: Typed `CliRequest`, inspect orchestration, renderer updates, and tests with mocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/domain/cli-request.ts
@src/app/run-command.ts
@src/cli/command.ts
@src/cli/render.ts
@src/adapters/tool-discovery.ts
@src/domain/command-outcome.ts
@src/domain/exit-codes.ts
@.planning/phases/02-media-probing-output-planning/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CliRequest and Commander inspect command</name>
  <files>src/domain/cli-request.ts, src/cli/command.ts, test/cli/command.test.ts</files>
  <read_first>
    - src/domain/cli-request.ts
    - src/cli/command.ts
    - src/cli/main.ts
  </read_first>
  <behavior>
    - `inspect <input>` required positional or option `--input` — pick one style and document in help string.
    - Optional `--output`, `--force`, `--json` (when `--json`, print structured plan JSON via `JSON.stringify` on serializable plan DTO).
  </behavior>
  <action>
    Extend `CliRequest` with `{ kind: "inspect"; inputPath: string; maybeOutputPath?: string; force: boolean; json: boolean }`. Register `inspect` subcommand in `createCommandProgram` using Commander `.argument` or `.requiredOption`. Update `parseCliRequest` paths if needed. Add tests that parse argv into the new request shape (mirror existing parse testing approach if present, else add minimal tests).
  </action>
  <verify>
    <automated>bun test test/cli/command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'kind: \"inspect\"' src/domain/cli-request.ts` OR equivalent union member `inspect` appears in `cli-request.ts`.
    - `rg '\\.command\\(\"inspect\"\\)' src/cli/command.ts` OR `.command('inspect')` exists.
    - `test/cli/command.test.ts` asserts parsing `inspect` with sample argv yields `inspect` request.
  </acceptance_criteria>
  <done>Inspect requests parse deterministically from argv.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runInspectRequest orchestration and outcomes</name>
  <files>src/app/inspect.ts, src/app/run-command.ts, test/app/inspect.test.ts</files>
  <read_first>
    - src/app/run-command.ts
    - src/adapters/ffprobe.ts
    - src/adapters/tool-discovery.ts
    - src/domain/output-path.ts
    - src/domain/output-plan.ts
    - src/domain/command-outcome.ts
  </read_first>
  <behavior>
    - Missing ffprobe on PATH -> `{ kind: "failure", reason: { kind: "missing-tools", tools: ["ffprobe"] } }`.
    - Parse/plan failure -> `{ kind: "failure", reason: { kind: "planning-failure", message: string } }` mapping to `ExitCode.planningFailure`.
    - Success carries serializable summary fields for renderer.
  </behavior>
  <action>
    Implement `runInspectRequest(request, deps)` resolving ffprobe path via `Bun.which("ffprobe")` injected as dependency default. Chain `runFfprobeProbe` -> `parseFfprobeJson` success -> `resolveOutputPath` with `Bun.file(path).exists` injected -> `planMediaOutput`. Return outcome compatible with `renderCommandOutcome` extensions.
  </action>
  <verify>
    <automated>bun test test/app/inspect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'runInspectRequest' src/app/inspect.ts` exists.
    - `rg 'missing-tools' test/app/inspect.test.ts` covers missing ffprobe stub.
    - `rg 'planning-failure' src/app/inspect.ts` OR planning failure reason appears when path resolution fails.
    - `src/app/run-command.ts` switches on `request.kind === "inspect"` calling `runInspectRequest`.
  </acceptance_criteria>
  <done>Inspect path produces structured success/failure outcomes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Human-readable renderInspectPlan</name>
  <files>src/cli/render.ts, src/cli/main.ts</files>
  <read_first>
    - src/cli/render.ts
    - src/domain/output-plan.ts
  </read_first>
  <behavior>
    - Success prints labeled sections: Input, Output path, Modality, Selected audio stream index, Planned audio codec, Planned container, Reason codes (if any).
    - `--json` mode prints one JSON object with the same fields for scripts.
  </behavior>
  <action>
    Extend `renderCommandOutcome` (or companion) to format inspect success distinctly. Ensure modality and codec/container strings match enum values from `output-plan.ts` literally so acceptance checks stay grep-friendly.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'renderInspect|inspect' src/cli/render.ts` shows inspect-specific rendering.
    - `rg 'Modality|Planned audio codec' src/cli/render.ts` confirms human-readable labels for D-08.
    - `bun run verify` exits `0`.
  </acceptance_criteria>
  <done>Users can read planned modality and explicit codec/container choices before processing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inspect argv -> paths | Paths feed ffprobe and path resolver only; renderer prints facts, not evaluated shell code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | JSON output | accept | JSON mode prints derived media facts only; no secret env dumping. |
| T-02-03-02 | Tampering | Path injection | mitigate | FFprobe argv passes path as single argument; document no shell expansion. |
</threat_model>

<verification>
Run `bun run verify` and smoke `bun run src/cli/main.ts inspect --help`.
</verification>

<success_criteria>
MEDIA-05 user visibility and VIDEO-05 explicit planning defaults are exposed via `inspect` without running denoise.
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-probing-output-planning/02-03-SUMMARY.md`.
</output>
