---
phase: 02-media-probing-output-planning
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/domain/cli-request.ts
  - src/cli/command.ts
  - src/app/inspect.ts
  - src/cli/render.ts
  - test/cli/command.test.ts
  - test/app/inspect.test.ts
autonomous: true
requirements:
  - MEDIA-05
  - VIDEO-05
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 02-replan-2026-05-02T001500Z
generated_at: "2026-05-02T00:15:00.000Z"
must_haves:
  truths:
    - "`av-denoiser inspect <path>` resolves to `CliRequest` kind `inspect` with flags `-o/--output`, `--force`, `--json` per D-11."
    - "Human-readable inspect output shows modality, resolved paths, selected audio stream index, and explicit planned codec/container (MEDIA-05 + VIDEO-05 visibility)."
    - "`--json` prints `InspectPlanSummary` JSON matching domain fields (stable keys for scripting)."
  artifacts:
    - path: src/cli/command.ts
      provides: "inspect subcommand registration"
    - path: src/app/inspect.ts
      provides: "runInspectRequest orchestration"
    - path: src/domain/inspect-summary.ts
      provides: "InspectPlanSummary DTO"
    - path: src/cli/render.ts
      provides: "renderInspectPlanText + JSON branch"
    - path: test/cli/command.test.ts
      provides: "Argv → inspect request regression"
    - path: test/app/inspect.test.ts
      provides: "Inspect outcomes regression"
  key_links:
    - from: "run-command.ts"
      to: "runInspectRequest"
      via: "request.kind === \"inspect\""
      pattern: "runInspectRequest"
---

<objective>
Re-lock **MEDIA-05** and **VIDEO-05** user visibility: `inspect` CLI wiring, structured summaries, and rendering—aligned with D-08/D-09/D-11—without adding denoise execution.

Purpose: End-to-end inspection is implemented; this wave prevents regressions in argv parsing, outcome taxonomy, and plaintext/JSON parity for planned codec/container.
Output: Green CLI/app tests, `bun run verify`, and grep-verified labels or JSON keys on summary objects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-media-probing-output-planning/02-CONTEXT.md
@src/domain/cli-request.ts
@src/cli/command.ts
@src/app/run-command.ts
@src/app/inspect.ts
@src/domain/inspect-summary.ts
@src/cli/render.ts
@src/domain/command-outcome.ts
@src/domain/exit-codes.ts

<interfaces>
Inspect request shape from `src/domain/cli-request.ts` (representative):
```typescript
{ kind: "inspect"; inputPath: string; maybeOutputPath?: string; force: boolean; json: boolean }
```
`runInspectRequest` returns `InspectCliOutcome` extending `CommandOutcome` with optional `inspect: { json, summary }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI argv regression for inspect (D-11)</name>
  <files>src/domain/cli-request.ts, src/cli/command.ts, test/cli/command.test.ts</files>
  <read_first>
    - .planning/phases/02-media-probing-output-planning/02-CONTEXT.md (D-11)
    - src/domain/cli-request.ts
    - src/cli/command.ts
    - test/cli/command.test.ts
    - src/cli/main.ts
  </read_first>
  <action>
    Verify Commander wiring: subcommand name `inspect`, positional media path, options `-o, --output`, `--force`, `--json`. Extend **tests only** if coverage misses a flag combination (minimum: default parse, `--output`, `--force`, `--json` together). Run smoke `bun run src/cli/main.ts inspect --help` and confirm help text mentions inspect purpose (no wording overhaul—presence check only).
  </action>
  <verify>
    <automated>bun test test/cli/command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\\.command\\(\"inspect\"\\)|\\.command\\('inspect'\\)" src/cli/command.ts` finds registration.
    - `rg -n "kind: \"inspect\"|kind: 'inspect'" test/cli/command.test.ts` proves parsed shape assertions exist.
    - `rg -n "--json|--force|--output|-o" src/cli/command.ts` shows flags defined in source.
    - `bun run src/cli/main.ts inspect --help` exits 0 and stdout contains `inspect`.
  </acceptance_criteria>
  <done>Inspect CLI surface matches CONTEXT flags and is test-backed.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestration + render parity for modality and VIDEO-05 fields</name>
  <files>src/app/inspect.ts, src/cli/render.ts, test/app/inspect.test.ts</files>
  <read_first>
    - src/app/inspect.ts
    - src/domain/inspect-summary.ts
    - src/domain/output-plan.ts
    - src/cli/render.ts
    - test/app/inspect.test.ts
  </read_first>
  <action>
    Assert `runInspectRequest` success path exposes `outputPlanToInspectSummary` fields including `modality`, `plannedAudioCodec`, `plannedContainer`, and `selectedAudioStreamIndex` where applicable. Add **one** test that sets `json: true` and verifies stringified JSON contains `"plannedAudioCodec":"aac"` and `"plannedContainer":"mp4"` for the minimal-audio fixture success path (or equivalent parsed object compare). In `renderInspectPlanText`, grep for human-visible labels covering modality and planned codec/container (e.g. labels mentioning modality and planned codec/container—do not rename user-visible strings unless inconsistent with summary DTO).
  </action>
  <verify>
    <automated>bun test test/app/inspect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "plannedAudioCodec|plannedContainer|modality" test/app/inspect.test.ts` hits assertions on summary fields.
    - New or existing JSON-mode assertion: `rg -n "json: true" test/app/inspect.test.ts` paired with `aac`/`mp4` expectation.
    - `rg -n "renderInspectPlanText" src/cli/render.ts` and plaintext output includes distinct lines or labels for modality and for planned audio codec and container (three grep-able substrings in function body).
    - `rg -n "missing-tools|planning-failure" test/app/inspect.test.ts` confirms failure taxonomy remains covered.
  </acceptance_criteria>
  <done>Users can see modality and explicit codec/container defaults via text or JSON before any processing phase.</done>
</task>

<task type="auto">
  <name>Task 3: Repository verify gate</name>
  <files>package.json</files>
  <read_first>
    - package.json (`scripts.verify`)
    - AGENTS.md (## Repo-Local Guidance if present)
  </read_first>
  <action>
    Run full `bun run verify` after Tasks 1–2; fix **only** Phase-2-owned violations surfaced (Biome/tsc/tests)—do not scope-creep into unrelated phases.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `bun run verify` exits code 0.
  </acceptance_criteria>
  <done>Phase 2 regression slice passes aggregate verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv → file paths | Paths passed to `runFfprobeProbe` and `resolveOutputPath` only; never evaluated as shell code. |
| inspect `--json` stdout | Structured facts derived from probe/plan; must not dump environment secrets or raw ffprobe JSON blob (D-11: no raw ffprobe dump flag). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Path injection | mitigate | Subprocess argv passes paths as single arguments; renderer prints escaped plain text/JSON via `JSON.stringify` on DTO (no HTML/script context). |
| T-02-03-02 | Information Disclosure | Logs/stderr | mitigate | Failure rendering uses `CommandOutcome` patterns from Phase 1; avoid printing full probe stderr blobs twice—keep messages concise (adjust only if verify reveals leakage beyond existing patterns). |
| T-02-03-03 | Denial of Service | Local probe | accept | User invokes inspect locally; resource abuse mitigations (timeouts) remain optional enhancements tied to `ProcessCommand` usage in inspect deps. |
</threat_model>

<verification>
Run `bun test test/cli/command.test.ts test/app/inspect.test.ts` and `bun run verify`; manual smoke: `bun run src/cli/main.ts inspect --help`.
</verification>

<success_criteria>
MEDIA-05 and VIDEO-05 are user-visible through `inspect` in plaintext and JSON modes with automated verification.
</success_criteria>

<output>
After completion, create or refresh `.planning/phases/02-media-probing-output-planning/02-03-SUMMARY.md`.
</output>
