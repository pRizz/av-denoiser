---
phase: 02-media-probing-output-planning
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/domain/output-path.ts
  - src/domain/output-plan.ts
  - src/index.ts
  - test/domain/output-path.test.ts
  - test/domain/output-plan.test.ts
autonomous: true
requirements:
  - MEDIA-04
  - MEDIA-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 02-2026-05-01T22-28-29
generated_at: 2026-05-01T23:00:00Z
must_haves:
  truths:
    - "Planned outputs cannot equal the source path without an explicit future dangerous mode, and collisions require force."
    - "Every plan carries a modality tag with machine-readable reasons before execution phases."
  artifacts:
    - path: src/domain/output-path.ts
      provides: "Collision-safe output path resolution"
      contains: "resolveOutputPath"
    - path: src/domain/output-plan.ts
      provides: "Output modality union and planning from probe"
      contains: "planMediaOutput"
---

<objective>
Implement pure output path resolution (D-04, D-05, D-06) and core output-plan modality classification from `MediaProbe` (D-07).

Purpose: Satisfy MEDIA-04 and the planning-time portion of MEDIA-05 without CLI wiring.
Output: `output-path` and `output-plan` domain modules with exhaustive tests using synthetic probes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-media-probing-output-planning/02-CONTEXT.md
@.planning/phases/02-media-probing-output-planning/02-RESEARCH.md
@src/domain/media-probe.ts
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveOutputPath with collision and source equality rules</name>
  <files>src/domain/output-path.ts, test/domain/output-path.test.ts, src/index.ts</files>
  <read_first>
    - .planning/phases/02-media-probing-output-planning/02-CONTEXT.md
    - src/domain/media-probe.ts
  </read_first>
  <behavior>
    - When `explicitOutputPath` is undefined, derive default using stem + literal suffix constant `.avdn` + preserved extension from input (example: `clip.m4a` -> `clip.avdn.m4a` documented in code comment).
    - When output path resolves equal to canonical input path: return planning failure kind `output-equals-input` (no write).
    - When target exists and `force` false: failure kind `output-exists`.
    - When `force` true and output exists: success.
  </behavior>
  <action>
    Implement pure functions using `node:path` (`resolve`, `normalize`) — Bun supports Node path. Accept `inputPath`, `maybeExplicitOutput`, `force`, and `doesOutputExist(path): boolean` as injected predicate for testability per D-04/D-05/D-06. Export types for success/failure outcomes as tagged unions.
  </action>
  <verify>
    <automated>bun test test/domain/output-path.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'output-equals-input' src/domain/output-path.ts` documents the forbidden same-path case.
    - `rg 'output-exists' src/domain/output-path.ts` documents collision failure.
    - `rg '\\.avdn\\.' test/domain/output-path.test.ts` OR default suffix appears in tests proving deterministic naming.
    - `rg 'Arrange' test/domain/output-path.test.ts` confirms structured tests.
  </acceptance_criteria>
  <done>Path rules encode MEDIA-04 without filesystem side effects in domain tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: planMediaOutput modality union from probe</name>
  <files>src/domain/output-plan.ts, test/domain/output-plan.test.ts, src/index.ts</files>
  <read_first>
    - src/domain/media-probe.ts
    - src/domain/output-path.ts
    - .planning/phases/02-media-probing-output-planning/02-CONTEXT.md
  </read_first>
  <behavior>
    - Given probe with no audio streams: modality `unsupported` with reason code `no-audio-stream`.
    - Given audio + video: modality `video-copy-safe` at minimum for Phase 2 stub (document that detailed fallback matrix arrives Phase 3) OR `fallback-required` when container hints are stubbed — pick one documented stub rule in code comment and test it.
    - Given audio-only input (no video): modality `audio-only`.
    - Selected audio stream follows D-10 order: default disposition first, else first audio, else highest channel count tie-break.
  </behavior>
  <action>
    Implement `planMediaOutput(input: { probe: MediaProbe; pathOutcome: OutputPathSuccess })` returning `OutputPlan` tagged union including fields: `modality`, `reasonCodes[]`, `selectedAudioStreamIndex`, planned container/codec placeholders (`plannedAudioCodec`, `plannedContainer`) set to explicit default enum values per D-09 (define enums in this file). Connect path outcome so plan references resolved output path string.
  </action>
  <verify>
    <automated>bun test test/domain/output-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'audio-only|video-copy-safe|fallback-required|unsupported' src/domain/output-plan.ts` lists modality variants.
    - `rg 'selectedAudioStreamIndex' src/domain/output-plan.ts` exists.
    - `rg 'no-audio-stream' test/domain/output-plan.test.ts` covers unsupported case.
    - Tests build minimal `MediaProbe` objects without FFprobe JSON strings.
  </acceptance_criteria>
  <done>MEDIA-05 classification exists as pure data for CLI rendering in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Path strings -> canonical comparison | Prevents accidental overwrite of source when paths differ lexically but resolve identically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | `resolveOutputPath` | mitigate | Normalize paths before equality check; reject output === input. |
| T-02-02-02 | Elevation | Force flag | accept | Overwrite allowed only when caller passes `force: true` (CLI maps from `--force` in Plan 03). |
</threat_model>

<verification>
Run `bun test test/domain/output-path.test.ts test/domain/output-plan.test.ts` and `bun run verify`.
</verification>

<success_criteria>
Output paths and modality unions are pure, tested, and ready for CLI presentation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-probing-output-planning/02-02-SUMMARY.md`.
</output>
