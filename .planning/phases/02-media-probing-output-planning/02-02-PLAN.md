---
phase: 02-media-probing-output-planning
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - test/domain/output-path.test.ts
  - test/domain/output-plan.test.ts
autonomous: true
requirements:
  - MEDIA-04
  - MEDIA-05
  - VIDEO-05
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 02-replan-2026-05-02T001500Z
generated_at: "2026-05-02T00:15:00.000Z"
must_haves:
  truths:
    - "Output path resolution rejects output identical to input and rejects existing outputs unless `force` is true (MEDIA-04)."
    - "Default sibling naming uses literal suffix segment `avdn` before the preserved extension."
    - "`planMediaOutput` exposes modality union with machine-readable `reasonCodes` before execution (MEDIA-05)."
    - "Supported plans carry explicit `plannedAudioCodec: \"aac\"` and `plannedContainer: \"mp4\"` defaults (VIDEO-05); unsupported omits codec/container fields."
  artifacts:
    - path: src/domain/output-path.ts
      provides: "resolveOutputPath"
      contains: "DEFAULT_OUTPUT_SUFFIX_SEGMENT"
    - path: src/domain/output-plan.ts
      provides: "planMediaOutput + OutputModality"
      contains: "plannedAudioCodec"
    - path: test/domain/output-path.test.ts
      provides: "Path rule regression lock"
    - path: test/domain/output-plan.test.ts
      provides: "Modality + codec regression lock"
  key_links:
    - from: "planMediaOutput"
      to: "selectAudioStream"
      via: "default disposition then index then channels"
      pattern: "selectAudioStream\\("
---

<objective>
Re-lock **MEDIA-04**, **MEDIA-05**, and **VIDEO-05** at the pure planning layer: output path safety, modality labeling, and explicit planned codec/container defaults—as implemented—without expanding Phase 3 fallback semantics.

Purpose: CONTEXT states Phase 2 stub marks mixed A/V as `video-copy-safe` with `phase-2-stub-video-copy-safe`; plans must verify that contract and prevent silent drift of defaults (`aac`/`mp4`).
Output: Green domain tests, grep-verified constants and reason codes, and optional minimal test additions only where D-10 ordering lacks assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-media-probing-output-planning/02-CONTEXT.md
@.planning/phases/02-media-probing-output-planning/02-RESEARCH.md
@.planning/REQUIREMENTS.md
@src/domain/output-path.ts
@src/domain/output-plan.ts
@src/domain/media-probe.ts

<interfaces>
From `src/domain/output-path.ts`:
```typescript
export const DEFAULT_OUTPUT_SUFFIX_SEGMENT = "avdn";
export function resolveOutputPath(input: ResolveOutputPathInput): ResolveOutputPathResult;
```
Failure kinds: `output-equals-input`, `output-exists`.

From `src/domain/output-plan.ts`:
```typescript
export type OutputModality = "audio-only" | "video-copy-safe" | "fallback-required" | "unsupported";
export function planMediaOutput(input: PlanMediaOutputInput): OutputPlan;
```
Phase 2 stub: audio+video → `video-copy-safe` + reason `phase-2-stub-video-copy-safe`; audio-only → `phase-2-stub-audio-only`; no audio → `unsupported` + `no-audio-stream`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression-lock output path safety (MEDIA-04)</name>
  <files>src/domain/output-path.ts, test/domain/output-path.test.ts</files>
  <read_first>
    - .planning/phases/02-media-probing-output-planning/02-CONTEXT.md (D-04, D-05, D-06)
    - src/domain/output-path.ts
    - test/domain/output-path.test.ts
  </read_first>
  <action>
    Verify `resolveOutputPath` behavior matches CONTEXT: `output-equals-input` when canonical output equals canonical input; `output-exists` when `doesOutputExist` is true and `force` is false; success when forcing overwrite; default derived path inserts `.${DEFAULT_OUTPUT_SUFFIX_SEGMENT}.` before extension (e.g. `clip.m4a` → contains `.avdn.m4a`). Add or adjust tests **only** if any branch lacks an assertion (e.g. relative vs absolute input normalization); avoid behavioral changes unless a test proves a CONTEXT violation.
  </action>
  <verify>
    <automated>bun test test/domain/output-path.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "DEFAULT_OUTPUT_SUFFIX_SEGMENT|avdn" src/domain/output-path.ts` confirms suffix constant.
    - `rg -n "output-equals-input|output-exists" src/domain/output-path.ts test/domain/output-path.test.ts` hits both domain and tests.
    - `rg -n "\\.avdn\\." test/domain/output-path.test.ts` OR equivalent assertion on resolved output containing `.avdn.`.
    - `bun test test/domain/output-path.test.ts` exits 0.
  </acceptance_criteria>
  <done>MEDIA-04 path rules remain deterministic and test-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Regression-lock modality + VIDEO-05 defaults + D-10 selection</name>
  <files>src/domain/output-plan.ts, test/domain/output-plan.test.ts</files>
  <read_first>
    - .planning/phases/02-media-probing-output-planning/02-CONTEXT.md (D-07, D-09, D-10)
    - src/domain/output-plan.ts
    - src/domain/media-probe.ts (stream shape)
    - test/domain/output-plan.test.ts
  </read_first>
  <action>
    Confirm tests cover: (a) no audio → `unsupported` + `no-audio-stream`; (b) audio-only → `audio-only` + `phase-2-stub-audio-only`; (c) audio+video → `video-copy-safe` + `phase-2-stub-video-copy-safe`; (d) successful plans set `plannedAudioCodec === "aac"` and `plannedContainer === "mp4"`; (e) `unsupported` plan omits codec/container fields per union. Add **one** focused test proving D-10 preference order if missing: two audio streams where only the lower-index stream has `disposition.default === 1` must select that stream’s `index` even if another stream has more channels.
  </action>
  <verify>
    <automated>bun test test/domain/output-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase-2-stub-video-copy-safe|phase-2-stub-audio-only|no-audio-stream" test/domain/output-plan.test.ts` matches all three reason patterns across tests.
    - `rg -n 'plannedAudioCodec|plannedContainer|"aac"|"mp4"' test/domain/output-plan.test.ts` proves defaults asserted on success paths.
    - `rg -n "video-copy-safe|audio-only|unsupported" src/domain/output-plan.ts` lists modality handling.
    - If D-10 test added: `rg -n "disposition" test/domain/output-plan.test.ts` includes multi-stream case asserting `selectedAudioStreamIndex` equals default-marked stream.
  </acceptance_criteria>
  <done>MEDIA-05 and VIDEO-05 planning facts remain explicit and test-locked; Phase 3 stub commentary in `output-plan.ts` stays accurate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Path strings → canonical compare | Prevents lexically different paths from bypassing `output-equals-input`. |
| Planned output path → future writers | Phase 2 only plans; executors must never write until later phases validate destinations again. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | `resolveOutputPath` | mitigate | Normalize via `resolve`/`normalize` from `node:path` before equality checks; reject identical canonical input/output. |
| T-02-02-02 | Repudiation | `force` flag | accept | Overwrite permitted only when caller passes `force: true` (CLI `--force` in inspect); default denies collision. |
| T-02-02-03 | Elevation | Codec/container enums | mitigate | Closed unions `PlannedAudioCodec` / `PlannedContainer`; defaults literal `"aac"`/`"mp4"` in planner—not parsed from user strings in Phase 2. |
</threat_model>

<verification>
Run `bun test test/domain/output-path.test.ts test/domain/output-plan.test.ts` then `bun run verify`.
</verification>

<success_criteria>
MEDIA-04, MEDIA-05, and VIDEO-05 remain satisfied at the pure planning layer with stub modality semantics documented and locked by tests.
</success_criteria>

<output>
After completion, create or refresh `.planning/phases/02-media-probing-output-planning/02-02-SUMMARY.md`.
</output>
